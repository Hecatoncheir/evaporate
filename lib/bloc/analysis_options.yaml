# Здесь пишут на диск и ходят в сеть: брошенная без `await` и без
# `unawaited(...)` задача не падает, а тихо не успевает. Вне этих папок
# правило шумело бы — виджеты законно зовут асинхронное из обработчиков
# нажатий, — поэтому включено только тут.
include: ../../analysis_options.yaml

linter:
  rules:
    - discarded_futures
