# Загрузки

Подробности и история решений, вынесенные из `CLAUDE.md`: там — короткие
правила, здесь — почему они такие.

`DownloadEngine` — контракт, `DtorrentEngine` — единственная реализация
(`dtorrent_task_v2`, чистый Dart, внешнего бинарника нет). Выбран ради SOCKS5:
через него идёт и обмен с пирами (`useForPeers: true`), тогда как aria2 SOCKS не
поддерживает вовсе. Смена прокси перезапускает активные задачи.

`dtorrent_task_v2` берётся **форком** (`Hecatoncheir/dtorrent_task_v2`,
коммит в `pubspec.yaml` закреплён): в 0.5.4 один из зашитых корневых узлов
DHT разрешается в `0.0.0.0`, отправка туда роняет UDP-сокет, а следующая
попытка возвращает порт уже закрытого — DHT выключался целиком. Правка
предложена автору; когда он её примет, зависимость вернётся на pub. Правки
форка идут в его репозиторий, а не сюда: так их можно отдать наверх обычным
PR, а этот репозиторий остаётся про приложение. Сверх DHT в форке —
приватность при SOCKS5 и пределы скорости (см. ниже) и поиск перемещённых
файлов: он шёл обходом всей папки игр на каждый недостающий файл свежей
раздачи и привязывал файл к одноимённому того же размера у **другой** игры
(`steam_appid.txt`, установщики библиотек). Теперь при старте ищет только
раздача с прогрессом, одним обходом, по точному имени, а неоднозначное не
привязывает вовсе.

**Прокси применяется перехватом создания `HttpClient`**
(`ProxyHttpOverrides`, ставится в `main`), а не настройкой каждого клиента.
Причина чужая: объявление трекеру внутри `dtorrent_task_v2` заводит клиента
само и о `ProxyConfig` не знает — его флаг `useForTrackers` читают только
пути паузы и scrape. Дотянуться до объявления больше неоткуда, а без него
раздача с закрытым у провайдера трекером теряет главный источник пиров.

Где о прокси решают отдельно — каталог Steam и база путей сохранений с их
флагом `useForSteam` — клиент берётся через `directHttpClient()`, мимо
перехвата. Своего применения прокси у них больше нет.

**Обновление ходит через прокси, как весь остальной HTTP** (`updateHttpClient`).
Прежде оно брало прямой клиент, а комментарий уверял, что оно «спрашивает
свои флаги», которых не было: решение было ничьим. Запрос к GitHub выдаёт,
что человек пользуется Evaporate, и включивший прокси ради скрытности вправе
ждать, что и он уйдёт через прокси. Своего выключателя, как у каталогов, у
обновления нет — там выбор «видно ли Steam мой адрес» против «работает ли
поиск обложек», здесь такого выбора нет.

Настройки прокси и его адрес ставятся одной парой, когда имя разрешилось, и
поздний ответ DNS от прежних настроек отбрасывается поколением. Не
разрешилось — прокси заблокирован, и разрешение повторяется каждые 30 секунд
(`retryDelay`): автозапуск бывает раньше сети.

**Прокси, до которого не дотянуться, отказывает, а не пропускает мимо
себя.** Имя разрешается один раз, при `apply`; не разрешилось — клиенту
подменяется способ соединяться, и всякий запрос падает
`ProxyUnreachableException`. Прежде здесь возвращался обычный клиент, и
запросы шли напрямую: объявление трекеру уходило с настоящим адресом
человека, а он видел включённый переключатель и строку в журнале, которую
никто не читает. Прокси включают ради скрытности, и молчаливый переход на
прямые запросы — худшее, что можно сделать с таким намерением.

Куда уходит HTTP прямо сейчас, видно снаружи: `ProxyHttpOverrides.routing`
— `direct`, `through` или `blocked`. Слушают его загрузки и на `blocked`
говорят человеку сообщением: у службы ни `Notice`, ни языка, а знать об
отказе должен тот, кто прокси включил.

**Перехват держит только HTTP, и остальное при прокси решено отдельно.**
`udp://`-трекеры библиотека шлёт своим сокетом, `ws(s)://` — статическим
`WebSocket`: при включённом прокси их у задачи нет (`announcesFor`, исходная
модель с полным списком остаётся для выгрузки; форк отбрасывает их и сам).

**При SOCKS5 для пиров мимо прокси не идёт ничего** — это держит форк, а
не приложение. SOCKS5 там несёт только TCP, поэтому задача и поиск
метаданных magnet (`MetadataDownloader` получает тот же `ProxyConfig`) не
поднимают DHT и LSD, отказываются от uTP и holepunch и не объявляются
UDP- и WS-трекерам. Пиров тогда дают только HTTP-трекеры, и magnet без
них отказывает словами сразу (`canFetchMetadata`), а не ждёт десять минут.
Прежде обещание «для приватности выбирайте SOCKS5» было шире кода вдвойне:
DHT шёл мимо прокси, а прокси, данный задаче при создании, **до пиров не
доезжал вовсе** — менеджер пиров получал его только из `setProxyConfig`,
которого приложение не зовёт. Сторожит это `proxy_privacy_test` форка.

**Контракт описывает всё, что движку поручают, — и это не придирка.**
Пока половины вызовов в нём не было (`verify`, `reorder`, `setProxy`,
`pumpQueue`, `torrentPathFor`), `DownloadsBloc` держал движок по
конкретному типу и заводил его сам, а значит, проверить его без живых
раздач было нельзя: из тринадцати событий тесты доходили до двух — ни
запроса загрузки, ни паузы, ни отмены, ни перестановки. Теперь движок
приходит параметром, а `FakeDownloadEngine` (`test/support/`) записывает
поручения и отвечает тем, что задал тест. Отсюда же переезд `pumpQueue` и
`reorder` из расширения `EngineQueue` в сам класс: членами интерфейса
расширения не бывают.

**Сам движок разложен по тому, что делит его состояние, а что нет.**
Правила очереди (`DownloadQueue`) и перевод наших настроек на язык
библиотеки (`torrent_task_options.dart`: конфиг прокси и трекеры при нём)
— отдельно и проверяются без единой раздачи.
Подъём задачи (`EngineQueue`) и файл сессии (`_EngineStore`) остались
расширениями: они делят с движком карту загрузок, очередь и запись
файла — ровно тот случай, когда расширение уместнее класса.

**Пределы скорости — два общих ограничителя на весь движок**
(`BandwidthLimiter` форка), а не предел на задачу: «не быстрее 2 МБ/с» —
одна скорость на приложение. Каждая задача получает те же два
(`setBandwidthLimiters`), а `applyLimits` меняет их скорость — и сразу у
всех, в том числе уже идущих. Приём тормозится паузой чтения сокета после
принятого блока (TCP сам придерживает отправителя), отдача — отложенной
отправкой куска. Окно расписания библиотеки (`addScheduleWindow`) не
зовётся нарочно: скорость из него она не читала вовсе, а постановка окна
снимала паузу, и приостановленные загрузки начинали качать на полную.
`dtorrent_engine_test` это сторожит.

Имена времён aria2 из кода убраны, и осталось от них ровно одно, намеренно:
`DownloadLink.fromJson` (часть `Game`) читает старый ключ `downloadGid`
наравне с `downloadTaskId` — библиотеки, записанные до переименования,
лежат у людей на дисках. Ветку прикрывают тесты в `library_bloc_test.dart`
и `model_roundtrip_test.dart`; удалять её нельзя, пока такие библиотеки
живы.

Формат `.torrent` приложение читает и пишет само — `TorrentFile`
(`lib/services/download/torrent_file.dart`), поверх `b_encode_decode`.
Разбору из `dtorrent_task_v2` доверять здесь нельзя: **infohash он не
считает, а угадывает** — ищет в файле байты `info` и принимает за границы
словаря первые же `d` и `e`, которых полно внутри двоичных хешей кусков. На
настоящей раздаче он промахивается всегда, а с чужим infohash её не узнают
ни трекер, ни пир. Поэтому `TorrentSource.fromBytes` пересчитывает
хеш по вырезанным байтам info-словаря и заодно читает имя как UTF-8 (там оно
разбирается байт за символ). Вырезать, а не пересобирать: у файла,
записанного не по канону, перекодирование дало бы другой хеш.

По magnet-ссылке приходит **голый info-словарь** (BEP 9), а не торрент:
вокруг него собирается настоящий файл, туда же переезжают трекеры из ссылки,
и результат ложится в `torrentsDir/<infohash>.torrent`. Оттуда его берут
и перезапуск (не искать метаданные в сети заново), и выгрузка со страницы
игры. Где искать файл раздачи, знает `TorrentExport`: своя копия по id игры,
файл движка по infohash, исходный `.torrent` пользователя — в этом порядке,
от нашего к чужому.
