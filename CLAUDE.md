# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Evaporate — десктопный лончер игр на Flutter (macOS / Linux / Windows): библиотека,
встроенный BitTorrent-клиент и перенос сохранений между устройствами. Каталога
контента в приложении нет — источник каждой игры задаёт пользователь.

## Команды

```bash
flutter pub get   # обязателен первым: он же генерирует lib/l10n/app_localizations*.dart
flutter test
flutter test test/save_manager_test.dart      # один файл
flutter test --plain-name 'метка правила'     # один тест или группа по имени
flutter run -d macos
```

Сгенерированные `lib/l10n/app_localizations*.dart` не в репозитории, и
**неявный `pub get` внутри `flutter test` их не создаёт** — на свежем клоне без
явного `flutter pub get` (или `flutter gen-l10n`) всё, что импортирует
локализацию, не соберётся. То же после правки `.arb`.

Перед отправкой правки — ровно то, что гоняет CI (`.github/workflows/ci.yml`):

```bash
dart format --output=none --set-exit-if-changed lib test tool
flutter analyze
flutter test
```

`tool` в списке не для красоты: `tool/check_coverage.dart` импортируется из
`coverage_report_test.dart`, а `tool/changelog_notes.dart` запускается на
теге — это обычный код проекта, и формат с него спрашивают так же.

**Анализатор считает провалом и подсказки (info), не только ошибки.** Смотрите
весь его вывод, а не отфильтрованный по слову `error`.

Прочее:

```bash
python3 tool/make_icon.py        # перерисовать иконки (png, .ico, набор для macOS)
python3 tool/fetch_manifest.py   # положить настоящий манифест Ludusavi в third_party/
flutter build macos --release    # linux / windows аналогично
```

Тесты не требуют ни сети, ни геймпада, ни собранного приложения. Часть
пропускается по платформе (реестр — только на Windows, `.app` — только на
macOS), это норма: **прогон идёт на всех трёх системах**, поэтому пропущенное
здесь выполняется там. `ludusavi_manifest_bench_test.dart` пропускается без
скачанного манифеста.

CI: `analyze` (формат и анализатор, ubuntu) и `test` — матрица из ubuntu,
macOS и Windows с `fail-fast: false`.

**Покрытие — порог, а не отчёт.** Снимается один раз, на ubuntu, уходит в
сводку прогона и в артефакт `coverage-<sha>`, но `tool/check_coverage.dart`
при недоборе валит прогон: 58% на весь код без генерации, 75% на
`lib/core` + `lib/models` + `lib/services`, 85% на `save_manager.dart` с
`restore_transaction.dart`. Локально то же:

```bash
flutter test --coverage && dart tool/check_coverage.dart
```

**Ещё один шаг `analyze` — `git diff --exit-code linux windows macos`.**
Регистраторы плагинов генерирует `pub get`, а лежат они в репозитории: обновив
зависимости, их коммитят вместе с правкой. Расхождение молчит — файл
компилируется, просто не регистрирует плагин, — и однажды так от релиза к
релизу не работало перетаскивание в окно на Linux и Windows.

`site/` и `design/prototype-v2|v3` в сборку приложения не входят: их
выкладывает на GitHub Pages отдельный `.github/workflows/pages.yml`, и только
при изменении этих путей.

Описание релиза не пишется отдельно: задача выпуска достаёт раздел версии из
`CHANGELOG.md` через `tool/changelog_notes.dart` и кладёт его в релиз. Релиз
заводится черновиком, публикуется после выкладки архивов и остаётся черновиком,
если раздела для версии нет. На теге раздел проверяется до сборок, а
`changelog_notes_test.dart` сторожит верхний раздел файла — тот, который
выпустят следующим.

Версия Flutter зафиксирована в `env.FLUTTER_VERSION` (3.47.2, Dart 3.13.2).
Сборки трёх платформ идут только на теге `v*` либо ручным `workflow_dispatch`;
на теге же архивы прикладываются к релизу и берут артефакты по маске
`evaporate-*` — иначе к релизу уехал бы и отчёт о покрытии. Каждая сборка
выпускает установщик и архив: установщик человеку, архив приложению для
обновления по нажатию (`windows/installer.iss`, `tool/package_macos.sh`,
`tool/package_linux.sh`, `tool/package_run.sh`).

**Выпуск версии — раздел в `CHANGELOG.md` со ссылкой на тег внизу файла, и
сам тег.** Больше её нигде нет: номер задаёт тег, а CI передаёт его в сборку
(`--build-name` и `--dart-define=EVAPORATE_VERSION`). В `pubspec.yaml` и в
`AppVersion.dev` стоят нули — это «сборка не из релиза». Порядок выпуска и две
грабли, на которые уже наступали (тег ставят на зелёный `main`; `push.followTags`
утаскивает локальные теги), записаны в `CONTRIBUTING.md` — здесь их не дублируем.

## Обновление по нажатию

`update_check` / `update_download` / `update_install` / `update_installer` в
`lib/services/system/`. Файл в релизе ищется **по хвосту имени**
(`Release.updateSuffix`), и системы тут расходятся: macOS и Linux берут архив
(`-macos.zip`, `-linux.tar.gz`) и заменяют папку, **Windows берёт установщик**
(`-windows-setup.exe`) и запускает его отсоединённым процессом — так между
приложением и заменой не остаётся ненадёжного PowerShell-помощника. Скачанное
проверяется по порядку: размер, потом sha256 из `SHA256SUMS`, и только потом
распаковка. Канал защищён TLS, но оборванная загрузка выглядит целым файлом, а
разворачивают её поверх установки.

Замену на macOS и Linux делает POSIX-помощник, и порядок его шагов —
возможность откатиться: дождаться, пока процесс исчезнет; **отодвинуть**
текущую папку, а не удалить; поставить новую; запустить; и только теперь убрать
отодвинутое. Сорвись замена на середине, прежняя установка ещё лежит рядом.
Помощник работает, когда приложения уже нет, поэтому пишет в свой журнал, а
приложение забирает его при следующем запуске (`collectLog`) — иначе о
неудавшейся замене не узнал бы никто: окно закрылось, не открылось, и ни следа
почему.

**Обновить можно не всякую копию, и молчать об этом нельзя.** На Windows —
только поставленную Inno Setup (проверка по `unins000.exe` рядом): portable-zip
не превращают в установленную копию без спроса. На остальных системах решает
пробная запись в папку приложения: положенное пакетным менеджером (`.deb`
ложится в `/opt`) обновлять должен он же. Отказ приходит сообщением
«обновите тем способом, каким ставили», а не тишиной.

Имена файлов связывают CI и приложение, и разойтись они могут молча, поэтому
`release_artifacts_test.dart` читает сам `ci.yml` и `windows/installer.iss`.

## Язык

Комментарии, документация, `CHANGELOG` и сообщения коммитов — по-русски.
Комментарий объясняет **почему**, а не что; хороший отвечает на «почему не
проще?». Тест называется утверждением о поведении («свежий прогресс не
затирается старым пакетом»), а не именем метода.

Все видимые строки живут в `lib/l10n/app_ru.arb` (исходник) и `app_en.arb`
(обязателен). Тесты-стражи в `test/localization_test.dart` сверяют наборы
ключей, подстановки и обходят `lib/ui` в поисках кириллицы в строковых
литералах — при срабатывании строку выносят в ARB, а не глушат проверку.

Исключение помечено в коде: `label` у моделей, действий и состояний движка и
функции в `lib/core/format.dart` остаются русскими намеренно — они идут в
журналы. Переводимые подписи для показа собраны в `lib/ui/labels.dart`, потому
что у моделей и слоя ввода нет `BuildContext`.

**`SavePathRule.defaultLabel` («Сохранения») не переводится.** По метке правила
сопоставляются между устройствами: переведись она, снимок с русской машины
перестал бы сходиться с правилом на английской. Хранится как есть, переводится
только показ (`ruleLabelText`).

## Состояние: Bloc с событиями

Четыре блока: `SettingsBloc`, `LibraryBloc`, `DownloadsBloc`, `NavigationBloc`.
Каждый — папка `lib/bloc/<feature>/` из событий, состояния и обработчиков,
связанных через `part`. Состояния иммутабельны (`Equatable`); у `SettingsBloc`
файлов два, потому что состояние у него — сам `AppSettings`.

Событийная модель здесь не церемония: **внешние источники подают события
наравне с нажатиями пользователя**. Лаунчер сообщает о завершении процесса
через `GameExited`, движок загрузок — через `EngineTasksChanged`,
`EngineStatusChanged`, `EngineStatsChanged`, каталог путей — через
`SavePathsProgressChanged`.

Асинхронные операции не пробрасывают исключения в виджеты:

- ключи выполняющихся операций лежат в `state.busy` (`state.isBusy(...)`) —
  собственный `bool _busy` в экранах не нужен;
- результат кладётся в одноразовый `Notice`, а единственный `BlocListener` в
  `AppShell` показывает его SnackBar'ом. У `Notice` есть счётчик `seq`: без него
  два одинаковых сообщения подряд считались бы одним состоянием.

Событие ничего не возвращает — отсюда пара мест, выглядящих странно: диалог
добавления игры сам генерирует id и передаёт его в `GameAdded`, а перед стартом
загрузки дожидается появления игры в состоянии, иначе два блока разошлись бы в
порядке обработки.

`flutter_bloc` построен на `provider`, поэтому оба используются по назначению:
блоки раздаёт `BlocProvider`, сервисы без состояния (`GamepadService`,
`NotificationService`) — обычный `Provider`.

Порядок в `main.dart` важен: настройки читаются до создания остальных блоков
(они нужны им в конструкторе), окно ставится до показа, значок в трее ставится
всегда. Язык блоки получают функцией `L Function()` — `BuildContext` у них нет,
а язык может смениться на ходу; по умолчанию `LRu()`.

## Запись на диск

`AppPaths` (`lib/core/app_paths.dart`) держит все каталоги: данные приложения —
в системной папке поддержки, сами игры по умолчанию — в домашней, потому что
они большие и их обычно уносят на отдельный диск. В тестах каталоги задаются
напрямую через `AppPaths.custom`, без системных вызовов.

Пишет всё `JsonStore` — **не `File.writeAsString`**, и на то три причины,
каждая проверена:

- запись атомарна (временный файл и переименование): падение посреди записи
  не должно убивать библиотеку;
- записи выстроены в очередь: два одновременных `write` работали бы с одним
  временным файлом, и второй падал бы на переименовании уже переименованного;
- `private` ставит права **на временном файле, до переименования** — в
  настройках лежит пароль прокси открытым текстом, и он не должен ни мгновения
  лежать читаемым для всех. На Windows это ничего не меняет.

`pretty: false` включают там, где файл читает только приложение (кэш базы
путей — десятки тысяч записей, отступы прибавляют около трети).

## Окно, трей и завершение

Половина записей отложена — библиотека ложится через 400 мс после правки,
положение окна через 500, — а закрытие окна и «Выход» в трее обрывают процесс
мгновенно. Отсюда `AppShutdown` (`lib/services/system/app_shutdown.dart`):
через него проходят **оба** пути выхода, и трею он передаётся как `onQuit` —
своими силами трей умеет только убить окно вместе с процессом, мимо
отложенных записей. Терялось при этом ровно то, что человек только что
сделал: наигранное время сеанса, добавленная игра, подвинутое окно.

Устройство завершения — из того же соображения «пользователь уже нажал
„Закрыть“»: шаги идут по очереди и независимо, сбой одного не отменяет
остальные, у каждого свой предел и у всех вместе общий бюджет (зависший
движок не имеет права оставить окно на экране навсегда). Повторный запуск
гасится `isStarted`: закрытие окна и «Выход» могут прийти одно за другим.

Окно живёт в `WindowState` + `ManagedWindowController`: обёртка нужна затем,
чтобы логику восстановления можно было проверить тестами — настоящее окно в
тестовой среде не создать. Запомненные числа перед применением проходят
`sanitized()`: монитор могли отключить или сменить разрешение, и окно по
старым числам окажется за краем экрана — человек решит, что приложение не
запустилось. `NaN` отсеивается **до** `clamp`, иначе испорченная ширина
превратилась бы в окно на двадцать тысяч точек. Перетаскивание сыплет
событиями десятками в секунду, поэтому запись отложена, но при выходе её
дописывают немедленно (`flush`).

`WindowStartMode` — один выбор из трёх (как оставили / развёрнутым / свёрнутым
в трей), а не набор галочек: «свёрнуто» и «развёрнуто» взаимоисключающи.
Ради третьего варианта значок в трее и ставится **всегда**: приложение,
стартовавшее с системой и не показавшее окна, иначе было бы ничем не открыть.

## Перенос сохранений — суть приложения

`lib/core/save_path_template.dart`: в профиле игры хранится **не абсолютный
путь, а шаблон** — `{APPSUPPORT}/MyGame/Saves`. На другой машине и другой ОС тот
же шаблон разворачивается в правильный локальный путь (`expand`/`collapse`).
`{GAME}` — папка установки игры, самый частый случай; её знает только тот, кто
знает, о какой игре речь.

`.evsave` — обычный zip: `manifest.json` (метаданные и правила) плюс
`data/<ruleId>/...`. При восстановлении правила сопоставляются сначала по id,
затем **по метке** — так снимок с Windows ложится в macOS-путь той же игры.
Версия формата лежит в `manifest['format']`, а читаемые версии перечислены в
`SaveSnapshot.readableFormats` — множество, а не сравнение с текущей: пакеты
переживают версии приложения, и проверка на равенство означала бы, что новая
сборка не читает ничего снятого раньше. Добавляя версию, старую не убирают.
Формат читают чужие сборки на других устройствах: несовместимое изменение рвёт
то, ради чего всё затевалось, и обсуждается до правки. Пакеты приходят извне,
поэтому при распаковке проверяется выход за пределы целевой папки (zip-slip).

Локально снимки лежат **не архивами**, а в хранилище по содержимому
(`SnapshotStore`, `blobs/<две буквы>/<sha256>`): двадцать снимков одной игры
различаются обычно одним файлом, а остальное лежало двадцать раз подряд.
Файл кладётся один раз под именем своего sha256 и сжатым; снимок хранит
только список ссылок. Формат `.evsave` это не затрагивает — пакет собирается
из ссылок по требованию и остаётся самодостаточным zip, потому что его
читают чужие сборки. Снимки, снятые до появления хранилища, лежат своими
архивами (`archivePath` непуст) и продолжают работать: их не переписывают,
они уходят по мере ротации. Уборку неиспользуемого содержимого запускает
`LibraryBloc`: полный список живых ссылок виден только ему.

Массовая загрузка сравнивает время снятия пакета с временем изменения сейвов и
пропускает игры, где это устройство ушло вперёд (допуск две минуты на расхождение
часов). Восстановление всегда сначала снимает резервную копию.

Откуда берутся пути:

- `LudusaviCatalog` — открытая база путей (манифест Ludusavi, MIT). Качается по
  требованию, лежит в кэше приложения, **разбирается в отдельном изоляте**:
  десятки тысяч записей заморозили бы интерфейс на секунды. В репозиторий и
  сборку не входит.
- `SaveActivityWatch` — то, что база не знает (торрент-релизы, всё мимо Steam):
  после выхода из игры смотрит, что изменилось за время её работы. Отбор по
  нескольким признакам сразу, известные кэши и логи отбрасываются по имени.
  Результат — **предложение человеку, а не запись молча**.
- `SavePathFinder` — поиск по названию игры в типичных местах. Тоже догадка.

Перенос всей библиотеки разом живёт в `BulkTransfer` (`lib/services/saves/`), а
не в блоке: на входе список игр, на выходе `BulkResult` с отчётом и строкой
сообщения, поэтому цену переезда можно проверить, не поднимая ни блока, ни
состояния. Готовые снимки отдаются колбэком `onSnapshot` — список должен
пополняться на глазах, выгрузка большой библиотеки идёт минуты.

Ветки реестра Windows база знает; переносить их приложение не умеет, но
сообщает о них — иначе снимок вышел бы неполным без единого слова.

## Загрузки

`DownloadEngine` — узкий контракт, `DtorrentEngine` — единственная реализация
(`dtorrent_task_v2`, чистый Dart, внешнего бинарника нет). Выбран ради SOCKS5:
через него идёт и обмен с пирами (`useForPeers: true`), тогда как aria2 SOCKS не
поддерживает вовсе. Смена прокси перезапускает активные задачи.

`dtorrent_task_v2` берётся **форком** (`Hecatoncheir/dtorrent_task_v2`,
коммит в `pubspec.yaml` закреплён): в 0.5.4 один из зашитых корневых узлов
DHT разрешается в `0.0.0.0`, отправка туда роняет UDP-сокет, а следующая
попытка возвращает порт уже закрытого — DHT выключался целиком. Правка
предложена автору; когда он её примет, зависимость вернётся на pub. Правки
форка идут в его репозиторий, а не сюда: так их можно отдать наверх обычным
PR, а этот репозиторий остаётся про приложение.

**Прокси применяется перехватом создания `HttpClient`**
(`ProxyHttpOverrides`, ставится в `main`), а не настройкой каждого клиента.
Причина чужая: объявление трекеру внутри `dtorrent_task_v2` заводит клиента
само и о `ProxyConfig` не знает — его флаг `useForTrackers` читают только
пути паузы и scrape. Дотянуться до объявления больше неоткуда, а без него
раздача с закрытым у провайдера трекером не едет вовсе: DHT в библиотеке не
поднимается (один из зашитых корневых узлов разрешается в `0.0.0.0` и роняет
сокет), и трекер остаётся единственным источником пиров.

Где о прокси решают отдельно — каталог Steam и база путей сохранений с их
флагом `useForSteam` — клиент берётся через `directHttpClient()`, мимо
перехвата. Своего применения прокси у них больше нет.

Имена времён aria2 из кода убраны, и осталось от них ровно одно, намеренно:
`Game.fromJson` читает старый ключ `downloadGid` наравне с `downloadTaskId` —
библиотеки, записанные до переименования, лежат у людей на дисках. Ветку
прикрывает тест в `library_bloc_test.dart`; удалять её нельзя, пока такие
библиотеки живы.

Формат `.torrent` приложение читает и пишет само — `TorrentFile`
(`lib/services/download/torrent_file.dart`), поверх `b_encode_decode`.
Разбору из `dtorrent_task_v2` доверять здесь нельзя: **infohash он не
считает, а угадывает** — ищет в файле байты `info` и принимает за границы
словаря первые же `d` и `e`, которых полно внутри двоичных хешей кусков. На
настоящей раздаче он промахивается всегда, а с чужим infohash её не узнают
ни трекер, ни пир. Поэтому `DtorrentEngine.torrentFromBytes` пересчитывает
хеш по вырезанным байтам info-словаря и заодно читает имя как UTF-8 (там оно
разбирается байт за символ). Вырезать, а не пересобирать: у файла,
записанного не по канону, перекодирование дало бы другой хеш.

По magnet-ссылке приходит **голый info-словарь** (BEP 9), а не торрент:
вокруг него собирается настоящий файл, туда же переезжают трекеры из ссылки,
и результат ложится в `torrentsDir/<infohash>.torrent`. Оттуда его берут
и перезапуск (не искать метаданные в сети заново), и выгрузка со страницы
игры. Где искать файл раздачи, знает `TorrentExport`: своя копия по id игры,
файл движка по infohash, исходный `.torrent` пользователя — в этом порядке,
от нашего к чужому.

## Поиск установленных игр

`LibraryScanner` отличает игру от **собрания игр** по числу разных подпапок с
исполняемыми файлами: у игры такая одна (даже если это `Binaries/Win64` у
Unreal), у собрания их столько же, сколько игр. Спорный случай — собрание из
одной игры — выдаёт отсутствие файлов рядом: у игры в корне всегда
что-нибудь лежит. Без этого выбор диска целиком давал одну «игру» с именем
контейнера, накрывавшую всю коллекцию.

`SteamInstall` читает `libraryfolders.vdf` и `appmanifest_*.acf` с диска
(`Vdf` — свой разборщик на сорок строк). Оттуда берутся точные `appid` и
названия; по `appid` дальше ищутся пути сохранений, и это единственный
способ не подставить сейвы другой игры с похожим именем. Каталога содержимого
в приложении по-прежнему нет: читаются файлы на диске самого игрока.

`ScanSession` держит ход обхода и его отмену через **поколения**: заход,
который отменили, не дописывает найденное в список следующего. Поиск
стартует до выбора папки — пока человек ищет её в системном окне, известные
места (`GameRoots`) уже осматриваются. Остановка не выбрасывает найденное:
«хватит искать» и «забудь найденное» — разные вещи.

`~/Documents`, `~/Desktop` и `~/Downloads` в `GameRoots` намеренно не входят:
на macOS обход каждой вызывает системный запрос прав, а поиск теперь
начинается сам, без отдельного нажатия.

## Название, обложка и appid

Цепочка, которой не видно по одному файлу: **имя раздачи → название → `appid` →
пути сохранений**. `ReleaseName` (`lib/services/metadata/`) чистит
`The.Witcher.3.Wild.Hunt.GOTY.MULTi15-ElAmigos` от версий, меток релиз-групп и
языков; `SteamCatalog` по очищенному названию находит `appid`, описание и
обложку; по найденному `appid` дальше ищутся пути сохранений — и это
единственный способ не подставить сейвы другой игры с похожим названием.
Держит цепочку `automatic_metadata_test.dart`.

Три решения в `LibraryBloc`, каждое оплачено ошибкой:

- `SteamLookupRequested` и `SavePathsLookupRequested` обрабатываются **по
  одному за раз** (`asyncExpand`), а не параллельно, как Bloc умеет по
  умолчанию: загрузка библиотеки ставит поиск каждой игре сразу, и сорок игр
  давали сорок одновременных соединений со Steam. Steam на залп отвечает
  отказом, а маркер «уже пробовали» к тому времени записан — игры оставались
  без обложек навсегда. Цена — очередь: ручной поиск во время разбора большой
  библиотеки дождётся своей очереди.
- Маркер `steamLookupAttempted` пишется **до сети**, чтобы аварийный выход не
  вызывал повтор; отказывает в поиске только он. Наличия `appid` для отказа
  мало: он мог прийти из манифеста Steam на диске, и обложки у игры ещё нет.
- Найденным названием карточку **не переписывают**: имя в библиотеке
  пользователь мог задать сам. А если `appid` уже известен, спрашивают прямо
  по нему: поиск по названию тут не лишний, а вредный — он способен ответить
  другой игрой.

## Перетаскивание в окно

`DropImport` (`lib/services/launch/drop_import.dart`) решает, чем оказалось
сброшенное: папка — установленная игра, `.torrent` — игра в очереди загрузки,
остальное помечается неподходящим и не проглатывается молча. Решение живёт
отдельно от виджета и проверяется на настоящих файлах.

**Magnet-ссылку сюда не притащить, и это не недоделка.** `desktop_drop` читает
на macOS только file-URL, на Windows — только `CF_HDROP`; ссылка из браузера
приходит как `public.url` / `CFSTR_INETURL` и до приложения не доезжает.
Пакет, который это умеет (`super_drag_and_drop`), тянет Rust в сборку — от
него отказались сознательно. Magnet вставляют в «Добавить игру».

## Ввод: клавиатура и геймпад — один словарь

`NavAction` (`lib/input/nav_action.dart`) — единый набор действий; и клавиши, и
кнопки контроллера сводятся к нему, дальше по приложению идёт только `NavAction`.
`InputScope` в оболочке добавляет недостающие `Shortcuts`/`Actions` и мостит
поток геймпада. `DirectionalFocusIntent` перехвачен намеренно: штатный
обработчик не умеет стартовать с пустого фокуса. Раскладка живёт в настройках,
применяет её `GamepadService`.

## Оформление

Две схемы — **два самостоятельных облика**, а не одна палитра с вывернутой
яркостью. Ночная «Арклайт» — чернильный корпус кинозала, тёплое золото
главного действия и холодный сигнальный голубой на показаниях. Дневная
«Картридж» — светлый корпус измерительного прибора: плоский насыщенный цвет
без переходов, чёрные надписи на оранжевом и клавиша на своём тёмном торце.
Осветлённая копия ночной выглядела бы выцветшей, и обратно — тоже.

`EvaporatePalette.dark` / `.light` в `lib/ui/app_colors.dart`, а `ThemeData` и
расширение `context.colors` — в `lib/ui/theme.dart`; цвета раздаются через
расширение темы (`context.colors.textSecondary`), а не константами — иначе две
схемы не сосуществовали бы. `test/theme_test.dart` меряет контраст каждого цвета
на каждой подложке и требует WCAG (4.5 для подписей, 7 для основного текста);
часть палитры отклонена от исходных значений именно им.

**Фирменный цвет разделён на две роли, и путать их нельзя.** `primary` —
для текста и значков, он проверен по контрасту на всех подложках;
`primaryFill` — для заливок, на нём пишут `onPrimary`. Разделение оплачено
Картриджем: насыщенный оранжевый хорош как блок под надписью и не дотягивает
до нормы как текст на светлом фоне, так что один токен на две роли означал бы
либо тусклые кнопки, либо нечитаемые подписи. Так же разведены `accent` и
`accentFill`. Ещё три токена описывают **материал**, а не семантику:
`glow` (ореол; днём прозрачный — светлый корпус не светится), `depth`
(торец под клавишей; ночью прозрачный) и `shadow`.

Три семейства, все с настоящей кириллицей и все вариативные: **Unbounded**
на заголовки и главные клавиши (широкий геометричный — звучит как надпись на
корпусе), **Golos Text** на тексты и подписи, **JetBrains Mono** на пути,
размеры и показания. `test/fonts_test.dart` проверяет, что они слушаются
`fontWeight`, по ширине набранной строки.

**Широкому шрифту нужен положительный разряд, а не отрицательный.** Заголовки
набраны заглавными с `letterSpacing` около `+1.4`: у Unbounded прижатые
заглавные слипаются, и привычный для узких шрифтов минус портит именно то
место, ради которого шрифт и взят. Подписи разделов заглавные только на вид —
диктору уходит обычное слово (`Semantics` + `ExcludeSemantics`), потому что
часть читалок разбирает капс по буквам, как сокращение.

Геометрия корпуса — четыре радиуса в `EvaporateTheme` (`radiusShell`,
`radiusPanel`, `radiusControl`, `radiusChip`), малые и **одни на обе схемы**:
разные углы в двух темах читались бы как два разных приложения. Числа в
`BorderRadius.circular(…)` по месту не пишут.

**Моторика тоже токены**, а не числа по месту: `EvaporateMotion`
(`lib/ui/motion.dart`) раздаёт четыре ступени — `instant`, `fast`, `base`,
`slow` — плюс шаг `stagger` для всхода полки; кривые (`ease`, `enter`,
`exit`, `settle`) лежат там же константами. Берут их через `context.motion`,
и там же соблюдается системная просьба не двигаться: при
`disableAnimations` возвращается набор из нулей. Проверять просьбу в каждом
виджете значило бы однажды забыть — а забытое место выглядит поломкой именно
у того, кому анимации мешают.

**Цвет в оболочку приносят игры, а не корпус.** `AmbientLight`
(`lib/ui/widgets/ambient_light.dart`) заливает окно тремя большими
переходами в цветах выбранной игры, а панель поверх нарочно неплотная —
заливка в упор погасила бы единственный цвет в окне. Оттенок берётся от
названия (`gameAmbientColors`), а не от пикселей обложки: разбор картинки
означал бы её декодирование на каждую перелистку, а свет всё равно размыт до
пятна. Оттенки при этом **не весь круг, а шесть выверенных якорей**
(`ambientHues`): свободный оттенок от хеша рано или поздно выдаёт
болотно-зелёный, и оболочка выглядит не «своей у каждого», а сломанной.

Масштабов два, и они независимы: `interfaceScale` — весь интерфейс,
`libraryScale` — только плитки библиотеки. `InterfaceScale`
(`lib/ui/widgets/interface_scale.dart`) не крутит один лишь `textScaleFactor`:
раскладка считается по увеличенному логическому размеру, а затем
масштабируется `Navigator` целиком — иначе вместе с текстом не выросли бы
заданные числом значки, области нажатия и диалоги. Кнопки окна остаются
снаружи: они принадлежат системе. Прочитанные из профиля значения зажимаются в
границы (`AppSettings.minInterfaceScale`/`max…`) — испорченный файл не должен
давать окно, в котором ничего не найти.

Украшения библиотеки (искры, волны, фольга на обложке, наклон карточки,
частицы, капли, рамка выбора, окружение, пробег света по крупной обложке) —
**не один выключатель, а флаг на каждое** в `AppSettings`: у них разная цена и разный вкус, а один общий
переключатель означал бы «или всё, или ничего». Старый `libraryEffects`
остался ради уже записанных профилей и читается наравне с новыми; значения по
умолчанию сторожит `effect_settings_test.dart` — они там перечислены
поимённо, и менять их молча не выйдет.

`interfaceAnimationsEnabled` включён по умолчанию: проявления, переезды и
всход полки — не украшение сверху, а то, чем оболочка объясняет, что куда
переехало. Всходит полка через `RiseIn`
(`lib/ui/widgets/rise_in.dart`), и задержка там сделана **интервалом внутри
одной анимации, а не таймером**: таймер, не успевший сработать, валит
виджет-тесты, а незакрытый переживает виджет. Очередь всхода получает только
первый экран — дальше ленивая сетка строит плитки по мере прокрутки, и
задержка означала бы, что домотанное появляется через полсекунды после того,
как человек до него домотал.

Кадры всем этим гонит один `DecorativeMotion`
(`lib/ui/widgets/decorative_motion.dart`) — часы перерисовки, ни разу не
пересобирающие то, что под ними нарисовано. Он сам останавливается на
`MediaQuery.disableAnimations`, свёрнутом окне, выключенном `TickerMode` и
неактивном маршруте: украшение не должно жечь батарею за спиной. Шаг
ограничен сверху, поэтому возвращение свёрнутого окна приходит одним
маленьким шагом, а не прыжком на минуту вперёд.

Видно ли сноп искр вообще — проверяется снимком отрисовки против
`test/goldens/portal_sparks_reference.png`: сравниваются только светящиеся
пиксели (иначе чёрный фон скрыл бы потерю искр), с допуском на сглаживание.
Посмотреть, что вышло на самом деле, можно переменной `PORTAL_PREVIEW` — тест
положит по этому пути свой PNG. Остальное про искры — обычные тесты: не растут
без предела, не уходят в NaN, переживают возвращение свёрнутого окна.

Экранному диктору сетку библиотеки объявить нечем: плитка с обложкой не
содержит текста вовсе. Подпись собирается в `GameCoverTile` и кладётся
**внутрь** плитки под `MergeSemantics` — обёртка с `excludeSemantics` снаружи
уносит вместе с лишними объявлениями и действие нажатия, и плитка перестаёт
нажиматься. Стережёт это `test/semantics_test.dart`.

Правило уведомлений: **SnackBar — для того, что пользователь только что нажал
сам; системное уведомление — для того, что закончилось в фоне** (загрузка
завершена, загрузка сорвалась, автоснимок после выхода из игры не удался). Об
ошибке загрузки уведомляем один раз — на переходе в состояние, а не на каждом
опросе движка.

## Журнал

`AppLog` (`lib/services/system/app_log.dart`) заводится в `main` раньше всего
остального и пишет в `<dataDir>/evaporate.log` с поворотом одного поколения.
Туда уходит каждый `Notice` с `isError`, а также места, которые гасят ошибку
молча: уборка хранилища снимков, ротация, обход папок в поисках следов игры,
выгрузка в папку синхронизации. Наружу журнал не уходит — его показывают в
настройках (`LogCard`), а отправлять решает человек.

Запись — настоящий файловый ввод-вывод, поэтому в `testWidgets` и она, и
чтение идут через `tester.runAsync`; опрос состояния после этого — **снаружи**
`runAsync`, потому что кадры внутри него в цикле не идут.

## Тесты

`test/support/test_app.dart` собирает всё приложение с подменённым потоком
геймпада. Две ловушки записаны там же в комментариях: `TestHarness` **обязан**
создаваться внутри `testWidgets` (Bloc запоминает зону подписки, и блок из
`setUp` доставлял бы события мимо фейкового времени), а временная папка,
наоборот, готовится снаружи — настоящий файловый I/O внутри `testWidgets` не
завершается никогда. По той же причине `saveRoots: () => const []`: обход
настоящих «Документов» не закончится.

Правка, меняющая поведение, идёт с тестом; правка, исправляющая ошибку, — с
тестом, который без неё падает.

## Что обсуждать до правки

Новый экран, замена зависимости, смена формата `.evsave` — сначала issue.
Формат снимков особенно: его читают чужие сборки на других устройствах.
