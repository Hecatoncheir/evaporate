# TODO — второй разбор: что сделано, выводы и план

Разбор сделан 2026-09-21 по состоянию `main` на коммите `a26acfa` — сразу
после тега `v0.37.0`. Первый разбор (2026-09-19, коммит `1b33588`) лежит
ниже архивом: все его 108 пунктов отмечены закрытыми, и вопрос этого
разбора — **так ли это на деле и хорошо ли вышло**.

Как проверялось. В этот раз ворота запускались, а не только читались:
формат, анализатор, `bloc lint`, сверка регистраторов, полный прогон с
покрытием, `tool/check_complexity.dart` и, для сверки с ним, измеритель
когнитивной сложности на настоящем AST. История — `git log` и `gh run
list`. Код читали пять параллельных рецензий по непересекающимся областям
(сохранения и ядро; загрузки, система и запуск; блоки и сборка приложения;
интерфейс и тема; тесты, стражи, CI и документы); поведение библиотеки
загрузок сверено по исходникам закреплённого форка. Главные находки
перепроверены вторым чтением, две — временным тестом вне репозитория.
Ничего, кроме этого файла, не менялось.

Как читать — так же, как первый разбор: **P0** — ошибка или риск для
данных человека, **P1** — редкий случай с тяжёлым исходом или обязательное
правило, **P2** — устройство, **P3** — гигиена; **S** — до пары часов,
**M** — до дня, **L** — несколько дней; 💬 — сначала issue (зависимость,
формат, имена файлов релиза). Новое — отметка уверенности:

- **[▶]** — подтверждено прогоном: временный тест упал ровно так, как
  описано в пункте;
- **[✔]** — перепроверено по коду вторым чтением (про библиотеку загрузок
  — по исходникам форка `dtorrent_task_v2`, коммит `2668afb`);
- **[○]** — найдено рецензией чтением кода, вторым чтением не проверялось.

По правилу проекта правка начинается с теста, который без неё падает, — для
**[○]** он же находку подтвердит или снимет. Не подтвердилась — пункт
вычёркивают со словами «не подтвердилось», а не молча.

---

## 1. Итог

**План выполнен по-настоящему, и код стал заметно лучше — но самое опасное
лежало не в плане.** Первый разбор смотрел, *как написан код*; этот
спрашивал, *что станет с файлами человека, когда что-то пойдёт не так*, — и
нашёл девять ошибок уровня P0, часть — в местах, которые первый назвал
образцовыми (`JsonStore`, раскладка сохранений, хранилище снимков). Сами эти
места сделаны хорошо; беда на их стыках — там, где карантин встречается с
уборкой, а «наша папка» оказывается чужой.

### Ворота — зелёные, и это проверено

| Что | Итог |
|---|---|
| `dart format` | 588 файлов, правок ноль |
| `flutter analyze` | замечаний нет, включая подсказки |
| `bloc lint lib` | 0 на 434 файла |
| регистраторы плагинов | расхождений нет |
| `flutter test`, случайный порядок | 1248 прошли, 16 пропущены по платформе, 69 с на Windows |
| покрытие | 84,0 / 84,7 / 92,7 при порогах 82 / 82 / 90 |
| храповики сложности, длины, вложенности | пусты |
| CI на `a26acfa` | зелёный на трёх системах; релиз 0.37.0 собирался во время разбора |

История: 151 коммит за три дня (53 + 87 + 11). Только тринадцать из них
трогают `lib/` без `test/`, и все они — выносы без смены поведения: правило
«правка с тестом» соблюдалось. Каждый из трёх тегов стоит на зелёном `main`,
а прогон тега повторяет всё на трёх системах. Красный `main` был
(`ca1356d`) — его поймал страж `_reportedNowhere` и починил следующий
коммит: страж сработал ровно как задуман. Оборотная сторона темпа — в
выводах (§2).

### Оценка

| Область | Было | Стало | Коротко |
|---|---|---|---|
| Состояние (Bloc) | 3.5 | 4.5 | События-намерения, зависимость в одну сторону, блоки экранов, движок параметром и 21 тест на него. Остались пути закрытия: снимок, очередь метаданных, второе «Закрыть» (этап C). |
| Тема | 3.5 | 4.5 | Роли текста, токены, схемы расходятся данными. Осталось: шкалы отступов нет, `HardwareSurfaceTheme` стал мешком ручек, роли расползаются через `copyWith` (этап F). |
| Структура виджетов | 2.5 | 4 | Правило держит страж, листья получили границы перестроения. Цена: 47 % параметров — пробросы, 79 % виджетов используются в одном месте, один регресс от выноса (этап F). |
| Когнитивная сложность | 4 | 4.5 | Храповики пусты, AST-измеритель согласен в пределах ±1–3. Но самодельный не видит конструкторы и фабрики, а потолок в 60 строк стал целью (этап E). |
| Сервисы | 4 | 4 | Разбор `SaveManager`, обновления и движка сделан хорошо, правки этапа 1 подтверждены. Оценку держат новые P0: обновление на Linux, трафик мимо SOCKS5, hybrid-торрент, пределы скорости (этап A). |
| Тесты и ворота CI | 4.5 | 4.5 | +300 тестов, зеркало `lib`, закреплённый CI. Но у стражей нет самопроверки, общий процент прячет файлы с нулём, собранное приложение до выпуска не запускается ни разу (этапы E, G). |
| Документация | 5 | 4.5 | По-прежнему редкого качества, но отстала от кода минимум в восьми местах, а на неё опираются все следующие правки (этап G). |
| Устойчивость данных к сбоям | — | 3 | Не оценивалась. К чужому файлу (`shortcuts.vdf`) приложение бережнее, чем к своим `snapshots.json` и `library.json` (этапы A, B). |
| Процесс выпуска | — | 3 | Не оценивался. Три релиза поверх переразложенного опасного кода без единого запуска собранного приложения; запись `CHANGELOG`, которой нет в коде (этап G). |

### Что сделано хорошо и трогать не надо

Перепроверено и подтверждено — рецензии пытались это опровергнуть:

- **правка игры намерением**: ни один обработчик `LibraryBloc` и `SavesBloc`
  больше не пишет значение, захваченное до `await` (`_edit`, двойной
  `_stillSameGame`, `_resolveStoredPaths`); `busyWhile` гасит занятость в
  `finally`; подписки между блоками заводятся одним синхронным проходом;
- **раскладка сохранений**: план целиком до первой записи, замена готовится
  рядом с целью, одна раскладка на оба источника, откат проверяется подменой
  `rename`; `RuleMatcher` один на предпросмотр и раскладку; путь из чужого
  манифеста никогда не становится целью; `readableFormats` — множество;
- **хранилище снимков в пределах одного процесса**: `guard` + эпоха +
  `_deletions`, размер засчитывается после удаления, гонка в тесте
  поставлена подменой обхода, а не таймером;
- **`JsonStore`**: очередь не застревает после сбоя, права на пустом файле,
  `chmod` проверяется, временный файл убирается;
- **распаковка обновления**: ссылки только вниз и без `..`, запись сквозь
  ссылку наружу невозможна, через границу изолята идут три строки;
- **прокси для `HttpClient`**: недоступный отказывает, SOCKS получает имя
  цели без утечки DNS; **ярлыки Steam**: байт в байт, отказ на незнакомом,
  копия перед записью; резервация запуска игры снимается в `finally`;
- `DownloadQueue` и `SlotState` как чистые классы, `TorrentFile._Scanner`,
  `WindowGeometry.sanitized`, `AppLog`, `FakeDownloadEngine` с одноразовым
  отказом, `waitForState`, `expectRatchet` с двусторонней проверкой;
- в интерфейсе: `DecorativeMotion`, дисциплина ресурсов (скрипт не нашёл ни
  одной утечки контроллеров и подписок в 28 `StatefulWidget`) и `mounted`
  после `await`, оболочка целиком на `select`, семантика плитки,
  `EvaporateAlpha` / `Layout` / `Motion`, пара `primary` / `primaryFill`;
- в CI: экшены по SHA, `--enforce-lockfile`, права по умолчанию `read`,
  релиз черновиком, `release_artifacts_test`, `NOTICE.md`, `dependabot`,
  версия только из тега, пороги покрытия на 2–3 пункта ниже достигнутого.

### Где «закрыто» не значит «сделано»

Галочка в архиве ниже стоит, а по коду — иначе:

1. **`DownloadHistoryBloc`** (архив, §3 и этап 4). План требовал блок уровня
   приложения с историями по id задачи; сделано переименование Cubit → Bloc.
   Все три беды из §3 на месте: у одной задачи две истории
   (`task_card.dart:29` и `game_detail.dart:37` живут в `IndexedStack`
   одновременно), история умирает с виджетом, при замершей загрузке выборок
   нет (`download_history_bloc.dart:49–53`), `DateTime.now()` зашит (`:26,55`).
   Обработчик не исполняется **ни одним** тестом (строки 46–76, покрытие
   24 %). При этом `CHANGELOG` выпущенной 0.36.0 утверждает: «График
   скорости у задачи теперь один на приложение» — этого в коде нет и не было
   (`git log -S'TasksSampled'` находит только сам план). Пункты D1 и G2.
2. **`dialogWidth`** (архив, этап 2) отмечен сделанным — токена нет: ноль
   упоминаний в `lib`, ширин диалогов пять (460, 520, 540, 560 ×3). Пункт D3.
3. **`SettingsChanged` → правка-функция** (архив, этап 1): переведены два
   отправителя с системным диалогом, шестнадцать по-прежнему шлют снимок
   настроек. Пункт C8.
4. **«Синхронная работа `archive` на главном изоляте»**: у обновления и
   восстановления — да; импорт пакета по-прежнему льёт записи синхронно
   (`save_manager.dart:617–632`), выгрузка — тоже. Пункт D4.
5. **«Исключения не гаснут без журнала»**: остановка задачи
   (`managed_download.dart:233–239`), пропуск сверки суммы
   (`update_download.dart:205,218`) и `_dropBackups` гасят молча. Пункты
   C19, G3, B2.
6. **Оговорки под галочкой**: `UpdateBloc` не на уровне приложения,
   `description` у ключей ARB нет, группы настроек не заведены, измеритель не
   на `package:analyzer`, клавиша обоймы не сведена, а «на живой macOS не
   проверено» и «без живого торрента не стоит» так и остались словами — при
   трёх релизах. Открытых пунктов файл показывает ноль. Этап D и пункт G1.
7. **«Вынос виджета поведение не меняет и нового теста не требует»** (архив,
   §5). Один раз из двухсот пятидесяти — изменил: ключ плитки уехал внутрь
   `build`, и `findChildIndexCallback` сетки умер. Ни один страж этого не
   заметил, потому что стражи смотрят на форму. Пункт F1.

---

## 2. Выводы

1. **Галочка мерила «сделано по плану», а не «работает у человека».**
   Девять P0 этого разбора найдены не в пунктах плана, а мимо них. Первый
   разбор читал код по слоям и искал, что в нём не так; разбор *по сценарию
   сбоя* — «файл не прочитался», «запущено два экземпляра», «распаковали не
   туда», «закрыли посреди работы» — находит другое. Следующий раз начинать
   с него.
2. **К чужому файлу приложение бережнее, чем к своим.** Для `shortcuts.vdf`
   действует «не понял — не трогай», копия перед записью и отказ при
   запущенном Steam. Для своих `snapshots.json` и `library.json` непонятое
   приравнено к испорченному: карантин, пустое состояние, работа дальше — и
   уборка хранилища стирает содержимое снимков, о которых больше не знает.
   Папка установки на Linux считается «нашей» без проверки. Правило одно на
   всё: **чего не понял — не удаляй и не перезаписывай**.
3. **Обещания о приватности шире кода.** HTTP-часть сделана так, как
   записано в `CLAUDE.md`: «отказывает, а не пропускает». А DHT,
   UDP-трекеры и поиск метаданных по magnet идут напрямую при включённом
   SOCKS5 — при подписи в настройках «для приватности выбирайте SOCKS5».
   Пределы скорости показаны в настройках, а библиотека их не читает вовсе.
   Форк починил DHT — и тем самым сделал утечку настоящей, а `CLAUDE.md` всё
   ещё говорит, что DHT не поднимается.
4. **Стражи держат форму, поведение держат только тесты.** Дробление прошло
   всех стражей и сломало ключ сетки; из 193 виджетов с единственным
   пользователем 162 не названы ни в одном тесте. Страж стоит столько,
   сколько его измеритель: самодельный счётчик сложности не видит 46 тел
   функций (конструкторы со списком инициализации, именованные фабрики,
   `operator ==`), и два невидимых уже сверх порога — при пустом списке.
5. **Потолок стал целью.** Восемь функций ровно в 59–61 строку, 23 `build` в
   полосе 50–60, а на месте методов-виджетов выросли замыкания до 52 строк
   внутри `build` — их правило не запрещает. Храповик лечит рост, но не
   подсказывает, *чем* мерить: следующая ступень — пробросы параметров и
   замыкания, а не строки.
6. **Темп обогнал проверку.** Около 40 % прогонов `main` отменены
   `cancel-in-progress` — пуши шли каждые 5–10 минут при девяти минутах
   тестов на Windows; та же группа отменит и еженедельную сборку. `CHANGELOG`
   писался по плану, а не по диффу, — отсюда запись о правке, которой нет.
   Собранное приложение до выкладки не запускается ни разу: регистрацию
   плагинов, шейдер, трей и `AppShutdown` первым исполняет пользователь.
7. **`CLAUDE.md` — память проекта, и она начала врать.** «Пять блоков» при
   четырнадцати папках, `DtorrentEngine.torrentFromBytes`, которого нет,
   «повторный запуск гасится `isStarted`», который никто не читает, «журнал
   причин», которого нет в выпускной сборке. На этот файл опирается каждая
   следующая правка, в том числе сделанная не человеком, — ошибка в нём
   размножается. Нужен страж (E9).

---

## 3. План

### Этап A. Чужие файлы, данные человека, приватность (P0)

Каждый пункт — малым коммитом с тестом, который без правки падает. A1
заслуживает отдельного выпуска.

- [x] **A1. Обновление по нажатию на Linux удаляет папку, в которой лежит
  приложение, — со всем чужим, что в ней было.** **[✔]** Архив пакуется без
  корневой папки (`ci.yml:286–287`: `tar -C bundle .`), `README.md:102`
  советует «распаковать куда угодно», папкой приложения считается
  `dirname(executable)` (`update_install.dart:38–41`), а «можно ли обновить»
  проверяет только запись (`update_installer.dart:82–89`). Помощник делает
  `mv root backup`, `mv staged root`, запуск, `rm -rf backup`
  (`update_install.dart:171–189`). Сценарий: `cd ~/Downloads && tar xzf
  evaporate-*.tar.gz && ./evaporate` → «Обновить» → содержимое `~/Downloads`
  удалено. Вторая дверь туда же — `./x.run --prefix ~/apps` при
  существующей папке (`tool/package_run.sh:97–108`: `mv`, затем `rm -rf
  "$target.old"`). Лечение:
  1. паковать архив с корневой папкой `evaporate/` и класть в сборку
     файл-маркер установки (`.evaporate-install`);
  2. `canInstall` отказывает, если маркера нет **или** в папке есть записи
     верхнего уровня сверх известных (`evaporate`, `lib`, `data`, маркер);
     отказ приходит тем же сообщением «обновите тем способом, каким ставили»;
  3. помощник сам сверяет маркер перед `mv` и перед каждым `rm -rf` — это
     последний рубеж, и он не должен верить тому, кто его собрал;
  4. `.run` отказывает, если цель существует и маркера в ней нет;
  5. **уже установленные версии чинятся только извне**: замену выполняет
     помощник *старой* сборки, и новая сборка его не исправит. Архиву для
     обновления дать новый хвост имени (`Release.updateSuffix`,
     `update_check.dart:112–117`) 💬 — клиент, не нашедший своего файла,
     клавишу «Установить» просто не показывает (`about_actions.dart:64`), и
     человек ставит новую версию руками, уже с защитой.
  Тесты: папка с чужим файлом → `canInstall == false`; POSIX-тест помощника
  «чужой файл рядом переживает обновление»; `release_artifacts_test` —
  в архиве ровно одна корневая папка. **M**
  *Сделано:* все пять шагов. Архив пакует `tool/package_tarball.sh`, хвост
  — `-linux-x64.tar.gz`; прежний в релиз не кладётся, и это сторожит
  `release_artifacts_test`. Проверка «своей папки» в помощнике общая с
  macOS: бандл узнаётся по `Contents/MacOS`. `.run` переставляет прежнюю
  установку и без маркера, если в папке нет ничего сверх сборки, — иначе
  не обновить тех, кто ставил им раньше. POSIX-тесты помощника идут только
  на macOS и Linux; на Windows сценарии прогнаны руками в Git Bash. C17
  (проба записи в `dirname(root)`, проверка запуска) не делался.
- [x] **A2. Испорченный `snapshots.json` стоит человеку содержимого всех
  снимков, и молча.** **[▶]** Файл не разобрался → карантин переименованием
  → состояние пустое → сразу `persist()` пустого списка
  (`saves_bloc.dart:226–241`); нечитаемые записи пропускаются поштучно без
  журнала (`:247–267`); ключ `version` пишется, но не читается. Следующая
  же уборка получает список живых из состояния (`:307–311`,
  `save_manager.dart:649–652`) и удаляет всё, чего в нём нет
  (`snapshot_store.dart:232–277`). Временный тест: снимок → испортить файл →
  перезапуск → новый снимок и его удаление → **блобов было 1, стало 0**,
  карантинная копия списка цела и бесполезна; `Notice` не пришёл. Лечение:
  карантин или хоть одна пропущенная запись **запрещают уборку до конца
  сеанса** и приходят сообщением, как у библиотеки
  (`_storageRecoveryNotice`); нечитаемая запись сохраняется как есть и
  пишется обратно нетронутой; мусор не удаляется сразу, а уходит в
  `blobs/.trash` со сроком — второй рубеж на случай, которого мы не
  предусмотрели. Тест — тот же сценарий: старые блобы на месте. **M**
  *Сделано:* `_reportDamage` в `SavesBloc` запрещает уборку до конца сеанса
  и приходит сообщением; нечитаемые записи лежат в `_unread` и пишутся
  обратно как были. Корзина — не `blobs/.trash`, а `blobs-trash` рядом с
  хранилищем (обход хранилища туда не заходит), срок две недели;
  вынесенное возвращается само — `contains`, `extractTo`, повторный `put`.
  Строка журнала о вынесенном — «вынесла в корзину», об удалённом насовсем
  — прежняя «освободила».
- [x] **A3. Частично испорченная библиотека пропадает на втором запуске.**
  **[▶]** Одна нечитаемая запись → `quarantine()` **переименовывает**
  `library.json` (`json_store.dart:112–118`), уцелевшие игры уходят в
  состояние (`library_bloc.dart:274–290`), но не на диск: `persist` никто не
  зовёт, а `close()` пишет, только если висит таймер (`:434–441`). У
  сложившейся библиотеки правок на старте нет (`_queueMetadata` молчит).
  Временный тест: после первого запуска `library.json` не существует, на
  втором — игр ноль и сообщения нет. Существующий тест
  (`library_bloc_test.dart:182–209`) останавливается на первой загрузке.
  Лечение: после карантина — `persist()` сразу; тест дописать вторым
  запуском. **S**
  *Сделано:* уцелевшие игры пишутся сразу после карантина, до того как
  библиотека объявлена загруженной; тест дописан вторым запуском.
- [x] **A4. Второй экземпляр приложения ничем не остановлен.** **[✔]** На
  Linux стоит явный `G_APPLICATION_NON_UNIQUE`
  (`linux/runner/my_application.cc:140`), в `windows/runner/main.cpp` нет
  мьютекса. Сценарий будничный: автозапуск «свёрнутым в трей», окна не
  видно, человек щёлкает ярлык. Два процесса — это два движка над одними
  файлами, «последний записавший прав» для `library.json` и настроек, а
  главное — уборка процесса A удаляет содержимое снимков процесса B: `guard`
  и эпохи живут в памяти одного процесса. Лечение: замок на файле в
  `dataDir` первым шагом `main` (`RandomAccessFile.lock` — система снимает
  его сама при гибели процесса); второй экземпляр просит первый показать
  окно и выходит; на Linux — снять `NON_UNIQUE` и показывать окно по
  `activate`. Тест службы замка: второй держатель получает отказ, после
  закрытия первого — замок. **M**
  *Сделано:* `SingleInstance` — замок на `evaporate.lock` первым шагом
  `main`, просьба показать окно через петлевой сокет (порт в
  `evaporate.port`: заблокированный файл на Windows другому процессу не
  прочесть). `NON_UNIQUE` на Linux оставлен: единственность держит замок,
  одинаково на трёх системах, а окно второго экземпляра не мелькает —
  раннеры показывают его после первого кадра. Тест — настоящим вторым
  процессом, иначе на POSIX он прошёл бы и без замка.
- [x] **A5. Правило, развёрнутое в корень, принимается — и восстановление с
  очисткой заменяет «Документы» целиком.** **[✔]** `resolve` требует только
  абсолютности (`save_profile.dart:102–107`): проходят `{DOCUMENTS}`,
  `{HOME}`, `C:\`. Выбрал в диалоге правила «Документы» → раскладка с
  очисткой отодвигает папку, ставит на её место файлы снимка и удаляет
  отодвинутое (`restore_transaction.dart:311–336,367–376`); без очистки —
  копирует все «Документы» в заготовку; снимок — в хранилище.
  `SavePathGlobs.expand` идёт мимо `resolve` вовсе **[○]**
  (`save_path_globs.dart:25–33`). Лечение: отказывать правилу, развёрнутому
  в корень плейсхолдера, домашнюю папку, корень диска или их предка; диалог
  говорит об этом словами. Тест на каждый корень. **S**
  *Сделано:* `SavePathTemplate.isTooBroad` — корни плейсхолдеров, папка
  игры целиком (`{GAME}`), корень диска и предки любого из них; отказ в
  `SavePathRule.resolve`, то есть и для правил, заведённых раньше. Диалог
  правила гасит клавишу и объясняет (`pathTooBroad`). `SavePathGlobs`
  результат и так разворачивается через `resolve`, но широкое теперь
  отсеивается уже там: `{HOME}/*` совпадал с «Документами».
- [ ] **A6. При включённом SOCKS5 часть трафика раздач идёт напрямую, с
  настоящим адресом.** **[✔]** Настройки обещают: «для приватности
  выбирайте SOCKS5», «SOCKS5 применяется и к обмену с пирами»
  (`app_ru.arb:1050,1057`). По форку: `MetadataDownloader` слова `proxy` не
  содержит — пиров заводит сам и сам поднимает DHT
  (`metadata_downloader.dart:95,333–335,414`), а приложение зовёт его без
  вариантов (`managed_download.dart:93–126`); `TorrentTask.start()` всегда
  делает `_dht.bootstrap()` и `announce` прямым UDP (`task.dart:450,
  1367–1372`). `udp://`-трекеры идут через `RawDatagramSocket`, а
  `ws(s)://` — через статический клиент `WebSocket`, запомнивший маршрут
  первого вызова **[○]**. Форк починил DHT — утечка стала настоящей. В духе
  уже принятого решения («отказывает, а не пропускает»):
  1. в приложении, **S**: при `proxy.isUsable` выбрасывать из `announces`
     схемы `udp`, `ws`, `wss` и не искать метаданные magnet по сети —
     отказывать словами («через прокси magnet не найти, добавьте
     `.torrent`»); чистая функция «сетевой план при прокси» под тестом;
  2. в форке 💬, **L**: прокси для `MetadataDownloader`, выключатели DHT,
     LSD, UPnP и holepunch; до тех пор — честная подпись в настройках о том,
     что именно идёт через прокси, а что нет.
  *Сделана часть 1 и честная подпись; пункт открыт до правки форка:* при
  `proxy.isUsable` задача получает модель без `udp`/`ws`/`wss`
  (`announcesFor`; исходная модель с полным списком остаётся для
  выгрузки), magnet при SOCKS5 в сеть не идёт и падает с
  `magnetNeedsTorrentBehindProxy` (`canFetchMetadata`; при HTTP-прокси
  пиры и так идут напрямую, и это в подписи к нему). Подпись к SOCKS5
  говорит про DHT. **Открыто:** выключатели DHT/LSD/UPnP и прокси для
  `MetadataDownloader` в форке 💬 **L**.
- [x] **A7. Проверку путей раздачи обходит v2/hybrid-торрент.** **[✔]**
  `_rejectUnsafePaths` смотрит только `model.files`
  (`torrent_source.dart:84–94`), а раскладку для v2 и hybrid библиотека
  строит по дереву (`download_file_manager.dart:178–188`), склеивая ключи
  как есть (`file_tree.dart:118–140`, ни одной проверки на `..`). Раздача с
  безобидным `files` и деревом `{"..": {"..": {"x.bat": …}}}` пишет вне
  папки игр — а приходит она от незнакомых людей. Комментарий «мимо этой
  проверки в движок не попадает ничего» неверен. Лечение: отклонять не-v1 —
  собственный комментарий на `:50–52` и так говорит, что с ними не
  работали, — либо обходить и `FileTreeHelper.extractFiles` с тем же
  `unsafePath` (путь и `symlinkPath`). Тест: bencode-hybrid с `..` в дереве
  → `UnsafeTorrentException`. **S**
  *Сделано:* проверка идёт и по дереву (`FileTreeHelper.extractFiles`,
  путь и `symlinkPath`), v2 не отклоняется. Попутно выяснилось, что
  hybrid движок признаёт только при `piece layers` в корне файла: без них
  раздача считается v2, дерево само разворачивается в `files` и ловилось
  и раньше — тест собирает настоящий hybrid.
- [ ] **A8. Пределы скорости не действуют вовсе, а их установка снимает
  паузу.** **[✔]** Библиотека кладёт предел в `_scheduledMax*Rate` и нигде
  его не читает (по всему форку — только геттеры, `task.dart:942–949`).
  Зато `addScheduleWindow` → `_evaluateNow()` → `resumeTask()`
  (`scheduler.dart:58–62,117`). `applyLimits` проходит по всем задачам с
  живым `task`, включая поставленные на паузу
  (`dtorrent_engine.dart:246–255`): запустил игру при заданном «пределе во
  время игры» — приостановленные загрузки качают на полную, а интерфейс
  показывает «Пауза». То же после остановки по рейтингу (`:276–287`).
  Лечение, **S**: не ставить окно задачам, у которых слот не `running`, и
  после `_limitTask` возвращать паузу; тесту нужен шов — фабрика
  `TorrentTask` в конструкторе движка: сейчас взаимодействие с живой задачей
  не проверяет ни один тест. Настоящий предел — только правкой форка 💬,
  **L**; до неё убрать настройку или подписать её честно.
  *Сделано иначе, чем предлагалось; пункт открыт до правки форка:* раз
  окно не ограничивает ничего и только снимает паузу, движок его не
  ставит вовсе — `applyLimits` пределы только запоминает (рейтинг
  раздачи считается нами и действует), `speedLimitWindow` удалён вместе
  с тестами (лежит в истории). Шов для `TorrentTask` поэтому не
  понадобился; от возврата вызова сторожит текстовый тест в
  `dtorrent_engine_test`. Настройки: предупреждение
  `speedLimitsInactive` над полями — выбрано «подписать», а не прятать.
  **Открыто:** настоящий предел в форке 💬 **L**.
- [x] **A9. «Документы» и Saved Games на Windows ищутся не там, где их
  держит система.** **[✔]** `placeholders` собирает их как
  `%USERPROFILE%\Documents` и `\Saved Games`
  (`save_path_template.dart:51–61`), не спрашивая систему. При
  «Документах», унесённых в OneDrive (обычное дело на Windows 11), правило
  базы путей `{DOCUMENTS}/My Games/X` разворачивается в папку, которой нет:
  автоснимок молча отвечает «сейвов ещё нет», а восстановление с другого
  устройства кладёт файлы туда, куда игра не смотрит, и рапортует успех. На
  Linux то же с `~/Документы`. Это главная функция приложения, и молчит она
  у заметной доли людей. Лечение: узнавать папки у системы один раз на
  старте (`AppPaths.init` уже ходит в `path_provider`; для Saved Games —
  `WindowsKnownFolder`) и подавать их в `SavePathTemplate` параметром;
  переменные окружения остаются запасным путём. Ядро при этом плагин не
  импортирует. Тест: корни подменяются, `{DOCUMENTS}` берётся из них. **M**
  *Сделано:* `SystemFolders` (ядро) — «Документы» через `path_provider`
  (known folder на Windows, `xdg-user-dirs` на Linux), Saved Games — из
  `User Shell Folders` через `reg`, с раскрытием `%VAR%` и проверкой, что
  папка существует (вывод `reg` в кодовой странице консоли). Подаются в
  `SavePathTemplate.useSystemFolders` из `AppPaths.init`; кэш корней
  сбрасывается там же. `WindowsKnownFolder` не понадобился — это была бы
  новая зависимость. Живой OneDrive-машины в прогоне нет: проверено
  подменой корней.

### Этап B. Перенос сохранений: редкий сбой, тяжёлый исход (P1)

- [x] **B1. Резервная копия перед восстановлением теряется именно тогда,
  когда нужна, а откат не переживает собственный сбой.** **[✔]** Копия
  снимается до `commit`, но в библиотеку попадает только из отчёта об успехе
  (`save_manager.dart:413–425`); сорвался `commit` — снимок `preRestore`
  нигде не заведён, и его содержимое унесёт следующая уборка, хотя
  комментарий на `:327–329` называет его «единственным путём назад».
  `_rollback` — без единого `try` (`restore_transaction.dart:340–349`): на
  Windows свежезаписанное держит антивирус (те же errno 32/145), исключение
  вылетает из `catch`, остальные цели не откатываются, а исходная причина
  теряется. Лечение: отдавать копию вызывающему колбэком **до** `commit`
  (как `onSnapshot` у переноса); каждый шаг отката в своём `try`; в
  сообщении называть путь, где лежат прежние сейвы. Существующий тест «сбой
  замены второй цели» стоит с `backupCurrent: false`
  (`save_manager_test.dart:546–553`) — с `true` копия обязана дойти. **M**
  *Сделано:* `restoreSnapshot(onBackup:)` отдаёт копию до `commit`, и
  блок кладёт её в состояние и на диск сразу (`_keepBackup`);
  `RestoreReport.backup` убран — второго пути быть не должно. `_rollback`
  — `try` на каждую цель с записью в журнал; не вернувшиеся на место копии
  называются в сообщении (`saveRollbackStranded`), а исходная причина
  сохраняется. Тесты — в `save_manager_test`: копия с `backupCurrent:
  true` при сорванной замене и откат, которому мешает занятый файл.
- [x] **B2. Возврат застрявших сейвов ленив, сносит живую заготовку,
  выбирает копию по неверному признаку и молчит.** **[✔]**
  `recoverInterrupted` зовётся только перед снимком и восстановлением
  (`save_manager.dart:163,336`), а при выключенном `autoSnapshotOnLaunch`
  (он выключен по умолчанию) — не зовётся перед запуском вовсе: игра не
  видит сейвов, заводит новые, цель «цела», а прогресс навсегда в
  `.evaporate-old-*`. `_recoverTarget` удаляет **любую** заготовку
  `evaporate-new` (`restore_transaction.dart:94–100`), включая ту, что
  прямо сейчас наполняет параллельная раскладка: `guard` — счётчик, а не
  замок, и автоснимок занятость не проверяет. «Последнюю отодвинутую» ищут
  по `modified` (`:108–111`), который переименование не меняет. О
  находке — строка в журнале на каждом снимке. Лечение: звать возврат в
  `beforeLaunch` без условий и на старте по всем играм; очередь операций по
  игре в `SaveManager` (или множество живых токенов, которые возврат
  пропускает); время — в имя (`evaporate-old-<время>-<uuid>`); остаток рядом
  с живой целью — через `Notice`; `_dropBackups` — с журналом. Тесты:
  остаток на диске + `snapshotBeforeLaunch` с выключенным снимком → цель на
  месте; `RestoreSource` на `Completer` + возврат посреди → оба файла в
  цели. **M**
  *Сделано:* живые пути раскладки (`_live` в `RestoreTransaction`) вместо
  очереди по игре — очередь ждала бы сама себя: снимок перед
  восстановлением идёт через тот же менеджер. Время — в имени
  (`backupPathFor`, микросекунды с нулями слева); у копий прежних сборок
  его нет, и они считаются старше. `recoverInterrupted` зовётся в
  `snapshotBeforeLaunch` без условий и один раз после загрузки библиотеки
  (`_checkInterrupted` — мимо обработчика, чтобы не держать закрытие);
  копии рядом с целой целью приходят `Notice` (`noticeRestoreLeftovers`).
  `_dropBackups` пишет в журнал. Тесты — `restore_transaction_test`
  (возврат посреди раскладки на `Completer`, выбор по имени, остаток) и
  `saves_storage_test` (запуск без автоснимка, сообщение на старте).
- [x] **B3. Автоснимок после выхода из игры теряется, если лончер закрыть
  следом.** **[✔]** Снимок уходит в состояние, затем идёт выгрузка в папку
  синхронизации (секунды; `autoExportToSync` включён по умолчанию), затем
  ротация, и только потом `persist()` (`saves_snapshots.dart:66–80`).
  `SavesBloc.close()` обещает дописать отложенное, но `_persistTimer` не
  присваивается нигде — `pending` всегда `false` (`saves_bloc.dart:149,
  406–419`). «Вышел из игры и закрыл лончер» — самый обычный порядок
  действий. Рядом: в `_onSnapshotDeleted` и после `_prune` уборка идёт
  раньше записи списка (`saves_snapshots.dart:140–163,259–278`) — обрыв
  между ними оставляет в списке снимок без содержимого. Лечение: `persist()` сразу после `emit` со
  снимком и безусловно в `close()`; везде порядок «список → удаление →
  уборка». Тест: `SaveManager` с зависшей выгрузкой, `close()`, повторная
  загрузка — снимок на месте. **S**
  *Сделано:* при включённой выгрузке список пишется до неё; без выгрузки
  — следом, как прежде (лишний `await` между `emit` сдвинул бы сообщение
  об удаче, см. `library_bloc_test`). `close()` дописывает список, если он
  менялся с последней записи (`_unsaved` через `onChange`; писать всегда
  нельзя — в виджет-тестах ввод-вывод при закрытии не завершается, и
  прогон вис); `_persistTimer` удалён. В `_prune` и
  `_onSnapshotDeleted` порядок «список → удаление → уборка». Тест —
  `snapshot_rotation_test`: выгрузка висит, блок закрыт, повторная
  загрузка находит снимок.
- [x] **B4. Импорт чужого пакета не сверяет ни CRC, ни размер — проверка
  осталась только на пути старых архивов.** **[✔]** «Записать запись пакета
  в файл» существует в двух копиях: `ArchiveEntrySource.writeTo` сверяет
  (`evsave_package.dart:159–177`), `_importEntry` — нет
  (`save_manager.dart:617–632`), а чужие пакеты теперь ходят вторым путём:
  импорт → хранилище → раскладка, где сверяется одна длина. Папка
  синхронизации — ровно то место, где файлы бывают недоехавшими. Тесты
  zip-slip, CRC и позднего выхода из папки (`save_manager_test.dart:382–435,
  437–500,582–632`) ходят через `archivePath`, то есть мимо. Лечение:
  переливать через `writeTo(tmp)` — уйдёт и дубль; заодно в изолят (D4).
  Тест: пакет из теста CRC через `importPackage` → `SaveException`. **S**
  *Сделано:* `_importEntry` переливает через `ArchiveEntrySource.writeTo`
  — дубль ушёл. Тест `save_manager_test` «импорт пакета с неверной CRC
  отказывает». Перенос в изолят — отдельно, в D4.
- [ ] **B5. `createdAt` в манифесте — местное время без смещения.** **[✔]**
  💬 `toIso8601String()` от `DateTime.now()` (`save_snapshot.dart:85,129`,
  `save_manager.dart:204`), читается как местное время **другого**
  устройства: снимок из UTC+3 в 10:00 на устройстве в UTC+0 считается
  снятым в 10:00Z, и защита «здесь новее» (`bulk_transfer.dart:221–225`,
  допуск две минуты) ошибается на три часа — в одну сторону затирает
  прогресс, в другую даёт ложные конфликты. Нечитаемая дата подменяется на
  «сейчас» (`save_manager.dart:535–537`): такой пакет всегда самый свежий.
  Лечение: писать `toUtc()` — `DateTime.parse` читает `Z`, старые сборки
  тоже, формат совместим, но это всё же формат: сначала issue. Нечитаемую
  дату считать эпохой. Тест: `DateTime.parse(toManifest()['createdAt']).isUtc`. **S**
- [x] **B6. Блоб пишется без `flush` перед переименованием, а `_isKept`
  верит одному имени.** **[○]** Индекс при этом сбрасывается на диск
  (`json_store.dart:145–149`), содержимое — нет (`snapshot_store.dart:
  149–156,171–188`). После обрыва питания под верным хешем лежит пустой или
  обрезанный файл, и каждый следующий снимок неизменившегося сейва отвечает
  «уже лежит». Выясняется в день восстановления. Временные имена там же
  строятся по часам (`:164–166`) — у двух одновременных `guard` на Windows
  они могут совпасть; брать `uuid`, как в `RestoreTransaction`. Лечение:
  `flush` перед `rename`; нулевую длину считать отсутствием; блоб, сорвавший
  раскладку, удалять, чтобы следующий `put` его переписал. Тест: обрезать
  лежащий блоб → `put` того же → восстановление проходит. **S**
  *Подтвердилось; сделано:* `_syncToDisk` (`RandomAccessFile.flush`)
  перед переименованием; временные имена — `uuid`; пустой файл под хешем
  — отсутствие (`_isWhole` в `_isKept`, `contains`, `extractTo`), и новая
  запись ложится поверх него; `StoredBlobSource.writeTo` на
  неразвернувшемся содержимом зовёт `discard` и отказывает словами, а не
  сырым `FormatException`. Тесты — `snapshot_store_test`, группа
  «испорченное содержимое».
- [x] **B7. Выгрузка пакета пишет прямо в целевой файл.** **[○]**
  `encoder.create` обрезает файл до проверки блобов, при сбое идёт
  `target.delete()` (`save_manager.dart:259–300`); автовыгрузка пишет под
  одно имя — пропал один блоб, и из Dropbox исчез вчерашний рабочий пакет, а
  клиент синхронизации успел унести половину файла. `.part` кладутся в чужую
  папку (`:280`). Лечение: временное имя рядом и переименование при успехе.
  Тест: лежит старый пакет, `addToArchive` бросает — старый уцелел. **S**
  *Подтвердилось; сделано:* `_materialize` пишет в скрытое
  `.<имя>.<uuid>.part` рядом и переименовывает при успехе; распакованные
  блобы — в `<dataDir>/export-staging`, а не рядом с пакетом. Тесты:
  «сорванная выгрузка не трогает прежний пакет» (и чужого в папке не
  оставляет), «удачная выгрузка заменяет прежний пакет» — заодно
  проверено, что замена существующего файла переименованием проходит на
  Windows.
- [x] **B8. Метки и вложенные правила: дыры остались на применении.** **[○]**
  `_onSaveRulesAdded` отсеивает только по шаблону
  (`library_edits.dart:198–217`), а диалог правила держит профиль на момент
  открытия: пока он открыт, поиск по базе заводит «Сохранения», человек
  сохраняет предзаполненные «Сохранения» — две одинаковые метки, перенос
  отказывает обеим. Два правила пакета с одной меткой молча сливаются в одну
  цель (`save_manager.dart:441–459`). Вложенность ловится только при
  восстановлении (`restore_transaction.dart:202–213`): есть `…/X/Saves`,
  база добавляет `…/X` — снимки с дублями, а любое восстановление падает.
  Лечение: метки через `_freeLabel` при применении; два входящих в одно
  местное — оба в `unresolved`; пересечение развёрнутых путей проверять при
  добавлении правила и при снимке, с отдельным сообщением. **S**
  *Подтвердилось; сделано:* `SaveProfile.withRules` — единственное место
  применения правил (и библиотека, и `SavesBloc._addRules`): повтор
  шаблона и пересечение по развёрнутым путям пропускаются, занятая метка
  разводится `_freeLabel` с учётом платформы. `RuleMatcher.assign` — два
  правила пакета на одно здешнее не получают пары оба, и предпросмотр идёт
  тем же путём. Пересечение: в диалоге правила (`overlaps`,
  `pathOverlapsRule`) и при снимке (`saveRulesOverlap`).
- [x] **B9. Файл данных от более новой сборки считается испорченным.**
  **[✔]** `version != 1` → `FormatException` → карантин → пустая библиотека
  (`library_bloc.dart:263–269`); у снимков `version` не читается вовсе.
  Пока версия всегда 1, беды нет, но первый же откат на прошлую сборку после
  смены схемы — а откат у обновления по нажатию предусмотрен — даст «всё
  пропало». Лечение: «новее, чем умею» — отдельный исход: ничего не
  перезаписывать, уборку запретить, сказать словами. **S**
  *Сделано:* версия новее своей — отдельный исход у обоих файлов:
  `_frozen` в `LibraryBloc` и `SavesBloc` запрещает запись (и уборку у
  снимков), вместо карантина — `noticeLibraryNewer` /
  `noticeSnapshotsNewer`. Нечитаемая версия (не число, меньше единицы)
  по-прежнему считается порчей. Тесты — «…от сборки новее…» в
  `library_bloc_test` и `saves_storage_test`: файл после правок цел
  байт в байт, содержимое снимков на месте.
- [ ] **B10. Снимок не хранит время изменения файлов.** **[○]** 💬 После
  любого восстановления всё датировано «сейчас» (`snapshot_blob.dart:7–27`,
  `snapshot_store.dart:214–222`; `setLastModified` в `lib` не встречается):
  `lastLocalChange` после раскладки равен «сейчас», и все пакеты старше
  получают ложное «здесь новее» — человек привыкает жать «перезаписать»; у
  игр, берущих «Продолжить» по mtime, сбивается порядок слотов. Лечение:
  mtime — в блоб и в запись zip (поле стандартное, манифест не меняется),
  ставить при раскладке. **M**
- [x] **B11. Провал автоснимка по вводу-выводу не доходит системным
  уведомлением.** **[✔]** В ветке `on Object` нет `_notifySnapshotFailed`
  (`saves_snapshots.dart:81–91`), а `createSnapshot` отдаёт сырой
  `FileSystemException` — файл ещё держит игра. Тихий автоснимок
  проваливается тихо, вопреки правилу уведомлений из `CLAUDE.md`. **S**
  *Сделано:* обе ветки провала идут через `_snapshotFailed` —
  уведомление автоснимку, сообщение ручному. Тест —
  `notifications_test`, снимок падает `FileSystemException`.
- [x] **B12. Отчёт массовой загрузки молчит о нечитаемых пакетах.** **[○]**
  Нечитаемое уходит только в журнал (`save_manager.dart:663–669`): сборка
  новее пишет формат `/2` — отчёт здесь говорит «применено: 0» без единой
  ошибки. Возвращать пропущенное из `scanSyncFolder` и класть в отчёт как
  `failed`. Рядом: `isCompatible` (`:549`) не смотрит на сопоставление по
  метке и красит значком «нет путей» пакет, который ляжет. **S**
  *Подтвердилось; сделано:* `scanSyncFolder(onSkipped:)`, и `importAll`
  кладёт пропущенное провалом с причиной. Совместимость — по игре с тем же
  названием через `RuleMatcher.assign` (`SaveManager.fits`, считается в
  обработчике сканирования); без такой игры остаётся прежнее суждение по
  платформе правил.

### Этап C. Завершение, блоки, загрузки (P1)

- [x] **C1. Второе «Закрыть» во время завершения убивает процесс сразу.**
  **[✔]** `run()` на повторном вызове возвращается немедленно
  (`app_shutdown.dart:49–51`), и второй `quit()` тут же делает `_exit(0)`
  или `destroy()` (`managed_window.dart:126–133`) — посреди первого прохода.
  Окно при этом до восьми секунд видно и нажимается; `isStarted` в `lib` не
  читает никто. Лечение: `run() => _running ??= _run()`, окно прятать в
  начале `quit()`. Существующий тест проверяет только последовательные
  вызовы (`app_shutdown_test.dart:74–83`); нужен шаг на `Completer` и два
  `quit()` подряд. **S**
  *Сделано:* `run() => _running ??= _run()`, `quit()` прячет окно первым
  делом. Тест — шаг на `Completer` и два `quit()` подряд.
- [x] **C2. То, что падает мимо блоков, в журнал не попадает.** **[✔]** Ни
  `FlutterError.onError`, ни `PlatformDispatcher.instance.onError`, ни
  `runZonedGuarded` в `lib` нет; `AppShutdown` создаётся без `onError`
  (`main.dart:68–69`); `AppLog.flush` стоит вторым шагом, а не последним —
  комментарий обещает обратный порядок; `Timer(…, persist)` выбрасывает
  `Future`, и отказ записи `library.json` пропадает без следа
  (`library_bloc.dart:244–247`). А журнал — «единственный способ узнать, что
  произошло у человека». Лечение: оба перехватчика в `_startLog`, `onError`
  в журнал, `flush` — последним, `persist` с записью сбоя. **S**
  *Сделано:* `AppLog.captureUnhandled` (оба перехватчика; функцией в
  службе, чтобы проверить тестом) из `_startLog`; у `AppShutdown` —
  `onError` в журнал (`_handleClose`); `AppLog.flush` — последним шагом;
  отложенная запись библиотеки — `_persistLogged`. Тесты —
  `app_log_test` и `library_bloc_test` (хранилище, отказывающее в
  записи: без правки ошибка уходила необработанной и роняла тест).
- [x] **C3. «Журнала причин» в выпускной сборке нет.** **[✔]** `verbose =
  kDebugMode` (`logging_observer.dart:19`): главный довод `CLAUDE.md` за
  Bloc у человека не работает. `onTransition` к тому же молчит про события,
  не меняющие состояние, — паузу, отмену, перестановку. Лечение: писать
  `onEvent` всегда, кроме короткого списка частых (`EngineStatsChanged`,
  `SpeedSampled`); имена событий, не содержимое — пароль прокси в журнал не
  попадает и сейчас. **S**
  *Сделано:* `onEvent` пишет имя события всегда; частые помечены
  интерфейсом `FrequentEvent` у себя, а не списком в наблюдателе
  (`EngineTasksChanged`, `EngineStatsChanged`, `SpeedSampled`,
  `SavePathsProgressChanged`, `GameSelected`, `LibraryQueryChanged`,
  `ScanSessionChanged`). Переходы — по-прежнему только в отладке.
- [x] **C4. Закрытие посреди очереди метаданных метит «уже пробовали» игры,
  за которыми в Steam не ходили.** **[✔]** Маркер пишется до сети, а выход
  по `_closing` его не откатывает (`library_metadata.dart:47–63`, зеркально
  `:421–449`); проверки `_closing` на входе нет, поэтому `close()` сливает
  буфер `asyncExpand` по событию, и каждое успевает поставить маркер и
  записать файл. Первый запуск, сорок игр, закрыл окно — часть осталась без
  обложек до ручной клавиши; шаг `library.close` всякий раз упирается в
  трёхсекундный предел. Лечение: `_closing` — первой проверкой; при выходе
  по нему после сети маркер возвращать. Тест: сборщик на `Completer`, три
  игры, `close()` — у второй и третьей маркер `false`. Там же осиротевший
  комментарий: описание `_applyMetadata` приклеилось к `_foundDetails`
  (`:79–89`). **S**
  *Сделано:* `_closing` — первой проверкой у обоих поисков (Steam и пути
  сохранений); выход по нему после сети возвращает маркер и пишет сразу
  (`_unmarkAttempt`). Комментарий `_applyMetadata` вернулся на место.
  Тест — сборщик на `Completer`, три игры, `close()`: у всех трёх маркер
  `false`.
- [x] **C5. Удаление игры не снимает её задачу в движке.** **[✔]** На
  `gameRemovals` подписан только `SavesBloc`; убрал из библиотеки качающуюся
  или раздающуюся игру — задача живёт дальше, переживает перезапуск, а
  клавиш у карточки нет: они рисуются только при игре
  (`task_header.dart:41–46`). Снять можно, лишь заведя ту же раздачу заново.
  Лечение: публиковать в `gameRemovals` связь с загрузкой, `DownloadsBloc`
  подписывается и зовёт `engine.remove`; удаление файлов — под `try` с
  `Notice`. Тест на `FakeDownloadEngine`: после `GameRemoved` в поручениях
  есть `remove`. **S–M**
  *Сделано:* `gameRemovals` несёт игру, а не id (загрузкам нужен её
  идентификатор задачи, а в состоянии её уже нет); `DownloadsBloc`
  подписан и зовёт `engine.remove`, отказ — в журнал. Удаление файлов в
  библиотеке — под `try` с `Notice`. Тест — `downloads_bloc_test`.
- [x] **C6. Игра, запущенная в момент закрытия лончера, теряет время сеанса
  и автоснимок.** **[○]** `dispose()` гасит колбэк выхода
  (`game_launcher.dart:156–164`), `elapsedFor` никто не записывает
  (`library_bloc.dart:433–448`). Записывать наигранное при закрытии. **S**
  *Сделано частично — время:* `close()` засчитывает `elapsedFor` идущих
  игр (правило «меньше минуты не в счёт» — как при выходе) и пишет файл
  сразу. Автоснимок при закрытии не снимается намеренно: игра ещё пишет
  сейвы, и снимок застал бы их на середине; выход её после закрытия
  лончера по-прежнему никто не видит.
- [x] **C7. `_onTasksChanged` держит снимок списка игр через `await
  _finalize`.** **[○]** Пока финализируется игра A (обход папки — секунды),
  игра B в соседнем заходе сорвалась: цикл идёт со старой `game` — второе
  уведомление о той же ошибке, повторный `_finalize`, `_relinkByInfoHash` по
  устаревшему списку (`downloads_bloc.dart:385–395,454,490`). У `_finalize`
  есть `finally`, но нет `catch`: при `FileSystemException` он повторяется
  каждую секунду. `library.persist()` сразу после `library.add(…)`
  (`:526–533,560–569`) пишет состояние **до** события. Лечение: финализация
  — отдельным событием по id, игра читается заново; ошибка →
  `GameDownloadRejected`. **M**
  *Подтвердилось; сделано:* финализация — событием
  `DownloadFinalizeRequested` по id, игра и задача читаются заново,
  `_finalizing` ставится при заявке; опрос движка стал синхронным и
  снимка через `await` больше не держит. Сбой проверки →
  `GameDownloadRejected` с причиной. `library.persist()` после `add`
  убран. Тест — «сорвавшаяся проверка приходит отказом, а не повтором».
- [x] **C8. `SettingsChanged` снимком остался у шестнадцати отправителей.**
  **[✔]** Включил автозапуск и тут же сменил режим окна — второй снимок
  несёт старый `launchAtStartup`, и обработчик честно выключает автозапуск в
  системе обратно (`window_startup_card.dart:28–37`). Окно узкое — время
  очереди записи плюс `reg.exe`, — но корень тот же, что у `GameUpdated`.
  Перевести всех на `SettingsPatched` и убрать событие-снимок. **S**
  *Сделано:* все отправители (и в приложении, и в тестах) шлют
  `SettingsPatched`; `SettingsChanged` удалён — второго способа записать
  настройки нет, объяснение — в `settings_event.dart`.
- [x] **C9. Два `SteamShortcutRequested` разом переписывают чужой
  `shortcuts.vdf` друг поверх друга.** **[○]** Обработчик параллельный и
  без проверки занятости (`library_metadata.dart:189–221`), временный файл
  один на всех. Очередь через `asyncExpand` и `isBusy` на входе. **S**
  *Подтвердилось; сделано:* `asyncExpand` у `SteamShortcutRequested` и
  `isBusy` на входе. Тест — запись ярлыков, считающая одновременные
  вызовы: без правки их было два.
- [x] **C10. Готовая раздающаяся задача держит слот очереди.** **[○]** По
  умолчанию слотов три, рейтинг — «раздавать вечно»: три скачанные игры, и
  четвёртая ждёт всегда, а строка состояния показывает «0 / 3» — интерфейс
  готовые задачи слотом не считает (`downloads_state.dart:19–29`), движок
  считает (`download_queue.dart:54–66`). Лечение: состояние `seeding`, не
  занимающее слот. Тест очереди: `isActive` ложно для готовой. **M**
  *Подтвердилось; сделано:* `SlotState.seeding`; переход решает чистая
  `DtorrentEngine.slotAfter` (running + complete → seeding), опрос движка
  после перехода сразу отдаёт слот следующей. Рейтинг раздачи смотрит на
  `isSeeding`. Тест — на `slotAfter`: живую задачу подставить нечем.
- [x] **C11. Magnet без пиров по-прежнему вечно «получает метаданные».**
  **[✔]** `MetaDataDownloadFailed` библиотека шлёт только после трёх
  несовпадений хеша; таймаута нет ни у неё, ни в `fetchMetadata()`
  (`managed_download.dart:93–126`) — тест сам описывает этот случай
  функцией `never` (`dtorrent_engine_test.dart:204–206`). В `CHANGELOG`
  0.35.0 это заявлено исправленным. Лечение: таймаут вокруг `fetch` →
  `_fail(metadataNotFound)`. Тест: `fakeAsync` + `never` → `error`, слот
  ушёл следующей. **S**
  *Сделано:* `metadataTimeout` у движка (по умолчанию 10 минут) вокруг
  получения метаданных; не дождались — `null`, и `_launch` срывает задачу
  с `metadataNotFound`. Тест — `never` и предел 50 мс, слот уходит
  следующей: без `fakeAsync`, предел — параметр.
- [x] **C12. Проверка целостности считает пропавшими файлы, которых на
  диске и не должно быть.** **[○]** Заполнители BEP 47 и
  `_____padding_file_N_____` входят в `model.files`, библиотека держит их
  виртуальными: раздача скачана целиком → `missing` → игра в ошибке
  навсегда (`dtorrent_engine.dart:458–463`). Пропускать `isPaddingFile` и
  записи с `symlinkPath`. **S**
  *Сделано:* `DtorrentEngine.expectedOnDisk` пропускает `isPaddingFile` и
  записи со `symlinkPath`; тест на заполнитель по имени, по атрибуту и
  ссылку.
- [x] **C13. «Отменить с удалением файлов» оставляет `<infohash>.bt.state`,
  и повторная загрузка верит старой карте кусков.** **[○]** Скачано 40 % →
  отмена с удалением → та же раздача снова: эти куски не запрашиваются,
  частично затронутые файлы создаются нулями, и `IntegrityCheck` их
  пропускает. Удалять `*.bt.state` и `*.bt.paths.json` в `remove()`. **S**
  *Сделано иначе:* не в `remove()` движка, а в
  `DownloadsBloc.deleteDownloaded` — там, где удаляют сами файлы: отмена
  без удаления файлов пусть по-прежнему позволяет докачать. Удаляются
  `<infohash>.bt.state` и `.bt.paths.json` в папке задачи; тест —
  `download_delete_files_test`.
- [ ] **C14. Каждый старт задачи перечитывает всю папку игр по разу на
  каждый ещё не созданный файл.** **[○]** 💬 `detectMovedFiles()` форка
  зовёт `dir.list(recursive: true)` от общей папки на каждый отсутствующий
  файл: свежая раздача в N файлов — N обходов библиотеки до первого байта.
  На macOS и Linux хуже: файл привязывается к одноимённому файлу того же
  размера у **другой** игры (`steam_appid.txt`, redist). Правка форка:
  поиск перемещённых — опцией, выключенной для свежего состояния. **M**
- [x] **C15. Перетаскивание вниз по очереди промахивается на позицию.**
  **[✔]** `[a,b,c]`, «a перед c» → `orderIndexBefore` отдаёт 2
  (`downloads_state.dart:52–56`) → `moveTo(a, 2)` → `[b,c,a]`
  (`download_queue.dart:33–42`). Все тесты двигают только вверх, а подделка
  движка записывает номер, не применяя его. Вычитать единицу, когда задача
  стоит выше соседа; подделка применяет перестановку. **S**
  *Сделано:* `orderIndexBefore(beforeId, moving:)` вычитает единицу, когда
  задача стоит выше соседа. Тест прогоняет посчитанный номер через
  настоящую `DownloadQueue.moveTo` — вниз и вверх; подделку движка
  трогать не понадобилось.
- [x] **C16. У `HttpFetch` и `UpdateTransport` нет предела на чтение
  ответа.** **[○]** Сеть пропала посреди ответа — `await for` не кончается
  (`http_fetch.dart:47–75`, `steam_catalog.dart:404–410`,
  `update_transport.dart:83–87`): очередь `asyncExpand` стоит до
  перезапуска, у обновления `installing` остаётся `true`. Общий таймаут у
  `HttpFetch`, простойный — у загрузки, `limitBytes` для JSON. Тест:
  петлевой сервер, замолчавший после заголовков. **S**
  *Сделано:* простойный предел `HttpFetch.idleTimeout` (30 с) на ожидание
  заголовков и каждого куска тела — им пользуются каталог Steam, база путей
  и проверка обновлений; тот же предел — у загрузки обновления
  (`UpdateTransport`, и на переадресациях). Общий предел не ставился
  нарочно: манифест путей по медленному каналу честно качается минутами.
  `limitBytes` у JSON уже был. Тест — петлевой сервер, замолчавший после
  заголовков.
- [x] **C17. Откат POSIX-помощника не покрывает незапустившуюся версию, а
  проба «можно ли обновить» смотрит не ту папку.** **[○]** `"$launch" &` — и
  сразу `rm -rf "$backup"`; бит запуска после распаковки ставится без
  проверки результата (`update_unpack.dart:111–113`). Проба пишет внутрь
  `root`, а `mv` требует прав на родителя: `/opt/evaporate` принадлежит
  пользователю → приложение закрылось, шесть секунд неудач, стартовала
  прежняя версия, в интерфейсе тишина. После запуска ждать и проверять
  `kill -0 $!`; пробу писать в `dirname(root)`. Делать вместе с A1. **S**
  *Сделано:* помощник ждёт `@SETTLE@` (5 с) и проверяет `kill -0` у
  запущенной; упавшую отодвигает в `.evaporate-broken`, возвращает
  прежнюю, запускает её и убирает сломанную (через `ours`). Проба записи —
  в `dirname(root)`, и папка обязана существовать. `chmod +x` при
  распаковке проверяется — не встал бит, обновление отказывает до замены.
  POSIX-тест «упавшая новая версия меняется обратно» идёт на macOS и
  Linux; на Windows сценарии прогнаны руками в Git Bash.
- [ ] **C18. `ProxyHttpOverrides.apply`: гонка на время разрешения имени,
  отказ DNS на старте не лечится, обновление идёт мимо прокси.** **[○]**
  Смена A→B: запрос в окне уходит на `A.ip:B.port` с учётными данными B; два
  `apply` завершаются в порядке ответов DNS (`proxy_http_overrides.dart:
  87–95`). Автозапуск до появления сети → `blocked` до правки настроек.
  Проверка и загрузка обновления берут прямой клиент
  (`update_check.dart:231`, `update_transport.dart:27,128`) — вопреки
  комментарию «обновление спрашивает свои флаги». Лечение: оба поля разом
  под счётчиком поколения; повтор разрешения при `blocked`; про обновление
  — решить и записать. **M**
- [ ] **C19. Остановка задач идёт по одной и гасит ошибки молча.** **[○]**
  Один `try` на три шага: сбой `metadata.stop()` отменяет `task.stop()`;
  `catch` без журнала (`managed_download.dart:233–239`). `task.stop()` ждёт
  объявление трекеру без таймаута — при пределе шага в три секунды до второй
  задачи на выходе дело может не дойти. `Future.wait` с `try` на шаг; на
  выходе и при смене прокси — `stop(true)`. **S**
- [ ] **C20. Поиск игр.** **[○]** macOS: `.app` в корне обхода разбирается
  как обычная папка и предлагается игрой «Contents» с поставленной галочкой
  (`library_scanner.dart:101–123,147–176`) — папку `*.app` сразу считать
  игрой. Linux: `~/.steam/steam` — обычно ссылка, `resolveSymbolicLinks`
  нет, и каждая игра Steam находится дважды (`steam_install.dart:63–68`).
  После `await inspect` нет проверки поколения — отменённый заход дописывает
  игру в список следующего (`scan_session.dart:162–174`). **S**

### Этап D. Недоделанное под галочками

- [ ] **D1. История скоростей — блоком уровня приложения**, как и было
  записано в §3 архива: события из потока движка, истории по id задачи,
  подменяемые часы, выборка и при замершей загрузке. Тесты на обработчик,
  которых сейчас нет ни одного. Запись в `CHANGELOG` 0.36.0 станет правдой
  задним числом — до тех пор см. G2. **M**
- [ ] **D2. `UpdateBloc` — в `AppServices.bootstrap`.** Найденное на старте
  уходит только в уведомление, карточка пуста до нажатия, а при выключенных
  уведомлениях стартовый запрос теряется целиком (`main.dart:105–112,
  322–342`). Цена мала: блок и так живёт всю жизнь приложения. Уведомление —
  по переходу в `found`. **S**
- [ ] **D3. `dialogWidth` — завести на деле**: пять ширин (460, 520, 540,
  560 ×3) → токен в `EvaporateLayout`, число — в стража. **S**
- [ ] **D4. Архив — с главного изолята везде**: импорт пакета
  (`save_manager.dart:617–632`, вместе с B4) и выгрузка; хеш и gzip в `put`
  тоже считаются на главном. **M**
- [ ] **D5. Решить и записать** то, что висит оговоркой: группы настроек
  (`Appearance`, `StartupSettings`, `SaveAutomation`) — снять с плана или
  делать; клавиша обоймы без моно 💬; `description` у ключей ARB — **M**,
  делать целиком или снять. Оговорка под галочкой — худший из статусов: её
  не видно.

### Этап E. Ворота: чтобы стражи мерили то, что обещают (P1)

- [ ] **E1. Измеритель сложности не видит 46 из ~1760 тел функций.** **[✔]**
  Суть перепроверена сверкой с AST-измерителем, счёт — по рецензии. После
  `)` он ждёт `{` или `=>` — список инициализации не проходит
  (`tool/check_complexity.dart:163–165`); имя после точки отбрасывается
  (`:222`) — мимо идут `factory Foo.fromJson`; тип-запись или тип-функция в
  возврате — «не объявление» (`:186–217`); непарная скобка — молчаливый
  пропуск (`:176`). Итого невидимы 22 фабрики (все `fromJson`), 21
  конструктор (каждого блока), 3 `operator ==`. Два невидимых уже сверх
  порога при пустом списке: конструктор `LibraryBloc` — 90 строк,
  `DownloadsBloc` — 62. AST-измеритель (`cognitive_complexity`) даёт
  `SaveActivityWatch._scan` 16 при нашем 15, а `_materialize` и
  `HttpFetch.bytes` — ниже 12 при наших 15. Не считаются `??`, `?.`, `when`,
  ветки `switch`-выражений, второй `&&` в элементах коллекций. Лечение:
  (а) сразу, **S**: «перепись» — число тел на уровне членов класса обязано
  совпасть с числом найденных функций, иначе страж красный; эталонный набор
  из ~25 фрагментов с известными числами; (б) 💬, **M**: перейти на AST —
  `package:analyzer` dev-зависимостью или `cognitive_complexity` с
  `--fail-threshold`, закреплённый, как `bloc_tools`.
- [ ] **E2. У шести стражей из семи нет теста «страж ловит нарушение».**
  **[○]** Сломанная регулярка — вечная зелень. `widget_structure` не видит
  `Widget get _header`, типы возврата вне трёх перечисленных (`Column
  _section()`), наследников проектных виджетов, `lib/main.dart` и
  `lib/input`; `theme_structure` — `Opacity(opacity: 0.35)` (уже три места:
  `draggable_game.dart:24,28`, `downloading_header.dart:37`),
  `Duration(seconds:`, `BorderRadius.all(Radius.circular(8))`,
  `Theme.of(context).brightness`; `layering` — транзитивное:
  `gamepad_binding.dart:2` сам тянет `package:gamepads`, то есть плагин в
  моделях уже есть, а «сервисы не знают блоков» заявлено, но не проверяется;
  `arb_usage` ищет `\.key\b` по всему тексту — убери `L.of(context).source`,
  и ключ останется «живым» за счёт `download.source` (таких около десятка).
  Лечение: табличные тесты детекторов на `SourceFile('lib/ui/x.dart',
  '<фрагмент>')`. **M**
- [ ] **E3. Общий процент прячет файлы с нулём.** **[✔]** По отчёту этого
  прогона: девятнадцать файлов с 0 % и сорок пять ниже 50 %. С нулём — весь
  интерфейс папки синхронизации (`sync_package_row`, `pick_game_dialog`,
  `bulk_report_view`, `bulk_outcome_group`, `sync_folder_contents`), окно
  захвата кнопки, окно выбора исполняемого файла; `update_transport.dart` —
  43 %: докачка с диапазоном, «ради которой загрузка по плохому каналу
  вообще заканчивается», не покрыта — тест подменяет её подделкой;
  `saves_bulk.dart` — 54 %, `managed_download.dart` — 62 %.
  `_reportedNowhere` ловит «ни разу не загружен», но не «загружен и не
  выполнен». `app_services.dart` — порядок подъёма и гашения блоков — внесён
  в исключения в ответ на красный прогон, а не покрыт. Лечение: храповик «на
  файл» — список файлов ниже 50 % с их числом, новым туда нельзя, **S**;
  слить lcov трёх систем, **M**: сейчас тест, существующий только для
  Windows или macOS, можно удалить, и порог не заметит.
- [ ] **E4. Запуск, код возврата и остановка игры на Windows не проверяются
  ни одним тестом.** **[✔]** Семь тестов из двенадцати пропущены со словами
  «скрипт sh не запустится на Windows» (`game_launcher_test.dart`) — на
  главной игровой системе `_track`, `terminate` и колбэк выхода не
  исполняются ни на одном раннере. «Игрой» взять `cmd.exe` с `['/c', 'exit
  3']` и `ping -n` для «идёт» и «остановить». **S**
- [ ] **E5. Следующая ступень храповика — то, чем дробление обходят.** **[○]**
  Замыканий-строителей в `build` — 68, из них 21 длиннее 25 строк
  (`library_grid.dart:51` — 52, `navigation_rack.dart:37` — 51,
  `library_body.dart:78` — 49); диалоги, собранные в функциях
  (`cancel_dialog.dart:71`, `confirm.dart:19`); три `const`-виджета верхнего
  уровня (`featured_art.dart:75,90,101`). Страж на длину замыкания в `build`
  и на число параметров виджета (больше семи), оба храповиком. **S**
- [ ] **E6. Тесты: паузы под проверками и чувствительность к лишнему
  `await`.** **[○]** Пятьдесят `Future.delayed`. «Ничего не произошло за
  50 мс» проходит вхолостую на медленной машине
  (`downloads_bloc_test.dart:257,381,485`, `update_bloc_test.dart:103,124,
  134`, `save_hints_test.dart:172`, `download_pipeline_test.dart:289`);
  положительные после паузы будут плавать (`downloads_bloc_test.dart:444`,
  `library_bloc_test.dart:504,787`). Под тесты подогнан код приложения
  (`saves_snapshots.dart:94–100`, синхронный `existsSync` в
  `saves_hints.dart:47–58`) — это слабость обвязки, а не свойство блоков.
  Лечение: барьером `await bloc.close()` или тестовый `BlocObserver` на
  `onEvent`/`onDone` с ожиданием «обработчиков в работе нет». Неперенесённые
  копии `waitFor` — `catalog_progress_test.dart:122`,
  `bulk_report_test.dart:88,95`. **M**
- [ ] **E7. `avoid_public_bloc_methods` в `bloc_lint` 0.4.2 не видит
  публичные члены после первого `@override`.** **[○]** Без пометок проходят
  `persist` у библиотеки и сохранений, `snapshotBeforeLaunch`, `applyLimits`
  и `torrents`, который снаружи не нужен вовсе. Проверить на минимальном
  примере, пометить эти места руками, сообщить автору пакета. В
  `analysis_options.yaml` написано «восемь мест», их девять. **S**
- [ ] **E8. Страж «тема не теряет поле» — по исходнику.** **[○]** Тест
  `values` не ловит поле, забытое и в `values`, и в `lerp`, — то есть ровно
  тот случай, ради которого написан. `copyWith` у `HardwareSurfaceTheme`
  принимает 2 поля из 22, у `EffectsPalette` — 1 из 5, и `values` там нет.
  Сверять число `final`-полей с `values.length` и числом аргументов `lerp`.
  **S**
- [ ] **E9. Страж «имена из документов существуют».** Каждый путь и каждое
  CamelCase-имя в обратных кавычках из `CLAUDE.md`, `README.md`,
  `CONTRIBUTING.md` находится в `git ls-files` или в исходниках; список
  исключений — «бывший `SaveFreshnessCubit`»; «N блоков» сверяется с
  `lib/bloc/*`. Рецензия сделала это скриптом: из 323 имён разошлись
  единицы, так что ворота выйдут дешёвыми. **S**

### Этап F. Интерфейс после дробления

Сначала ошибки (P1), потом устройство (P2).

- [ ] **F1. Дробление потеряло ключ плитки.** **[✔]** `itemBuilder`
  возвращает `LibraryGridTile` без `key` (`library_grid.dart:76–98`), а
  `ValueKey(game.id)` уехал на `MouseRegion` внутри `build`
  (`library_grid_tile.dart:62`); до выноса `_tile` возвращал `MouseRegion` с
  ключом напрямую (`git show 102cac8^`). `findChildIndexCallback` ключа
  больше не получает — комментарий над ним неправда: набрал букву в поиске
  или сменил полку — плитки собираются заново, `RiseIn` всходит повторно,
  приподнятость теряется. Лечение: `key: ValueKey(game.id)` на
  `LibraryGridTile`. Тест: состояние `RiseIn` той же игры после отбора — тот
  же объект. **S**
- [ ] **F2. Замер для догоняющей прокрутки считает другими числами, чем сама
  сетка.** **[✔]** `_measure` берёт 64, 36 и +40 (`library_grid.dart:25–26,
  108–115`), делегат — поля 28+28, просвет 28, шаг ряда +32 (`:66–74`). По
  подсчёту рецензии число столбцов расходится для 45 ширин из 170: при 1280
  — шесть настоящих против пяти посчитанных. Это главный путь возврата из
  игры: `GamePage` заменяет `LibraryBody`, `PageStorageKey` в проекте нет,
  сетка возвращается в ноль и надеется на `scrollTo` — 120 игр, открыл
  сотую, «назад» — прыжок мимо, фокус не вернулся. Лечение: одна функция над
  теми же постоянными; лучше — `PageStorageKey` сетке. Тест: `_measure` и
  `getLayout` делегата сходятся. **S–M**
- [ ] **F3. «/» съедается во всех текстовых полях под оболочкой.** **[✔]**
  `SingleActivator(slash) → search` (`input_scope.dart:221`), защиты нет —
  проверка на `EditableText` стоит только у «назад» (`:153–155`). В пароле
  прокси «/» не вводится и уводит в поиск; «Fate/stay» в поиске не набрать.
  Единственный тест проверяет, что «/» уводит фокус. Лечение: не
  перехватывать при фокусе в `EditableText` (или
  `DoNothingAndStopPropagationTextIntent` над полями). Тест: «/» в поле
  остаётся в поле. **S**
- [ ] **F4. Текст на заливке `danger` не проходит WCAG.** **[○]**
  `dangerFilled` в «Картридже» — 2,67:1 (`button_styles.dart:24–25`),
  SnackBar ошибки — около 3,0:1 в обеих схемах (`snack.dart:14–15`);
  комментарий там говорит о белом тексте, а текст — `textPrimary`. У
  `danger` нет пары заливка/надпись, как у `primary`, и тест контраста
  меряет его только текстом на подложках. Завести `dangerFill` и `onDanger`
  с тестом пары. **S**
- [ ] **F5. В интерфейсе показана русская «журнальная» подпись.** **[○]**
  `info_section.dart:53–54` выводит `source.label`, задокументированный как
  «для журналов» (`game_source.dart:15–20`): в английской локали —
  «Source: Локальная папка: …». Страж локализации мимо — литерал лежит в
  `lib/models`. Лечение: `gameSourceLabel(l, kind)` в `ui/labels.dart` и
  страж «`lib/ui` не читает `.label` моделей». **S**
- [ ] **F6. Обложки декодируются в полный размер.** **[○]** У `CoverArt`,
  `DetailCover`, `CoverBackdrop`, `FeaturedArt` нет `cacheWidth`: 600×900 —
  около 2 МиБ, 48 плиток — около 99 МиБ при пределе кэша картинок в 100, а
  выбранная человеком 2000×3000 — 23 МиБ одна. В сетке — `cacheWidth ≈ 215 ·
  scale · dpr`. `cacheWidth: 1024` у кадров (`shot_frame.dart:52`) ничего не
  даёт: вверх `ResizeImage` не масштабирует. **S**
- [ ] **F7. Геймпад и клавиатура: четыре места, куда не дойти.** **[○]** У
  `LauncherActionButton` нет состояния фокуса — только 12 % заливки поверх
  золота (`launcher_action_button.dart:30–68`); ползунок мёртвой зоны с
  геймпада не меняется (влево-вправо всегда уходят в `focusInDirection`);
  окно захвата кнопки с геймпада не отменить — любая кнопка становится
  назначением; очередь загрузок переставляется только мышью. Рядом: окно
  поиска игр не закрывается Esc, но закрывается B; `NavigationKey` не
  передаёт `selected` в `Semantics`. **M**
- [ ] **F8. Состояние экрана — в дерево, а не пробросами.** **[○]** В
  виджетах с четырьмя и более параметрами 162 из 348 только передаются
  дальше: `LibraryBody` — 12 из 17, `AddGameFields` — 11 из 14,
  `ProxyAddressFields` — 10 из 10, `LibraryToolbar` — 8 из 8; `onShelf` и
  `onQuery` идут четыре шага. Причина одна: `LibraryViewBloc` создаётся без
  провайдера (`library_page.dart:42,71`), а родители `AddGameBloc` и
  `ProxyFormBloc` всё равно превращают события в колбэки. Лечение:
  провайдер, листья читают блок сами; однопользовательские с пробросом от
  70 % — слить обратно (`SourceFields`, `LibraryShelfBar`,
  `LibraryHeadingBar`, `ProxyAddressFields`). Критерий выноса: повторное
  использование, свой `State` или ресурсы, свой `select` при трёх и менее
  параметрах, граница перерисовки, отдельный тест. **Дробить дальше — не
  лечение.** Туда же: наведение делает `setState` всей страницы и следом
  вторую перестройку от `GameSelected`; в `build` есть побочные действия —
  `forgetGone` освобождает `FocusNode` (`library_page.dart:131`), `_measure`
  пишет в контроллер; контроллер сетки раздать `InheritedNotifier`. **M**
- [ ] **F9. `HardwareSurfaceTheme` стал мешком ручек одиннадцати
  виджетов.** **[○]** Из 22 полей шесть — у `GlassSurface`, двенадцать — у
  десяти других, по одному; четыре мёртвых (`backdrop*Opacity`,
  `grilleHole`); цветов — два. Архив (§2) советовал тему на компонент рядом
  с виджетом — к этому и вернуться: `double`-ручки к тому, кто их читает. В
  `evaporate_theme.dart:153` остался `isDark ? 0.82 : 0.9`. **M**
- [ ] **F10. Роли текста расползаются, а шкалы отступов нет вовсе.** **[○]**
  47 употреблений ролей из 133 правят роль через `copyWith` (`caption` — 8
  из 10, `figure` — 4 из 5); `label` (9 / w700 / +1,4) и `eyebrow` (9,5 /
  w800 / +1,6) — то самое расхождение, которым §2 архива обосновал роли, —
  стали двумя ролями; `small`, `chip`, `pathSmall` — все 11,5. Мимо стража:
  `fontWeight:` — 15 мест, `letterSpacing:` — 7 мест с семью значениями,
  `TextStyle(` — 11. Отступы: 163 `SizedBox` с 17 значениями, 76
  `EdgeInsets` с 22 числами; размеры значков — 93 места, 13 значений;
  `Curves.*` по месту — 10 при четырёх токенах; 900 мс в
  `animated_progress.dart:64` идут мимо `context.motion`, то есть мимо
  системной просьбы не двигаться. Лечение: шкала `EvaporateSpacing` и
  размеры значков токенами, стражи храповиком; роли свести, правку ролей
  через `copyWith` — только цветом. **M–L**, по папкам, как этап 2 архива.
- [ ] **F11. Вечно живые страницы подписаны на состояние целиком.** **[○]**
  `downloads_page.dart:31–32` и `saves_page.dart:31–34` перестраиваются
  каждую секунду, пока скрыты в `IndexedStack`; `SavesPage` сортирует все
  снимки в `build`, а их список — неленивая `Column`; `engine_readout.dart:
  23` и `app_footer.dart:23` — `watch` там, где хватило бы `select`. **S**
- [ ] **F12. Числа, даты и размеры идут мимо локали.** **[○]** `formatBytes`
  всегда «1.5 GB» (9 вызовов из интерфейса), `formatDateTime` всегда
  dd.MM.yyyy (14), при обещании «1,2 МБ» в `ui/labels.dart:124`. Журнальные
  функции оставить русскими, показ — через локаль. **M**
- [ ] **F13. Мёртвый код после дробления.** **[○]** `HardwareGrille`
  (единственное упоминание — `findsNothing` в тесте, который упасть не
  может), `gameRootLabel` — единственный читатель пяти ключей ARB, которые
  из-за него проходят стража, `AppColors.ambientWarm/Cool/Deep`,
  `frostDark/Light`, `EvaporateTheme.radiusShell` (из «четырёх радиусов
  корпуса» живут три), `EvaporateMotion.settle`, `GamepadBinding.unassign`,
  обязательный и нечитаемый `RuleTile.gameDir`, `putBytes` в хранилище;
  `assets/branding/frost_world_background.png` — 2,4 МБ без ссылок, едет в
  каждую сборку и каждое обновление. Два цикла импортов: `shots_backdrop` ↔
  `shots_slideshow` ↔ `shot_frame` и `shell` ↔ `shell_layout`. **S**

### Этап G. Выпуск и документы

- [ ] **G1. Собранное приложение до выкладки не запускается ни разу.** **[○]**
  `integration_test` нет, `main.dart` и `app_services.dart` исключены из
  покрытия поимённо. Лечение: ключ `--smoke` (первый кадр, запись
  `JsonStore` во временный дом, `exit 0`) и его запуск в каждой сборке — на
  Linux под `xvfb-run`; на macOS — «репетиция обновления»: свой же
  `UpdateUnpack` над `dist/*-macos.zip` и `codesign --verify --deep`. **M**
  И чек-лист перед тегом, десять минут, в `CONTRIBUTING.md`: обновление с
  прошлого релиза на Windows; малый легальный magnet через SOCKS5 до
  «Играть»; выход из игры → автоснимок → восстановление поверх изменённых
  сейвов; `.evsave` на другую систему; недоступный прокси даёт `blocked`;
  `.run` — поставить, обновить, удалить. Это и закрывает «на живой macOS не
  проверено» из архива.
- [ ] **G2. `CHANGELOG` сверять с диффом, а не с планом.** Запись 0.36.0 про
  единый график скорости не соответствует коду: до D1 — поправить разделом
  «Исправлено» в следующей версии. В порядок выпуска: каждая строка раздела
  сверена с `git diff v<прошлая>..HEAD`. Пункт «Для разработки» в 0.37.0 —
  внутренний; исправление ухода запросов мимо прокси заслуживает раздела
  «Безопасность». **S**
- [ ] **G3. Подлинность обновления держится на одной учётной записи
  GitHub.** **[○]** 💬 Windows-клиент молча запускает `setup.exe
  /VERYSILENT` из последнего релиза; `SHA256SUMS` считает то же задание и
  кладёт в тот же релиз, а его недоступность обновление не отменяет — это
  закреплено тестом (`update_install_test.dart:436`), и пропуск сверки в
  журнал не пишется (`update_download.dart:205,218`). Лечение: подпись
  `SHA256SUMS` (ed25519 / minisign) с открытым ключом в приложении и отказ
  без неё; правило защиты тегов `v*`; в `README.md:110` поправить совет для
  macOS — с macOS 15 обход через контекстное меню убран. **M**
- [ ] **G4. CI.** **[✔]** `cancel-in-progress` отменяет прогоны `main`, а
  группа общая для `push`, `schedule` и `workflow_dispatch` — любой пуш в
  понедельник утром отменит еженедельную сборку (`ci.yml:20–22`);
  репозиторий публичный, минуты бесплатны — экономии нет. Отменять только
  для `pull_request`, имя события — в группу. **[○]** `pages.yml` держит
  экшены по тегам и без `timeout-minutes` — вопреки «закрепить всё, что CI
  берёт снаружи»; транзитивные зависимости `bloc_tools` разрешаются заново
  на каждом прогоне — перенести в `dev_dependencies` под
  `--enforce-lockfile` 💬; зерно перемешивания писать в сводку при падении,
  репортёр `failures-only`; сделать тег в форке `dtorrent_task_v2`. **S**
- [ ] **G5. «Ровно то, что гоняет CI» — не ровно.** Локальный перечень в
  `CLAUDE.md` — три команды; в CI сверх них `bloc lint`, сверка
  регистраторов, порог покрытия и случайный порядок — красный `ca1356d`
  упал именно на пороге. Один `tool/gate` со всеми шагами и хук pre-push (он
  же пачкует пуши и снимает половину отмен из G4); версия Flutter локально
  3.47.5 против 3.47.4 в CI — сверять. **S**
- [ ] **G6. Документы против кода.** **[○]** `CLAUDE.md`: «Пять блоков» при
  четырнадцати папках в `lib/bloc` — семь блоков экранов не названы;
  `DtorrentEngine.torrentFromBytes` → `TorrentSource.fromBytes`; «`Vdf` — на
  сорок строк» при ста пятидесяти; «DHT в библиотеке не поднимается» —
  форк его починил (и см. A6); «повторный запуск гасится `isStarted`» —
  см. C1; «в журнал уходит „что случилось → что изменилось“» — только в
  отладке (C3); «диалог добавления сам генерирует id» — теперь `AddGameBloc`;
  правка игры переехала в `library_edits.dart`, `library_drops.dart` не
  назван; `SettingsPatched` не описан; «единственный `BlocListener`» — их
  три; «`DownloadHistoryScope` оборачивает обе половины» — областей две
  (D1). `README.md:364–393` — «пять задач», `needs: analyze`, без
  расписания; `:493` — ссылка на несуществующий `lib/ui/app_colors.dart`.
  `CONTRIBUTING.md:35` — «сборки только на теге», перечень CI без `bloc
  lint`, регистраторов и покрытия. После правок — страж E9. **S**
- [ ] **G7. Этот файл.** Архив ниже — 1500 строк закрытого плана. Ценное в
  нём — решения с причинами: тема в три яруса, «по умолчанию Bloc»,
  `GameRepository` и блоки захвата кнопки и окна — рассмотрены и отклонены,
  нет `bloc_test`, `freezed`, DCM. Им место в `docs/decisions/NNNN-*.md`,
  таблице оценок — в `docs/reviews/2026-09-19.md`; остальное хранят `git` и
  `CHANGELOG`. В `TODO.md` остаётся живое. Четыре стража ссылаются в
  комментариях на «этап 3 / 5 / 6 в `TODO.md`» — это этапы архива; при
  переезде поправить и их. Этапы этого разбора названы буквами как раз
  затем, чтобы с ними не путаться. **M**
- [ ] **G8. Гигиена.** **[○]** В рабочей копии на Windows 93 файла с CRLF,
  включая `tool/*.sh` (в индексе LF, CI в порядке — лечится перевыкладкой);
  нет `.editorconfig`; `site/assets/NunitoSans.ttf` без текста OFL; в
  `pubspec.yaml` шаблонный хвост после `fonts:`; у трёх записей стража
  длительностей нет причины; копия `shortcuts.vdf` перед записью хранится в
  одном поколении — вторая правка затирает оригинал человека; `equatable`
  3.0 и `tray_manager` 0.7 — мажорные обновления ждут решения. **S**

---

## 4. Порядок

1. **A1 — сегодня и отдельным выпуском**: единственное место, где
   приложение удаляет то, чего не создавало. Вместе с ним C17 — тот же
   помощник. Помнить про пятый шаг A1: уже установленных чинит только смена
   имени архива.
2. **A2, A3, A5, A7** — по часу-двум каждое, тесты-сценарии уже описаны; A2
   и A3 временным тестом воспроизведены. Затем **A4** (замок экземпляра) —
   он закрывает второй вход в беду A2.
3. **A6 и A8, половины «в приложении»**: отказ честнее обещания. Правки
   форка — отдельной работой и своим issue; до них подписи в настройках
   говорят правду.
4. **A9** — главная функция на главной системе; затем этап B сверху вниз:
   B1–B4 — про то, что человек теряет при сбое, B5 — сначала issue.
5. **C1–C5** — пять малых правок про закрытие и журнал; остальной этап C —
   по мере работы рядом: загрузки — пачкой, и C14 вместе с правками форка.
6. **Этап E идёт параллельно со вторым шагом** и ничего не ломает: E1(а),
   E3 и E4 — сразу. Перед любым новым дроблением или разбором — E2 и E5:
   иначе следующая перекройка снова пройдёт стражей и снова что-нибудь
   сдвинет.
7. **F1–F3** — сразу, это ошибки; F8 — раньше F9–F10: после него файлов
   станет меньше, и открывать их придётся один раз.
8. **G2, G4–G6** — вечер; **G1** — до следующего релиза, в котором тронут
   обновление, раскладку сохранений или движок.

Темп. Три дня по пятьдесят коммитов — это и есть причина вывода 6. Для
этапов A и B — наоборот: правка, тест, **ручная проверка сценария на живых
данных**, и только потом следующая. После каждого шага — ворота из
`CLAUDE.md`, полный прогон и без `| tail`.

## 5. Чего в плане нет намеренно

- нового дробления виджетов и понижения порогов длины: следующая ступень —
  пробросы и замыкания (F8, E5), а не строки;
- возврата к `GameRepository`, блоку захвата кнопки и `WindowBloc`: решения
  в архиве обоснованы, и рецензии их подтвердили;
- смены формата `.evsave`: B5 с ним совместим, B10 манифест не трогает — и
  оба всё равно идут через issue;
- ухода с `dtorrent_task_v2`: A6, A8 и C14 лечатся правками форка; вопрос о
  замене встанет, только если автор их не примет, а держать форк станет
  дороже;
- хранения пароля прокси в связке ключей: новая зависимость с платформенным
  кодом ради того, что на POSIX закрыто правами, а на Windows — профилем
  пользователя; вернуться, если появится синхронизация настроек;
- `integration_test` на весь интерфейс: дымового запуска собранного
  приложения (G1) хватает, чтобы поймать то, чего тесты не видят вовсе.

---

# Архив. Первый разбор (2026-09-19) — закрыт

Всё, что ниже, оставлено как было: 108 пунктов, все отмечены закрытыми, с
пометками «сделано иначе» и «рассмотрено и отклонено». Что из этого «закрыто»
разошлось с кодом, перечислено в §1 выше; куда этому тексту переехать —
пункт G7. Ссылки вида «§3» и «этап 4» внутри архива — на его собственные
разделы.

Разбор сделан 2026-09-19 по состоянию `main` на коммите `1b33588`.
Прочитан весь `lib/` (32 тыс. строк), `test/`, `tool/` и CI: тема, блоки и
`main.dart` — напрямую, сервисы, экраны и тесты — четырьмя параллельными
рецензиями, ключевые находки которых затем перепроверены по коду. Ничего,
кроме этого файла, не менялось; тесты и сборка не запускались.

Как читать:

- **P0** — ошибка или риск потери данных, чинится первым и с тестом,
  который без правки падает (правило из `CLAUDE.md`);
- **P1** — правила, которые владелец проекта считает обязательными:
  минимальная когнитивная сложность, ни одного приватного виджета, тема;
- **P2** — архитектура состояния; **P3** — гигиена;
- трудоёмкость: **S** — до пары часов, **M** — до дня, **L** — несколько дней;
- 💬 — по `CLAUDE.md` сначала issue (новая зависимость, новый экран,
  смена устройства слоёв).

Числа ниже сняты эвристическим скриптом (комментарии и строки вырезаны,
функции найдены по скобкам, счёт по мотивам когнитивной сложности
SonarSource: +1 за ветвление и цикл, +уровень вложенности, +1 за смену
логического оператора, +1 за тернарник). Это ранжирование «где болит», а
не точный замер; как превратить его в ворота — см. этап 0.

---

## 1. Оценка

| Область | Оценка | Коротко |
|---|---|---|
| Документация и «почему» в комментариях | 5 / 5 | `CLAUDE.md` и комментарии объясняют решения и их цену — редкость. Есть осиротевшие комментарии после распила блоков (этап 1). |
| Тесты и ворота CI | 4.5 / 5 | 940 тестов на 32 тыс. строк, три системы, порог покрытия, тесты-стражи (локализация, контраст, имена артефактов), только ручные фейки. Нет стражей на структуру виджетов, слои и сложность; сборка проверяется только на теге; `DownloadsBloc` покрыт наполовину, потому что движок в него не подменить. |
| Сервисы | 4 / 5 | Транзакция восстановления (план до первой записи, откат, zip-slip, отказ от симлинков), подмена платформы функциями в конструкторе, `AppShutdown`. Но есть серьёзные находки: распаковка обновления не знает симлинков бандла macOS, ошибка загрузки вечна и держит слот, прокси при сбое открывается напрямую (этап 1). Два класса-комбайна: `SaveManager` и `DtorrentEngine`. |
| Когнитивная сложность | 4 / 5 | Из 1357 функций только 4 выше 15 и 26 выше 10. Но 140 функций длиннее 40 строк, 38 — длиннее 60: почти всё это `build` и обработчики блоков. |
| Состояние (Bloc) | 3.5 / 5 | Событийная модель честная, внешние источники поданы событиями. Слабые места: события носят **снимок** игры (гонка «потерянного обновления»), блоки держат друг друга напрямую, бизнес-логика осталась в `State` пяти виджетов. |
| Тема | 3.5 / 5 | Цвета — образцово: `ThemeExtension`, две роли у фирменного цвета, контраст под тестом, сырых цветов в виджетах нет. Нет **типографики**: 184 `TextStyle(...)` по месту в 49 файлах, `textTheme` виджеты не читают ни разу. Около 30 ветвлений `isDark` в 12 файлах виджетов. |
| Структура виджетов | 2.5 / 5 | Против правила «приватных виджетов нет»: 41 приватный виджет, 70 методов, возвращающих виджеты, 35 файлов, где виджетов больше одного (из них 18 — с несколькими публичными). Зато вынос обнажит с десяток повторов, которые сейчас не видны. |

Что сделано хорошо и что трогать не надо:

- токены цвета и моторики через `ThemeExtension`, доступ `context.colors` /
  `context.motion`, системная просьба «не двигаться» соблюдается в одном месте;
- единый `primaryActionFor` вместо четырёх `switch`;
- `JsonStore` (атомарность, очередь, права до переименования), `AppShutdown`,
  хранилище снимков по содержимому с `guard`;
- очередь `asyncExpand` у поиска метаданных, маркер «уже пробовали» до сети;
- тесты-стражи как способ держать правило — этот же приём предлагается
  ниже для новых правил.

---

## 2. Ответы на вопросы о теме

### Хорошо ли сделана тема

Фундамент — да. Не хватает двух верхних этажей:

1. **Типографики нет вовсе.** `EvaporateTheme._textTheme` настраивает
   `TextTheme`, но виджеты его не читают (`textTheme.` в `lib/ui` — ноль
   вхождений). Вместо этого 184 раза написан `TextStyle(...)` по месту, 82
   разных сочетания. Шесть самых частых дают половину всех случаев:

   | Раз | Размер | Вес | Цвет |
   |---|---|---|---|
   | 32 | 13 | — | — |
   | 16 | 12.5 | — | `textSecondary` |
   | 14 | 12 | — | `textSecondary` |
   | 9 | 13 | — | `textSecondary` |
   | 9 | 12 | — | — |
   | 8 | 13 | w600 | — |

   А «моно-метка капсом» уже разошлась сама собой: 9 / w700 / +1.4 в
   `ReadoutCell` (`readout_panel.dart:94`), 8.5 / w700 / +1.4 в
   `_PlaytimeReadout` (`featured_game.dart:418`), 9.5 / w800 / +1.6 в
   `_Eyebrow` (`featured_game.dart:336`). Это ровно тот довод, которым в
   `motion.dart` обоснованы токены длительностей: «разбросанные по виджетам
   числа расходятся сами собой».

2. **Различие схем записано кодом, а не данными.** `CLAUDE.md` называет
   схемы «двумя самостоятельными обликами», но облик компонента решается
   внутри виджета: `colors.isDark ? 0.9 : 0.94`. Таких ветвлений около
   тридцати в двенадцати файлах; в одном `GlassSurface` их шесть
   (`spatial_surface.dart:79–118`). Третья схема
   (высокий контраст, OLED) потребовала бы правки каждого виджета.
   Туда же — пары констант, названные по схеме и выбираемые через `isDark`:
   `hardwareShadowDark/Light`, `grilleHoleDark/Light`, `frostDark/Light`
   (`app_colors.dart:271–289`). Это токены темы, лежащие мимо темы.

Мелочи: `AppColors.heroEyebrow`, `ambientWarm` и `dark.primary` — одно и то
же `0xFFE9C877`, записанное трижды; у Material-компонентов тема задана не
всем (нет `textButtonTheme`, `iconButtonTheme`, `switchTheme`,
`checkboxTheme`), поэтому стиль доопределяют по месту через `styleFrom`
(`featured_game.dart:380`, `common.dart:190`, `common.dart:432`); `copyWith`
и `lerp` на 20 полей написаны руками — забытое в `lerp` поле молча ломает
плавную смену схемы.

### Как во Flutter лучше всего делать темы для виджетов

Три яруса токенов, и каждый следующий берёт значения только у предыдущего:

| Ярус | Что это | Где живёт | Как доступно |
|---|---|---|---|
| 1. Примитивы | сырые значения: `0xFFE9C877`, 13 pt, 380 мс | приватные константы в файлах схем | никак: виджеты их не видят |
| 2. Семантика | роли: `textSecondary`, `primaryFill`, `caption`, `motion.base` | `ThemeExtension`: палитра ✔, моторика ✔, **типографика — добавить** | `context.colors`, `context.motion`, `context.text` |
| 3. Компонент | ручки одного виджета: прозрачность панели, торец клавиши, ореол | `ThemeExtension` **на компонент** — для своих виджетов; `ThemeData.*Theme` — для Material | `GlassSurfaceTheme.of(context)` |

Так устроен сам Flutter: у `Card` есть `CardThemeData`, виджет читает
`CardTheme.of(context)` и достраивает умолчания из `ColorScheme`. Для своих
виджетов штатная точка расширения — `ThemeExtension<T>`.

### Класс темы у каждого виджета?

**Да, но не у каждого и не наследованием.**

- *Не у каждого.* Класс темы нужен компоненту, у которого выполняется хотя
  бы одно: сегодня в нём есть ветвление `isDark`; у него три и больше
  оформительских константы, которые захочется крутить; он переиспользуется
  с вариациями. Виджет, который только собирает другие и читает
  семантические токены, своей темы не заслуживает. Первые кандидаты:
  `GlassSurface` (6 ветвлений), `LauncherActionButton` (торец, ореол, ход
  клавиши), `SpatialBackdrop`, `HardwareGrille`, `AmbientLight`,
  `ReadoutPanel`, кадр `FeaturedGame` (тень), рейка навигации, `CoverBackdrop`.
- *Не наследованием.* Схема «абстрактный `ButtonTheme` + `DarkButtonTheme` и
  `LightButtonTheme`, которые его реализуют» во Flutter не работает: тема —
  это **данные, а не поведение**. Нужен один иммутабельный класс и два его
  **экземпляра**. Наследники ломают `lerp` (плавная смена схемы идёт через
  `AnimatedTheme` и смешивает расширения по полям), `copyWith` и локальное
  переопределение через `Theme(data: …)`.

```dart
/// Материал панели. Схемы расходятся здесь данными, а не ветвлениями.
class GlassSurfaceTheme extends ThemeExtension<GlassSurfaceTheme> {
  const GlassSurfaceTheme({
    required this.fillOpacity,
    required this.sheenOpacity,
    required this.rimOpacity,
    required this.shadow,
    required this.counterLightOpacity,
  });

  final double fillOpacity;
  final double sheenOpacity;
  final double rimOpacity;
  final Color shadow;
  final double counterLightOpacity;

  static const arclight = GlassSurfaceTheme(
    fillOpacity: 0.9, sheenOpacity: 0.62, rimOpacity: 0.15,
    shadow: Color(0x8C000000), counterLightOpacity: 0.05,
  );
  static const cartridge = GlassSurfaceTheme(
    fillOpacity: 0.94, sheenOpacity: 0.72, rimOpacity: 0.32,
    shadow: Color(0x578A8574), counterLightOpacity: 0.16,
  );

  static GlassSurfaceTheme of(BuildContext context) =>
      Theme.of(context).extension<GlassSurfaceTheme>() ?? arclight;

  // copyWith и lerp — как у EvaporatePalette.
}
```

Порядок старшинства тот же, что у Material: **параметр виджета → тема
компонента → умолчание из семантических токенов** (`opacity ?? t.fillOpacity`).

### Хранить тему отдельно от виджетов или рядом

По ярусам:

- **семантические токены** (палитра, типографика, моторика, геометрия) —
  централизованно, в `lib/ui/theme/`: ими пользуются все, и схема обязана
  читаться с одного взгляда;
- **класс темы компонента** — **рядом с виджетом**
  (`widgets/glass_surface/glass_surface.dart` +
  `glass_surface_theme.dart`): это часть его API, он меняется и удаляется
  вместе с ним. Так лежит и у Flutter: `card.dart` и `card_theme.dart` рядом;
- **значения обеих схем** — в том же файле темы компонента, двумя
  константами подряд (`arclight`, `cartridge`): оба облика компонента видны
  разом, и правка одного не забывает второй;
- **сборка** — в одном месте, `EvaporateTheme._build`: только оно знает
  полный список расширений. Страж (этап 0) проверяет, что набор расширений у
  двух схем совпадает и каждое переживает `lerp`.

Что остаётся константой мимо темы — и это нормально: радиусы и семейства
шрифтов в `EvaporateTheme` одни на обе схемы и нужны в `const`-контексте.

---

## 3. Bloc или Cubit

Сейчас правило в `CLAUDE.md` разрешает Cubit «где событие ничего не
добавляет». Предлагается ужесточить — **по умолчанию Bloc**:

> Cubit допустим, только если верно всё сразу: состояние живёт не дольше
> одного виджета или диалога; нет асинхронной работы и внешних источников;
> переходы — простые присваивания (переключатель, выбранная вкладка).

Почему Bloc, а не «тот же блок без обряда»:

1. **Журнал причин.** У события есть имя, и `BlocObserver` пишет в `AppLog`
   «что случилось → что изменилось». Для приложения, где журнал —
   единственный способ узнать, что произошло у человека, это главный довод.
   `BlocObserver` сейчас не заведён вовсе (этап 4).
2. **Трансформеры.** Задержка набора в поиске, «второе нажатие на
   „проверить обновления“ игнорируется», очередь — это свойства события.
   В проекте `asyncExpand` уже стоит в трёх местах.
3. **Один способ вместо двух** — меньше решений на каждую новую надобность,
   то есть меньше когнитивной нагрузки.

Оба нынешних Cubit новому правилу не проходят:

| Сейчас | Почему не Cubit | Что сделать |
|---|---|---|
| `SaveFreshnessCubit` | асинхронное чтение диска | `RestorePreviewBloc` с событием `RestorePreviewRequested(game, snapshot)`: заодно забирает из диалога восстановления всё, что тот считает сам (этап 4). Три состояния «не знаем / не было / менялись тогда-то» сохраняются. **S** |
| `DownloadHistoryCubit` | источник внешний — движок; по правилу из `CLAUDE.md` это уже признак блока. Кормит его виджет `DownloadHistoryScope` через `didUpdateWidget` (`download_activity.dart:35`), и отсюда три беды. У одной задачи **две истории**: карточка в очереди (`task_card.dart:60`) и страница игры (`game_detail.dart:82`) живут одновременно, каждая со своим Cubit, — ровно то расхождение, ради защиты от которого Cubit заводили. История умирает вместе с виджетом: прокрутил список или закрыл страницу — график с нуля. При замершей загрузке выборок нет (`download_history_cubit.dart:53–57`), и «минута до сейчас» перестаёт двигаться | `DownloadHistoryBloc` уровня приложения: события `TasksSampled(tasks)` из потока движка (как `_pushTasks`, `downloads_bloc.dart:112`) и `TaskForgotten(id)`; состояние — истории по id задачи, `peak` и `diskSpeed` — поля, а не методы; часы подменяемые (`DateTime.now()` зашит в `:37,59`). **M** |

Где логика сидит в `State` и просится в блок — этап 4. Что законно остаётся
в `State`: наведение, нажатие, `FocusNode`, контроллеры текста и прокрутки,
тикеры анимаций.

`NavigationBloc` остаётся блоком — решение принято раньше и с новым
правилом согласуется.

---

## 4. План

### Этап 0. Стражи — сначала ворота, потом уборка (P1)

Правило без ворот не держится, а нарушений уже 41 + 70 + 18. Приём —
**храповик**: тест-страж несёт список известных нарушителей; список
нельзя пополнять, а запись, которая перестала нарушать, обязана быть
вычеркнута (тест падает на устаревшей записи). Уборка идёт по частям, а
новых нарушений не появляется с первого дня.

- [x] `test/widget_structure_test.dart` — обходит `lib/ui`, как
  `localization_test.dart`, и падает на: `class _X extends
  (Stateless|Stateful|Inherited…)Widget`; метод, возвращающий `Widget` /
  `List<Widget>`, кроме `build`; больше одного класса-виджета в файле.
  `_XState` законны: это идиома Flutter, а не приватный виджет. **S**
- [x] `tool/check_complexity.dart` + `test/complexity_test.dart` — длина
  функции, вложенность, счёт ветвлений; пороги: сложность ≤ 15, длина ≤ 60,
  вложенность ≤ 3, затем опускать следом за достигнутым (как порог
  покрытия). Первая версия — без зависимостей, на регулярках; точная — на
  `package:analyzer` 💬. **M**
- [x] `test/theme_structure_test.dart` — в `lib/ui` вне `lib/ui/theme/` нет
  `isDark`, нет `TextStyle(` с `fontSize:`, нет `Duration(milliseconds:` и
  `BorderRadius.circular(<число>)`; набор расширений у двух схем совпадает.
  Тоже храповиком. **S**
- [x] `test/layering_test.dart` — `lib/bloc`, `lib/services`, `lib/models`,
  `lib/core` не импортируют `lib/ui`; `lib/models` и `lib/core` не
  импортируют Flutter и сервисы. Сегодня он нашёл бы:
  `downloads_bloc.dart:21` (`ui/labels.dart`), `navigation_bloc.dart:2,57`
  (`FocusNode` в блоке), `models/app_settings.dart:2,4` (`material` и
  `input`), `models/save_snapshot.dart:1` (сервис). **S**
- [x] Линтер. Даром, срабатываний ноль: `avoid_catches_without_on_clauses`
  (все 58 `catch` уже с `on`), `avoid_void_async`. С правкой:
  `only_throw_errors` (бросают строки — `add_game_dialog.dart:306,321,337,338`),
  `prefer_single_quotes` (23 места; заодно закрывает дыру стража кириллицы —
  тот видит только литералы в одинарных кавычках,
  `localization_test.dart:165`), `no_adjacent_strings_in_list`,
  `prefer_const_literals_to_create_immutables` (выпало из набора вместе с
  теми двумя, что проект уже вернул), `unnecessary_await_in_return`,
  `avoid_positional_boolean_parameters` (около 12 мест). `discarded_futures`
  — вложенным `analysis_options.yaml` только для `lib/bloc` и
  `lib/services`: в UI он зашумит. Строка `use_super_parameters`
  (`analysis_options.yaml:52`) лишняя — правило уже в наборе. **S**
- [x] `bloc_lint` 💬. Его `prefer_bloc` — ровно правило «по умолчанию Bloc»,
  поставленное на ворота; из рекомендованного набора полезны
  `avoid_flutter_imports` и `avoid_public_fields`. Запускается не
  анализатором, а `bloc lint .` из `bloc_tools` — отдельный шаг CI.
  `avoid_public_bloc_methods` сработает на осознанных `persist()`,
  `snapshotBeforeLaunch`, `applyLimits`, `closeOpenedGame` — их придётся
  либо исключить с объяснением, либо перевести в события. **S**
  *Зависимость заведена по решению владельца.* Включены `prefer_bloc`,
  `avoid_flutter_imports`, `avoid_public_bloc_methods`,
  `prefer_file_naming_conventions` и `prefer_void_public_cubit_methods` —
  набор в `analysis_options.yaml` под ключом `bloc`, шаг CI «Правила
  bloc» с закреплённой версией `bloc_tools`. Нарушений ноль: девять
  осознанных мест помечены `// ignore:` каждое со своим объяснением
  (потоки наружу, хук «снять сейв перед запуском», ожидание первого
  чтения настроек, `ValueListenable` от движка).
  **`avoid_public_fields` не включён**, и это решение: он про то, что
  данные блока живут в состоянии, а срабатывает на двадцати двух
  внедрённых зависимостях (`settings`, `library`, `engine`,
  `notifications`, `paths`) — их публичность и есть способ подменить их в
  прогоне, а в состояние им нельзя.
  Проверяется только `lib`: в тестах живут блоки-двойники, и
  `prefer_file_naming_conventions` требует от `_CounterBloc` своего файла.
- [x] Страж мёртвых ключей ARB: сейчас их девять (`downloadPaused`,
  `findGamesInFolder`, `gamesWithPaths`, `noticeRestorePartial`, `ofAmount`,
  `pickGameOnTheLeft`, `torrentFallbackName`, `updateNoteLine1`,
  `updateNoteLine2`). **S**
- [x] Записать новые правила в `CLAUDE.md` и `CONTRIBUTING.md` — иначе их
  знает только этот файл: «по умолчанию Bloc» (вместо нынешнего раздела про
  Cubit), «один файл — один публичный виджет, методов-виджетов нет», три
  яруса темы и где что лежит, пороги сложности. **S**

### Этап 1. Ошибки и риски (P0)

Всё здесь найдено **чтением кода**, без запуска. По правилу проекта каждая
правка начинается с теста, который без неё падает, — он же и подтвердит
или снимет находку. Сначала — то, что грозит данным и главной функции.

**Сервисы**

- [x] **Обновление по нажатию на macOS, похоже, ломает бандл.** CI пакует
  архив через `ditto` именно ради симлинков (`ci.yml:178–179`), а
  распаковщик о них не знает: `_extract` (`update_download.dart:273–291`)
  различает только «файл» и «папку», и ссылка ляжет обычным файлом с путём
  внутри. У бандла это `Versions/Current` и сам бинарник фреймворка —
  приложение не стартует, а помощник к этому времени уже убрал прежнюю
  копию. Пакеты сохранений симлинки отвергают явно
  (`restore_transaction.dart:22`), здесь такой проверки нет. На живой macOS
  не проверено. Лечение: создавать `Link` для записей-ссылок, с той же
  проверкой выхода за пределы папки; тест на архиве с симлинком. **M**
- [x] **`SteamGame.merge` забыл `screenshots`** (`steam_catalog.dart:45–51`).
  `bestMatch` (`:303`) сливает найденное по названию с подробностями — и
  кадры теряются: у всех игр, найденных по названию, а не по `appid`,
  подложка из кадров пуста. Тест подменяет `bestMatch` целиком
  (`automatic_metadata_test.dart:52`) и этого не ловит. **S**
- [x] **Ошибка загрузки вечна и держит слот.** `catch` в `_launch`
  (`engine_queue.dart:103–107`) ставит `error`, но оставляет `started`, и
  `_activeCount` (`:37`) продолжает считать задачу; `pumpQueue` (`:23`)
  задачу с ошибкой пропускает, а `resume` (`dtorrent_engine.dart:334`)
  ошибку не снимает. Повторить можно только перезапуском приложения; три
  ошибки при лимите в три запирают очередь. Не найденные метаданные дают
  `null` и тихий выход (`:79–84`) — задача навсегда в «получении
  метаданных». Корень — состояние задачи размазано по шести полям;
  лечится перечнем состояний слота с методами-переходами. **M**
- [x] **Уборка хранилища снимков закрыта от гонки не до конца.** Если работа
  под `guard` началась и закончилась, пока `collect` шёл по диску, закреплённое
  уже сброшено (`snapshot_store.dart:84`), а обход продолжается со старым
  списком живых (`:186–199`) и уносит свежее содержимое. Лечится счётчиком
  эпох: `guard` увеличивает, `collect` при смене прекращает обход. **S**
- [x] **Прокси отказывает «в открытую».** Имя не разрешилось — создаётся
  прямой клиент (`proxy_http_overrides.dart:35,58`), и анонсы трекеру
  уходят с настоящего адреса; остаётся строка в журнале. Для настройки,
  которую включают ради скрытности, безопаснее отказ соединения и
  сообщение человеку 💬. **S**
  *Сделано по решению владельца: отказ и сообщение.* Клиенту подменяется
  способ соединяться, и всякий запрос падает `ProxyUnreachableException`
  — не выдуманным адресом прокси, иначе это выглядело бы оборванной
  сетью и ждало бы таймаута. Куда уходит HTTP, видно снаружи
  (`ProxyHttpOverrides.routing`: `direct` / `through` / `blocked`);
  слушают это загрузки и на `blocked` говорят человеку сообщением — у
  службы ни `Notice`, ни языка. Разрешение имени подменяется в прогоне,
  поэтому проверено и то, что запрос отклонён, и то, что он не ушёл
  напрямую.
- [x] **Пароль прокси мгновение читаем всем.** `CLAUDE.md` обещает «ни
  мгновения», но `_writeText` сначала пишет содержимое, потом зовёт `chmod
  600` (`json_store.dart:123–127`), и код возврата не проверяет. Порядок
  обратный: создать пустой файл, сменить права, записать. **S**
- [x] **Битый UTF-8 в `library.json` — приложение не дождётся библиотеки.**
  `JsonStore.read` ловит только `FormatException` (`json_store.dart:48`), а
  негодная кодировка приходит `FileSystemException`; карантина нет, у
  `_onLoadRequested` (`library_bloc.dart:245`) своего `catch` нет, и
  `loaded` не наступит. `readText` (`:79`) то же исключение ловит. Туда же
  ленивый `.cast<String>()` в `Game.fromJson` (`game.dart:258–262`): ошибка
  типа вылетит при первом чтении поля, мимо `try` в `:259`. **S**
- [x] **Двойной запуск игры.** Проверка «уже запущена»
  (`game_launcher.dart:74`) и регистрация процесса (`:128`) разделены
  ожиданиями, а `_onLaunchRequested` занятость не проверяет
  (`library_bloc.dart:399`): два быстрых нажатия дают два процесса, и выход
  первого стирает запись второго. Резервировать id синхронно. **S**
- [x] **`put` читает файл дважды** — раз для хеша, раз для содержимого
  (`snapshot_store.dart:112,115`). Игра дописала сейв между чтениями — под
  хешем ляжет другое содержимое, и позже это ничем не ловится. Считать хеш
  с тех же байтов, что пишутся. **S**
- [x] После падения между двумя переименованиями
  (`restore_transaction.dart:213–217`) сейвы остаются под
  `.evaporate-old-*`, а уборки таких остатков при следующем запуске нет —
  хотя `update_install.dart:91` её обещает. **M**
- [x] `ScanSession._run` без `try/finally` (`scan_session.dart:86`): любое
  исключение — например, незащищённый `list().any`
  (`library_scanner.dart:170`) — оставляет `_running` навсегда. **S**
- [x] `_openArchive` при исключении не закрывает `InputFileStream`
  (`save_manager.dart:705–715`): на Windows битый `.evsave` остаётся заперт
  до выхода. `_importPackage` (`:624–641`) не проверяет размер, в отличие
  от плана восстановления. **S**
- [x] `maxConcurrent` из файла не зажат (`app_settings.dart:273`): при нуле
  очередь не едет вовсе (`engine_queue.dart:22`). Остальные числа из
  профиля зажимаются — это пропущено. **S**
- [x] Синхронная работа `archive` на главном изоляте: `writeContent`
  (`restore_transaction.dart:278`, `save_manager.dart:632`,
  `update_download.dart:282`), сжатие в `ZipFileEncoder.addFile`
  (`save_manager.dart:243`), чтение и разбор всего обновления в памяти
  (`update_download.dart:168,234`). При снимке в гигабайты это секунды
  замершего окна. `Isolate.run`, как уже сделано для базы путей. **M**
  *Сделано.* У обновления разбор и распаковка идут в изоляте. У сейвов
  вышло иначе и лучше: восстановление снимка из хранилища больше не
  собирает временный zip, а раскладывает файлы прямо оттуда — сжатие и
  разжатие гигабайтов исчезли вовсе, а не уехали в другой поток.
  Раскладка осталась одна: различается только источник байтов
  (`RestoreSource`). Остался синхронный `ZipFileEncoder` при **выгрузке**
  пакета наружу — там сжатие и есть работа, и идёт оно по прямой просьбе
  человека, с показанным ходом.
- [x] Отчёт массовой загрузки врёт: старший дубликат пакета помечается
  «здешние сохранения новее» (`bulk_transfer.dart:165–172`). **S**
- [x] «Не понял — не трогай» у ярлыков Steam дыряво: `allowMalformed` с
  перекодированием молча меняют чужие не-UTF-8 строки
  (`binary_vdf.dart:94,148`), `addGame` выбрасывает значения не-`Map`
  (`steam_shortcuts.dart:248–254`); `pgrep -x steam` (`:178`) на macOS
  стоит проверить — процесс там зовётся иначе. **S**
- [x] Исключения гаснут без журнала: пропуск сверки контрольной суммы
  (`update_download.dart:195–198`), битые пакеты (`save_manager.dart:697`),
  `_adoptMetadata` (`managed_download.dart:96`), витрина
  (`steam_shortcuts.dart:389`). **S**
- [x] Наследие aria2 — не «ровно одно», как сказано в `CLAUDE.md`:
  `ProxySettings.bypass` никем не читается, `DownloadTask.followedBy` движок
  не заполняет (а `downloads_bloc.dart:375` читает),
  `DownloadState.removed` никто не порождает, `_Root.token/depth`
  (`save_path_finder.dart:203`) не используются. **S**

**Блоки и экраны**

- [x] **Потерянное обновление в `GameUpdated`.** Событие несёт целую игру —
  снимок на момент отправки. Обработчик (`library_bloc.dart:306–349`)
  спасает от затирания поля поимённо, и список спасаемых отстал от модели:
  в нём нет `rating` и `shotPaths`. Сценарий: добавили папку → пошёл
  автоматический поиск в Steam → человек тем временем выбирает исполняемый
  файл (`files_section.dart:66–69`: `game` захвачена до `await openFile()`)
  → Steam ответил, записал оценку и кадры → приходит `GameUpdated` со старой
  игрой → оценка и кадры стёрты, файлы кадров осиротели на диске, а
  `steamLookupAttempted` уже взведён, и сам поиск не повторится. Второй
  сценарий: игра запущена, на её странице открыт диалог правила
  (`saves_section.dart:194–227`, до трёх `await`); игра закрылась,
  `GameExited` дописал наигранное время; подтверждение диалога возвращает
  старые `playtime` и `status: running`. То же грозит любому новому полю.
  Лечение — событие несёт **намерение, а не снимок**, и обрабатывается от
  **текущей** игры, как это уже сделано в `_onSaveHintsAccepted`
  (`saves_hints.dart:43`): `GameStatusChanged(id, status, {lastError})`,
  `GameExecutableSet(id, path)`, `GameInstallDirSet(id, dir)` (поиск
  исполняемого — внутри блока, под ключом занятости),
  `GameDownloadLinked(id, taskId, infoHash)`, `GameInstalled(id, …)`, а для
  сохранений — `SaveRuleAdded(gameId, …)`, `SaveRuleRemoved(gameId, ruleId)`,
  `AutoSnapshotChanged(gameId, …)`. Слияние на 44 строки исчезает.
  Отправители: `downloads_bloc.dart` (12), `saves_bloc.dart:203`,
  `saves_section.dart:218,231`, `files_section.dart:69,122`,
  `action_panel.dart:222`, `auto_snapshot_toggle.dart:24`. **M**
- [x] **Пустой шаблон правила разворачивается в рабочую папку процесса.**
  «Сохранить» в `RuleDialog` активна всегда (`rule_dialog.dart:119–122`),
  `_draft()` шаблон только обрезает. `SavePathTemplate.expand('')` отдаёт
  `p.normalize('')`, то есть `.` (`save_path_template.dart:105`), а
  `SavePathRule.resolve` (`save_profile.dart:96`) пустоту не отсекает, и
  проверки на относительный путь в `SaveManager` нет. Снимок такого правила
  заберёт рабочую папку приложения, а восстановление с очисткой цели —
  очистит её. Лечение: `resolve` возвращает `null` для пустого и
  относительного результата; клавиша гаснет на пустом шаблоне; тест на оба. **S**
- [x] **Четвёртый путь добавления правил считает метки не так, как три
  остальных.** `saves_hints.dart:87` `_rulesFor` считает метки по всему
  набору правил, и комментарий там же требует, чтобы источники в этом не
  расходились. Но поиск по базе в `LibraryBloc`
  (`library_metadata.dart:572–583`, `_FoundPaths.labelFor`) считает их
  **только по найденному**: у игры с одним правилом «Сохранения» база
  находит один новый путь → `labelsFor` для единственного пути отдаёт ту же
  метку по умолчанию → в профиле два правила «Сохранения». На другом
  устройстве `_matchLocalRule` (`save_manager.dart:515–525`) от двоякой
  метки честно отказывается — и перенос для этой игры перестаёт работать:
  оба правила уходят в «не удалось сопоставить». Пятый путь — ручное
  добавление: `RuleDialog` предзаполняет ту же метку по умолчанию
  (`saves_section.dart:206`), и уникальность никто не проверяет. Лечение:
  одна функция на все пути (место ей — `SaveProfile.withAddedPaths`), а
  диалог не даёт сохранить метку, которая уже занята; тест на оба сценария. **S**
- [x] **Нажатие на уже выбранный набор эффектов бросает `StateError`.**
  `effects_card.dart:77–81`: стоит `emptySelectionAllowed: true`, а
  обработчик берёт `selection.first`. Нажатие на выбранный сегмент отдаёт
  пустое множество. Лечение: пустой выбор — не действие
  (`if (selection.isEmpty) return`), тест нажатием. **S**
- [x] **Поле скорости теряет набранное.** `speed_field.dart:77–78`: значение
  фиксируется только по Enter и щелчку мимо, а `settings_page.dart:63–72`
  нарочно уводит фокус из полей стрелками — поле продолжает показывать
  число, которое никуда не записано. Свой `onTapOutside` к тому же отменяет
  штатное снятие фокуса, а `didUpdateWidget` нет. Фиксировать на потере
  фокуса. **S**
- [x] **Во время захвата кнопки геймпада срабатывает её прежнее действие.**
  Сервис шлёт и сырую кнопку, и привязанное действие
  (`input/gamepad_service.dart:162–164`), `InputScope` их не глушит
  (`input/input_scope.dart:86–113`): нажатие Y в диалоге захвата
  (`gamepad_settings.dart:238–240`) уводит в библиотеку к поиску, LB/RB
  листают разделы. Захватывается и крестовина — вопреки правилу в `:20–21`,
  `assign` её не фильтрует (`input/gamepad_binding.dart:66`). Лечится блоком
  захвата, который на своё время глушит действия (этап 4). **S**
- [x] **`SettingsChanged` — та же болезнь, что у `GameUpdated`:** событие
  несёт весь `AppSettings`. `settings_page.dart:218–222,364–368`: настройки
  захвачены до `await getDirectoryPath()`, и правка, сделанная, пока открыт
  системный диалог, затирается. Окно узкое, но корень тот же. Правильно
  сделано в `sync_folder_card.dart:75–79`. Лечение — правка функцией от
  текущего значения: `SettingsPatched((s) => s.copyWith(…))`. **S**
- [x] **Сброс файлов в окно — без `catch` и мимо `Notice`.**
  `widgets/game_drop_target.dart:88–180`: свой `_importing`, разбор путей,
  генерация id, ожидание `library.stream.firstWhere` (`:174`, копия из
  диалога добавления), `SnackBar` напрямую (`:119,125`). `finally` флаг
  гасит, но `catch` нет: исключение из `DropImport.inspect` уходит
  необработанным — человек бросил файл, и не произошло ничего, ни
  сообщения, ни записи в журнале. Лечение — событие `FilesDropped(paths)` в
  `LibraryBloc` (этап 4). **M**
  *Сделано:* у приёмника остался один приём. Что завелось, библиотека
  публикует потоком `gameDrops` — раздачу ставят в очередь загрузки, а
  подсвечивает добавленное навигация: ни то ни другое не её дело.
  Разбор подменяем (`inspectDrop`), и в виджет-тестах он не ходит на
  диск — настоящий I/O внутри `testWidgets` не завершается никогда.
- [x] **Запуск пишет устаревшую игру.** `library_bloc.dart:415`:
  `games[index] = game.copyWith(status: running)`, где `game` — из события, а
  перед этим два ожидания (снимок перед запуском идёт секунды). Брать
  `state.games[index]`. **S**
- [x] **Занятость, которую некому погасить.** `saves_bulk.dart:5–33`:
  `_onBulkExport` без `try` — исключение из `_resolveStoredPaths`, `_pruneAll`
  или `persist` оставит ключ `bulk` взведённым, и клавиши переноса погаснут
  до перезапуска. Лечится общим помощником (этап 4), у которого гашение — в
  `finally`. **S**
- [x] **Блок зависит от слоя UI.** `downloads_bloc.dart:21` импортирует
  `ui/labels.dart` ради `engineStateLabel`. Подписи, нужные блокам, —
  в нейтральное место (`lib/l10n/labels.dart`). **S**
- [x] **Осиротевшие комментарии** после распила `LibraryBloc`/`SavesBloc`:
  описание класса досталось `typedef GameExit` (`library_bloc.dart:31–39`);
  про массовый перенос — полю `_launcher` (`:142`); про манифест — геттеру
  `launcher` (`:170`); заголовок «сейвы» стоит над `close()` (`:468`);
  оборванные на полуслове — `library_metadata.dart:592`,
  `saves_snapshots.dart:170–174`; чужое описание у `_onSavePathsProgress`
  (`library_metadata.dart:458`); описание `_matchLocalRule` приклеилось к
  `previewTargets` (`save_manager.dart:488–489`); описание `StatusChip`
  уехало к `IconAction` (`common.dart:154`); «приходят из кубитов» про
  блоки (`shell.dart:89`). **S**
- [x] О новой версии уведомляют видом `NotificationKind.test`
  (`main.dart:339`) — завести `updateAvailable`. **S**

Риски помельче (P2), каждый — **S**:

- [x] Шейдер капель не освобождается при замене и сбросе
  (`cover_drops.dart:96,110`): `dispose()` зовётся только в `dispose`
  виджета, а замена случается на каждый уход выделения с плитки. Кодек не
  освобождается, если `getNextFrame` бросит (`:86–88`).
- [x] Синхронный диск в `build`: `existsSync` дважды на каждое правило
  (`saves/rule_tile.dart:27–29`). Признак «папка есть» — подавать снаружи.
  *Сделано:* `SavePathsPresenceRequested` и `pathPresence` в состоянии
  сохранений. Проверка идёт раз на событие, а не на кадр, и у неё три
  исхода, как у свежести сейвов: есть, нет и «ещё не знаем» — последнее
  строка правила не выдаёт за «нет».
- [x] Ошибки мимо `Notice` и журнала: `showError` и `SnackBar` прямо из
  виджетов — ~~`saves_section.dart:250,416`~~ (уехали в `SavesBloc` вместе
  с поиском папок и разбором пакета), ~~`detail/info_section.dart:111`
  (там же `Process.run` в виджете, `:102–113`)~~ (сделано:
  `GameFolderOpenRequested` и `FileManager` — своя служба, потому что
  команда у каждой системы своя, а настоящий проводник посреди прогона
  никому не нужен), `detail/files_section.dart:80` (там не проглоченная
  ошибка, а ответ на нажатие: уедет вместе с
  `GameExecutableDetectRequested`).
  *Закрыто.* В `lib/ui` остались три прямых сообщения, и все три — ответ на
  только что нажатое: «добавлено N игр» после поиска, «сначала заведите
  игру» и «разрешение не выдано». Это ровно то, для чего SnackBar и
  заведён; проглоченных ошибок мимо журнала больше нет.
- [x] Часов украшений три, а не одни, как обещает `CLAUDE.md`: шаг с
  ограничением `dt` скопирован в `library_atmosphere.dart:90–110`,
  `foil_card.dart:110–119` и `widgets/decorative_motion.dart:71–82`, причём
  атмосфера не смотрит на `lifecycleState` (`:47–50`), в отличие от двух других.
- [x] Поиск папок сохранений из виджета идёт мимо подменяемого `_saveRoots`
  блока: `SavePathFinder.suggest` зовётся статически
  (`saves_section.dart:246`), в тестах его не подменить, а повторное нажатие
  запускает второй обход и второй диалог.
  *Сделано:* `SavePathSuggestionsRequested` в `SavesBloc` с ключом
  занятости, корни подаёт блок, найденное ложится в те же `saveHints` —
  диалога догадок больше нет. Подсказки помечены источником
  (`SavePathOrigin`): «изменились, пока игра работала» про найденное по
  имени было бы неправдой ровно там, где человек решает, доверять ли ей.
- [x] В строке правила нет `Flexible`: метка и три тега в `Row`
  переполнятся на длинной метке (`saves/rule_tile.dart:54–84`).
- [x] `CurvedAnimation` создаётся в каждом `build` и не освобождается
  (`widgets/fade_indexed_stack.dart:68`): на контроллере, живущем всю
  сессию, остаётся по слушателю на каждую смену раздела. То же, мягче, в
  `widgets/rise_in.dart:61`. Завести один раз полем и освобождать.
- [x] Два определения «активной задачи»: метка в обойме считает по
  `activeTasks` (`bloc/downloads/downloads_state.dart:16`), страница
  загрузок — по-своему, с ошибочными и без очереди
  (`downloads_page.dart:54–56`), и это число уходит в показание «N / max».
  Один геттер в состоянии.
- [x] Третий слушатель окна: `window_frame.dart:202–262` держит
  `_maximized`, `_fullScreen` и трюк с `_revision` против гонок — при двух
  уже существующих в `services/system/managed_window.dart:39,91`, о чём
  предупреждает собственный комментарий (`window_frame.dart:144–146`).
  *Сделано:* `WindowModeWatch` — служба с подменяемым окном, и гонка
  наконец проверяется: опрос висит, событие приходит раньше ответа,
  отставший ответ выбрасывается. Рамка подписывается и рисует; форма окна
  уехала в `WindowShell`.
- [x] Чужой док-комментарий в `speed_field.dart:6–10` (остался от
  `ThemePicker`, а сам `ThemePicker` в `pickers.dart:115` — без описания).

### Этап 2. Тема (P1)

- [x] `lib/ui/theme/` — папка вместо трёх файлов в корне `lib/ui`:
  `palette.dart`, `typography.dart`, `motion.dart`, `shape.dart`,
  `evaporate_theme.dart` (сборка), `decor_colors.dart` (бывший `AppColors`).
  `theme.dart` остаётся бочкой с экспортами — импорты не меняются. **S**
- [x] **`EvaporateTypography`** — `ThemeExtension`, собирается из палитры,
  чтобы роль несла и цвет по умолчанию; доступ `context.text.caption`.
  Роли вывести из фактического употребления: `body` 13, `bodyMuted`,
  `bodyStrong` 13/w600, `note` 12.5 muted, `caption` 12 muted, `label` 11.5,
  `path` (моно 12–12.5), `eyebrow` (моно капс), `readoutLabel`, `readout`
  (моно, табличные цифры), `keycap` (Unbounded на клавише), `heroTitle`.
  Затем заменить 184 `TextStyle` по месту. **L**, идёт по папкам.
  Откуда видно, что роли настоящие, — одни и те же стили уже выписаны
  по нескольку раз:
  - пояснение 12.5 + `textSecondary`: `scan_folder_dialog.dart:271,282,291`,
    `action_panel.dart:169,307`, `detail_header.dart:40,63`,
    `restore_dialog.dart:122`, `watched_folders.dart:58`;
  - моно-путь 11.5–13: `rule_tile.dart:88`, `restore_dialog.dart:167`,
    `watched_folders.dart:87`, `scan_folder_dialog.dart:222`,
    `rule_dialog.dart:72`;
  - заголовок строки 13/w600: `rule_tile.dart:58`, `snapshot_tile.dart:84`,
    `watched_folders.dart:48`;
  - строка списка 13 и 11.5 в четырёх диалогах — это работа
    `listTileTheme`, в котором сейчас заданы только цвета (`theme.dart:77`);
  - заголовок игры 24/w700 (`detail_header.dart:29`) — мимо `textTheme`,
    хотя `headlineSmall` настроен ровно для этого;
  - подпись настройки, кегль 13, — 19 раз в `settings/` (`settings_page.dart`,
    `proxy_settings_card.dart`, `gamepad_settings.dart`, `pickers.dart`,
    `speed_field.dart`, `path_setting.dart`, `notification_settings.dart`);
  - у одной роли «пояснение» три разных набора: 12.5 / 1.5
    (`settings_page.dart:418`, `log_card.dart:92`, `effects_card.dart:47`,
    `available_games.dart:48`, `queue_column.dart:291`), 12 / 1.4
    (`proxy_settings_card.dart:272`, `notification_settings.dart:59`) и
    13 / 1.5 (`sync_folder_card.dart:64`, `snapshot_history.dart:43`,
    `bulk_transfer_card.dart:135`);
  - «метка на корпусе» (моно, капс, w700–800, разряд от 0.6 до 2.2):
    `section_heading.dart:56`, `readout_panel.dart:94`,
    `queue_column.dart:254,266`, `app_footer.dart:86`, `top_bar.dart:85`; без
    моно выбиваются `download_activity.dart:236` и `navigation.dart:227`;
  - стиль текста `SegmentedButton` скопирован четырежды (`pickers.dart:41,100,154`,
    `effects_card.dart:82`) — ему место в `segmentedButtonTheme` (`theme.dart:211`).
  *Сделано:* роли заведены (`context.text`, производные от палитры, а не
  `ThemeExtension` — при смене схемы идут за смешанной палитрой сами),
  из 168 кеглей по месту осталось шесть. Пять из них — типографика
  картинки (надписи на обложке и крупном кадре, знак приложения). Не
  сведена клавиша обоймы: это метка без моно, и перевод на `label`
  заметно меняет облик 💬. Подпись подсказки геймпада — теперь `small`.
- [x] Темы компонентов (по образцу из §2), в порядке числа ветвлений:
  `GlassSurface` (6) → `SpatialBackdrop` и `HardwareGrille` →
  `LauncherActionButton` → `AmbientLight` → `ReadoutPanel`, `SectionCard`,
  панель оболочки (`shell.dart:171`), рейка навигации
  (`navigation.dart:103`), кадр `FeaturedGame` (`:73`), `CoverBackdrop`
  (`:74`), подложка панели инструментов (`toolbar.dart:58`). Итог: `isDark`
  остаётся только в `lib/ui/theme/`. **M**
  Два наблюдения в помощь. Тень панели — одна и та же пара (22/10 ночью,
  8/2 днём) в `navigation.dart:103–104` и `featured_game.dart:73–74`: это
  токен материала `panelShadow`, а не ветвление. И образец уже есть в коде:
  `pulse_dot.dart:30` и `LauncherActionButton` (`common.dart:44,117`)
  спрашивают не «какая схема», а «есть ли у материала ореол / торец»
  (`glow.a`, `depth.a`) — так ветвление по схеме превращается в свойство
  токена.
- [x] Кант и заливка состояния — одна роль с четырьмя альфами (0.42–0.6):
  `engine_status.dart:38–39,86–89`, `queue_column.dart:82`,
  `top_bar.dart:72,246–249`, `available_games.dart:122–127`,
  `snapshot_history.dart:90–95`; всего в `lib/ui` 41 вызов
  `withValues(alpha:)` с 27 разными числами. Свести к шкале из
  четырёх-пяти ступеней (`tint`, `rim`, `scrim`, `glass`). Цвет состояния
  движка выбирается одинаковым `switch` в `app_footer.dart:80–85` и
  `engine_status.dart:16–21` — в `labels.dart` или в тему. **S**
  *Сделано:* `EvaporateAlpha` (`theme/alpha.dart`) — пять ступеней по
  роли: `subtle` 0.12, `soft` 0.25, `rim` 0.4, `strong` 0.55, `veil`
  0.82; двадцать четыре альфы по месту сведены к ним, страж держит
  остальные храповиком. В нём остались художники, где прозрачность —
  часть расчёта анимации, подписи поверх обложки и заливка SnackBar'а
  ошибки (0.9: вуаль ослабила бы контраст белого на красном). Цвет
  состояния движка — `EvaporatePalette.engine` в
  `downloads/engine_state_color.dart`.
- [x] Раскладка — тоже токены: поля 28 (`settings_page.dart:75`,
  `saves_page.dart:47`, `downloads_page.dart:85,96,207`), ширина 1340
  (`downloads_page.dart:79`, `saves_page.dart:53`), высоты 64 / 48 / 40
  (`top_bar.dart:24`, `navigation.dart:92`, `app_footer.dart:30`), подпись
  настройки шириной 220 (11 раз) → `EvaporateLayout` и общий
  `ContentFrame` (центр, предельная ширина, поля). **S**
  *Сделано:* `EvaporateLayout` с `pagePadding`, `inset()` и
  `contentConstraints`, страж ловит 28/1340/220 по месту. `ContentFrame`
  не заведён: сохранения прокручиваются списком, а у загрузок снизу
  `Expanded` на всю высоту — общая обёртка вышла бы с флагами на оба
  случая. `_wide = 1340` в панели библиотеки — точка перелома, а не
  ширина содержимого, и осталась своей.
- [x] Украшения: `ambientParticleColor(isDark)`, `waveColors(isDark)` и
  ветвления в `library_atmosphere.dart:68,213,235`, `portal_sparks.dart:279`,
  `game_wave.dart:82` — в расширение `EffectsPalette` (`waveColors`,
  `waveAlpha`, `sparkBlend`, `ambientWash`, цвет частиц): выбор по схеме
  делает тема, а не вызывающий. Текст и тени на обложке
  (`game_cover.dart:229,311`, `detail_cover.dart:102`) — в `CoverTheme`. **S**
- [x] Доопределить темы Material: `textButtonTheme`, `iconButtonTheme`,
  `switchTheme`, `checkboxTheme`, `listTileTheme` (кегль), опасная заливка
  кнопки — вариантом, а не `styleFrom` по месту. **S**
  *Сделано:* кегль сегментов — в `segmentedButtonTheme`; угол текстовой
  клавиши и клавиши-значка — в их темах; `listTileTheme` плотный, и
  плотная раскладка сама даёт кегли 13/12 (`dense` по месту убран);
  опасная, опасная залитая и компактная клавиши — `context.buttons`
  (`theme/button_styles.dart`). `switchTheme`/`checkboxTheme` не
  понадобились: переопределений по месту у них нет.
- [x] Длительности мимо токенов: `nav_tile.dart:66,92,95` (120 мс →
  `motion.instant`), `game_cover.dart:177`, `fade_indexed_stack.dart:14`,
  `liquid_selection.dart:40`, `animated_progress.dart:65`. Радиусы мимо
  токенов: `game_cover.dart:319`, `save_tag.dart:15`,
  `detail_cover.dart:113`, `button_hints.dart:97`. Ширина диалога 560
  выписана в четырёх местах (и 540 в `rule_dialog.dart:56`) — токен
  `dialogWidth`. **S**
- [x] `lerp`/`copyWith` на 20 полей: либо тест «каждое поле смешивается»
  (перебор через `copyWith`), либо генерация (`theme_tailor`) 💬. **S**
  Сделано тестом: у `EvaporatePalette` и `HardwareSurfaceTheme` есть
  `values`, и тест проверяет, что `lerp` на концах и `copyWith()` не
  теряют ни одного поля.

### Этап 3. Виджеты: один файл — один публичный виджет (P1)

**Закрыт: все три списка стража пусты.** Ниже — как оно было и чем стало;
записи оставлены ради «почему так названо» и «почему не проще».

Порядок работы над файлом: приватные классы → методы-виджеты → соседи по
файлу. Имена и папки — ниже; после каждого файла вычёркивать его из
списка стража.

**Приватные виджеты (41):**

| Файл | Сейчас | Станет |
|---|---|---|
| ~~`library/featured_game.dart`~~ | ~~`_Art`, `_CompactContent`, `_Eyebrow`, `_Actions`, `_PlaytimeReadout` + метод `_full`~~ | сделано: `FeaturedArt`, `FeaturedCompactBar`, `FeaturedEyebrow`, `FeaturedActions`, `PlaytimeReadout`, `FeaturedPoster` и `FeaturedFrame` (корпус кадра) в `library/featured/` |
| ~~`library/game_cover.dart`~~ | ~~`_Art`, `_TitlePlate`, `_StatusBadge`, `_ProgressStrip` + `_tile` (70 строк)~~ | сделано: `CoverArt`, `CoverTitlePlate`, `CoverStatusBadge`, `CoverProgressStrip`, `CoverFrame` и `CoverFace` в `library/cover/`; два `_Art` больше не спорят за имя |
| ~~`library/shots_backdrop.dart`~~ | ~~`_Slideshow`, `_Shot`~~ | сделано: `ShotsSlideshow`, `ShotFrame` в `library/featured/` |
| ~~`library/scan_folder_dialog.dart`~~ | ~~`_ScanFolderDialog`, `_Progress`, `_DropArea` + `_list`~~ | сделано: `ScanProgress`, `ScanDropArea`, `ScannedGamesList` в `library/scan/` |
| ~~`library/add_game_dialog.dart`~~ | ~~`_AddGameDialog`, `_PathPicker` + 3 метода~~ | сделано: `AddGameForm`, `SourceKindPicker`, `SourceFields`, `StartNowTile` в `library/add/`; `PathPickerField` — в `widgets/`, поверх `InsetTile` |
| ~~`library/toolbar.dart`~~ | ~~`_AddGameButton` + `_arrange`, `_search`~~ | сделано: `AddGameMenuButton`, `ToolbarLayout`, `LibrarySearchField`, `ShelfTabs` и `ShelfButton` — по файлу в `library/toolbar/` |
| ~~`library/saves_section.dart`~~ | ~~`_FindPathsButton`~~ | сделано: `FindPathsButton`, `SavePathsSection` и `SnapshotsSection` — по файлу в `library/saves/`; сам сборник удалён |
| ~~`library/detail/action_panel.dart`~~ | ~~`_SteamShortcutButton`, `_SteamLookupButton` + `_buildRow`, `_primaryActions` (77 строк)~~ | сделано: `BusyOutlinedButton` (в `widgets/`) и `SteamActions`; `PrimaryActions` выбирает одну из четырёх веток — `RunningGameActions`, `DownloadControlActions`, `PlayActions`, `DownloadStartActions`; `DownloadSummary` и `GameErrorNote` — своими файлами |
| ~~`library/detail/rating_row.dart`~~ | ~~`_Count`, `_Metacritic`~~ | сделано: `ReviewCount`, `MetacriticBadge` — по файлу в `library/detail/` |
| ~~`library/foil_card.dart`~~ | ~~`_FoilScope` (+ `_FoilMotion`, общий для `FoilCard` и `FoilSurface`)~~ | сделано: `library/effects/foil/` — `FoilMotion`, `FoilScope`, `FoilCard`, `FoilSurface` по файлу |
| ~~`library/saves/restore_dialog.dart`~~ | ~~5 методов~~ | сделано: `RestoreDialogBody`, `LocalFreshnessNote`, `RestoreTargetList`, `RestoreOptionsForm` — в `library/saves/restore/`, модель `RestoreOptions` — своим файлом. Клавиш своим виджетом нет: `AlertDialog.actions` ждёт именно список кнопок, и обёртка вокруг них сломала бы его раскладку переполнения |
| ~~`library/saves/*`~~ | ~~`_summary`, `_action`, `_hintRow`, `_footer`, `_absoluteWarning`, локальная `row()`~~ | сделано: `SnapshotTileSummary` (имя `SnapshotSummary` занято общим разделом), `WatchedFolderRow`, `WatchedFoldersActions`, общий `InlineWarning`, `LabeledSwitchRow`. `_action` — не `IconAction`: у той подложка с кантом ради графика загрузки, а ряд таких на каждой строке списка стал бы частоколом, поэтому голая клавиша вынесена своим `TileIconButton`, и строка снимка в общем разделе взяла его же |
| ~~`library/game_detail.dart`, `game_page.dart`~~ | ~~`_content`, `_wrapHistory`~~ | сделано: `GameDetailBody`, `RemoveGameButton`, `BackToLibraryButton` |
| ~~диалоги, собранные внутри методов~~ | ~~`files_section.dart:84–120`, `remove_game_dialog.dart:32–60`~~ | сделано: `ExecutablePickerDialog`, `RemoveGameDialog`; `askRemoveGame` осталась — она открывает окно и ждёт ответа, а это не виджет |
| ~~`shell.dart`~~ | ~~`_Sections` + `_layout`, `_panel`, `_footer`~~ | сделано: `ShellLayout`, `ShellPanel`, `ShellFooterStrip`, `ShellSections`; раздел и волну панель выбирает сама, а не получает сквозь два метода |
| ~~`shell/top_bar.dart`~~ | ~~`_WindowDragArea` + `_brand`, `_actions`, `_windowActions`~~ | сделано: `WindowDragArea`, `TopBarBrand`, `TopBarActions`, `ThemeCycleAction`, `WindowActions`, `TopAction` — по файлу |
| ~~`shell/navigation.dart`~~ | ~~`_QueueBadge` + `_rack` (55 строк), `_button` (58)~~ | сделано: `QueueBadge`, `NavigationRack`, `NavigationKey`, а расчёт «что влезает» — `RackFit` своим файлом; `// ignore` снят перестановкой аргументов |
| ~~`settings/settings_page.dart`~~ | ~~7 методов~~ | сделано: `settings/cards/` — `AppearanceCard`, `WindowStartupCard`, `DownloadSettingsCard` (скорости отдельно, `SpeedLimitsSettings`), `MetadataCard`, `EngineInfoCard`, `SaveSettingsCard`; пояснение — `SettingNote`, выбор папки — `pickSettingsFolder`. Карточки сами читают блоки, страница ни на что не подписана |
| ~~`settings/gamepad_settings.dart`~~ | ~~`_BindingRow`, `_CaptureButtonDialog` + `_status`, `_deadzone`~~ | сделано: `GamepadBindingRow`, `CaptureButtonDialog`, `GamepadStatusRow`, `DeadzoneSlider` |
| ~~`settings/proxy_settings_card.dart`~~ | ~~`_Field`, `_Warning`, `_Note` + 3 метода~~ | сделано: `SettingTextField`, `ProxyKindPicker`, `ProxyAddressFields`, `ProxyApplyRow`; предупреждение — общий `InlineWarning` (`widgets/`), он же в уведомлениях; пояснение — `SettingNote` во всех пяти местах |
| ~~`settings/pickers.dart`~~ | ~~три публичных виджета~~ | сделано: `SegmentedSetting<T>`, поверх него `LanguagePicker`, `ThemePicker`, `WindowStartPicker` — по файлу; `SettingSwitch` на месте всех десяти `SwitchListTile`. `SettingRow` не понадобился: подпись с органом справа осталась только в `SegmentedSetting` и `SettingTextField` |
| ~~`settings/effects_card.dart`, `notification_settings.dart`~~ | ~~`_presets`, `_details`; `_warning`, `_buttons`~~ | сделано: `EffectPresetPicker`, `EffectDetails` (с таблицей украшений); `NotificationActions` |
| ~~`saves/sync_folder_card.dart`~~ | ~~`_PackageRow` + диалог в `_pickGame` (62 строки)~~ | сделано: `SyncPackageRow`, `PickGameDialog`; правило — `gamesMatchingFirst` в `services/saves/title_match.dart`, рядом `sameGameTitle`, на котором теперь и `BulkTransfer.matchGame` |
| ~~`saves/snapshot_history.dart`~~ | ~~`_SnapshotRow` + `_summary`~~ | сделано: `SnapshotRow` (на `HoverBuilder`), `SnapshotSummary` |
| ~~`saves/bulk_transfer_card.dart`~~ | ~~`_BulkReportView` + диалог в `_askAboutNewer`~~ | сделано: `BulkReportView`, `BulkOutcomeGroup`, `ImportNewerDialog` |
| ~~`saves/saves_page.dart`~~ | ~~`_Heading`, `_readout`~~ | сделано: `_Heading` удалена, `SectionHeading` по месту; `SavesReadout` |
| ~~`downloads/downloads_page.dart`~~ | ~~`_Heading`, `_columns`, `_readout`~~ | сделано: `DownloadsHeading`, `DownloadsColumns` (получает размер всей страницы — пороги считаются от неё), `DownloadsReadout` |
| ~~`downloads/download_activity.dart`~~ | ~~`_Metric` + `_metrics`, `_amounts`~~ | сделано: `DownloadMetric`, `DownloadMetrics`, `DownloadAmounts`; соседи `DownloadHistoryScope`, `DownloadChart` (с художником графика) — своими файлами |
| ~~`downloads/task_card.dart`~~ | ~~`_header`, `_actions`, `_stats`~~ | сделано: `TaskHeader`, `TaskActions`, `TaskStats`; ~~`_cancel` (`:21`) и `_removeFromQueue` (`queue_column.dart:18`) — один поток с разными строками, хватит одной функции~~ (сделано: `cancelDownload` в `cancel_dialog.dart`, и страница игры зовёт её же) |
| ~~`downloads/queue_column.dart`~~ | ~~5 публичных виджетов~~ | сделано: `QueueList`, `QueuedCard`, `SectionTitle`, `QueueHint` — по файлу; игру для задачи ищет `LibraryState.gameForTask` |
| ~~`downloads/available_games.dart`~~ | ~~`_RemoveButton`~~ | сделано: `RemoveFromLibraryButton` (имя `RemoveGameButton` занято планом страницы игры); `DraggableGame`, `GameChip` — своими файлами |
| ~~`widgets/button_hints.dart`, `animated_progress.dart`~~ | ~~`_HintChip`, `_Hatching`~~ | сделано: `HintChip`, `ProgressHatching` (с художником насечек). Остались `IndeterminateProgress` и `ProgressFill`: `ClipRRect` там повторён дважды |
| ~~`widgets/liquid_selection.dart`~~ | ~~`_LiquidInkScope`~~ | сделано: `widgets/liquid/` — `LiquidGeometry` (где капля сейчас), `LiquidInkScope`, `LiquidSelection`, `LiquidSelectionInk`, `LiquidPainter`, `liquidSelectionPath`. Помеха снята геометрией: подписи и рисовальщик берут у неё готовый контур, а не поля чужого `State` |
| ~~`widgets/window_frame.dart`, `readout_panel.dart`, `ambient_light.dart`~~ | ~~`_resize`, `_withBars`, локальная `wash`~~ | сделано: `WindowResizeZone`, `ReadoutRow`, `AmbientWash`; `WindowControl`, `ReadoutCell`, а ещё `WindowChrome` и `runWindowAction` — своими файлами, чтобы полоса у края не ссылалась на рамку по кругу |
| ~~`shell/app_footer.dart`, `downloads/engine_status.dart`~~ | ~~по два публичных~~ | сделано: `EngineReadout` и `EngineFailure` — своими файлами |
| наведение | `_hovered` в четырёх `State` (`top_bar.dart:218`, `available_games.dart:104,176`, `snapshot_history.dart:73`) | сделано: `HoverBuilder` (`widgets/hover_builder.dart`), все четыре места на нём |

**Методы, возвращающие виджеты (70).** Это те же приватные виджеты, только
без своего `Element`: нет границы перестроения, нет `const`, в дереве
инспектора их не видно. Лидеры: `settings_page.dart` (7; `_downloadsCard` —
83 строки, `_savesCard` — 65) → по карточке на файл в `settings/cards/`;
~~`library_page.dart` (7: `_heading`, `_toolbar`, `_measuredGrid`, `_grid`,
`_tile`, `_cover`, `_empty`)~~ — сделано: `LibraryGrid`, `LibraryGridTile`,
`LibraryEmptyState`, `LibraryHeadingBar`, `LibraryShelfBar`,
`LibraryFeaturedSlot`, а память сетки (ключи плиток, фокусы, наведение,
прокрутка) — `LibraryGridController`; отбор по полке — `gamesOnShelf` в
`shelf.dart`; `restore_dialog.dart` (5: `_content`,
`_localFreshness`, `_targetList`, `_options`, `_actions`); по три —
`top_bar.dart` (`_brand`, `_actions`, `_windowActions`), `shell.dart`,
`proxy_settings_card.dart`, `add_game_dialog.dart`, `task_card.dart`
(`_header`, `_actions`, `_stats`). Полный список даёт страж из этапа 0.

Не путать с этим `effects_card.dart:133` `_effects`: он возвращает таблицу
данных (`List<_Effect>`), а не виджеты, — так и надо, таблица вместо
тринадцати выписанных переключателей и есть снижение сложности.

**Файлы-сборники:** ~~`widgets/common.dart` (6 виджетов и 3 функции) →
`launcher_action_button.dart`, `icon_action.dart`, `status_chip.dart`,
`section_card.dart`, `empty_state.dart`, `info_row.dart`, а `showError` /
`showInfo` / `confirm` → `lib/ui/feedback/`~~ (сделано: сборник удалён, у
каждого импортёра — точный импорт, а не реэкспорт); ~~`downloads/queue_column.dart`
(5); по три — `toolbar.dart`, `download_activity.dart`,
`available_games.dart`, `spatial_surface.dart`, `pickers.dart`.~~ (тоже
разошлись; из `spatial_surface.dart` вышли `GlassSurface` и
`HardwareGrille`, а `SpatialBackdrop` удалён — его не звал никто).

**Повторы, которые вынос обнажит** — их сводят в общие виджеты, а не
копируют по новым файлам:

- ~~`play_button.dart` — пустая обёртка над `LauncherActionButton` (`:12–16`),
  удалить~~ (удалена);
- ~~подпись хода загрузки: `switch` в `_ProgressStrip`
  (`game_cover.dart:289–297`) и в `CoverProgress`
  (`detail/detail_cover.dart:80–87`) совпадают побайтово;~~ (сведено в `downloadProgressShort` в `ui/labels.dart`);
- ~~условие «задача есть и не завершена» выписано в пяти местах
  (`game_cover.dart:59,77`, `game_detail.dart:42`, `action_panel.dart:42`,
  `detail_cover.dart:28`), хотя у задачи уже есть `isFinished`
  (`models/download_task.dart:70`);~~ (все пять на `isFinished`);
- ~~тонированная плашка: `save_tag.dart:14`, `rating_row.dart:144` и
  `StatusChip` (`common.dart:228`) — три реализации с альфой 0.13 / 0.14 →
  один `TonedChip`;~~ (сделано);
- ~~карточка-строка: `rule_tile.dart:31–38`, `snapshot_tile.dart:42–49`,
  `watched_folders.dart:31–38` → `InsetTile`;~~ (сделано, и пакет синхронизации туда же); ~~«колодец» поиска и полок —
  `toolbar.dart:119–125` и `:246–252`;~~ (сделано: `ToolbarWell`);
- ~~предупреждение со значком (12–12.5, `warning`, высота 1.4) —
  `action_panel.dart:65`, `restore_dialog.dart:133,153`,
  `rule_dialog.dart:138`, `scan_folder_dialog.dart:369` → `InlineWarning`;~~
  (сделано);
- ~~кружок занятости 14×14 — в шести местах → `BusySpinner`;~~ (сделано);
- ~~`DottedBorderBox` (`drop_overlay.dart:56`) рисует сплошную рамку — имя
  обещает другое → `DropFrame`.~~ (сделано, и своим файлом).

Приватные `CustomPainter` виджетами не являются и под правило не попадают,
но ~~`portal_sparks.dart` (536 строк: виджет, художник, атлас, пачка искр,
отрисовщик, геометрия `edgeAt` на 68 строк) стоит разложить в
`library/effects/portal/` ради проверяемости частей.~~ (сделано: кромка —
`PortalOutline`, и проверяют её теперь без снопа, одной подстановкой
размера; `edgeAt` ужалась вдвое поворотной симметрией контура). ~~Сами украшения
(`portal_sparks`, `particle_field`, `cover_drops`, `game_wave`, `hero_sweep`,
`foil_card`, `library_atmosphere`) — в `library/effects/`: папка `library/`
сейчас мешает их со страницами.~~ (переехали; `shots_backdrop` остался в
`library/`: это не украшение, а подложка из кадров самой игры).

### Этап 4. Состояние (P2)

- [x] **`BlocObserver`** → `AppLog`: ошибки всегда, переходы — в отладке. **S**
  *Сделано:* `LoggingBlocObserver`, ставится в `_startLog` — до первого
  блока. Переходы только в отладочной сборке: движок загрузок шлёт своё
  состояние каждую секунду на каждую задачу, и рассказ о случившемся в
  этой ленте утонул бы.
- [x] **Общее у блоков — примесями.** `_notice`, `_withBusy`,
  `_finishBusy`, `_l`/`_localizations`, `_notifySystem`, отложенная запись с
  дожиданием в `close()` повторены в трёх блоках почти дословно. Десяток
  обработчиков повторяет «взвести → `try` → погасить + сообщение» — заменить
  помощником, у которого гашение в `finally`:

  ```dart
  Future<void> _busyWhile(Emitter<S> emit, String key,
      Future<String?> Function() work) async { … }
  ```
  **M**
  *Сделано:* `bloc/bloc_common.dart` — `NoticeBloc` (сообщения с номером и
  журналом), `BusyBloc` (`busyWith`, `finishBusy`, `busyWhile`) и
  интерфейс `BusyState`, который состояния реализуют одной строкой.
  Гашение занятости в `finally`: обработчик, упавший на чужом исключении,
  оставлял клавишу погашенной до перезапуска. На `busyWhile` переведены
  те обработчики, где «взвести → работа → погасить» и есть весь смысл;
  у массового переноса и восстановления гашение своё — они кладут в
  состояние ещё и отчёт.
- [x] **`UpdateBloc`** вместо состояния в `AboutCard`
  (`about_card.dart:42–204`: `_busy`, `_message`, `_isError`, `_found`,
  `_updating` — при правиле «свой `_busy` в экранах не нужен»). События:
  `UpdateCheckRequested`, `UpdateInstallRequested`, `UpdateProgressed` (от
  загрузчика — внешний источник). Уровень приложения: сейчас проверка при
  запуске (`main.dart:326`) и проверка по клавише — две разные, и найденное
  на старте карточка не знает. **M**
  *Сделано, кроме уровня приложения:* блок пока живёт при карточке, а
  проверка на старте по-прежнему своя. Свести их — значит поднять блок в
  `main` и решить, кто показывает уведомление; это отдельная правка.
  Замену приложения теперь есть чем проверить, и проверки написаны:
  найденная версия, свежая копия, неудача проверки, копия, которую нам не
  переписать, отказ открыть ссылку и запись в меню приложений, которую
  после переключения переспрашивают у системы.
- [x] **`AddGameBloc`** вместо `_AddGameDialogState` (`_busy`, `_error`,
  проверки броском строк, ожидание появления игры в чужом состоянии —
  `add_game_dialog.dart:249–262`). **M**
  *Сделано, и тест сразу нашёл ошибку:* `Uri.decodeComponent` на битой
  доле процентов в magnet-ссылке бросает `ArgumentError`, а ловился
  только `FormatException` — вставленная откуда попало ссылка роняла
  разбор прямо во время набора. Окно разложено на `AddGameDialogView` и
  `AddGameFields`; контроллеры текста и системные окна выбора остались у
  окна — это ресурсы, а не состояние.
- [x] **`LibraryViewBloc`** — запрос и полка из `_LibraryPageState`
  (`library_page.dart:45–46`), отбор и сортировка (`:444–491`) — чистыми
  функциями рядом с блоком, под тестом; событие набора — с задержкой. В
  `State` остаются наведение, фокус, прокрутка. **M**
  *Сделано, кроме задержки на набор — и это решение, а не пропуск.* Отбор
  и сортировка полусотни игр стоят доли миллисекунды, а таймер в этом
  месте пришлось бы пережидать в каждом виджет-тесте, который что-нибудь
  ищет. `Shelf` и `gamesOnShelf` переехали в `lib/models`: блоку `lib/ui`
  закрыт.
- [x] **`ScanBloc`** для окна поиска игр: `ScanSession` — внешний источник,
  как движок загрузок, то есть блок по правилу самого `CLAUDE.md`. События:
  `ScanStarted`, `ScanNarrowed(dir)`, `ScanFolderDropped(paths)` (забирает
  `Directory.exists` из `scan_folder_dialog.dart:106`), `ScanStopRequested`,
  `ScanSessionChanged`, `ScanGameToggled(dir, on)`. Сейчас выбранное
  пересобирается на каждую строку списка (`:81–88`), а в библиотеку уходит
  N событий `GameAdded` (`:118–133`) — вместо них одно `ScannedGamesAdded`.
  Признак «тащат над окном» остаётся в `State`. **M**
  *Сделано.* Отбор переживает приход новой находки, `Directory.exists`
  уехал из виджета, а заведение идёт одним событием — не десятком с
  десятком отложенных записей. Окно разложено на `ScanDialogView` и
  `ScanDialogBody`.
  *Попутно выяснено и записано в тесте:* блок, заведённый **внутри**
  `tester.runAsync` вместе с диалогом, доставляет свои события в
  настоящем времени, а не в фейковом времени теста, — и `pump` их не
  ждёт. Отметке в проверке нужен `runAsync` перед кадром.
- [x] **`RestorePreviewBloc`** вместо `SaveFreshnessCubit`: состояние
  `{freshness, targets, newer}`. Сейчас правило «снимок новее здешних
  сейвов» живёт в виджете (`restore_dialog.dart:39–41`), а `previewTargets`
  зовётся из `build` через `context.read<SavesBloc>().saveManager`
  (`:59–62`). Галочки «резервная копия» и «очистить» — состояние формы,
  остаются в `State`. **S**
- [x] **`RuleFormBloc`** для диалога правила: метка, шаблон, развёрнутый
  путь, переносимость, `canSave`. Сейчас превью пересчитывается в `build`
  через `setState(() {})` на каждое нажатие клавиши (`rule_dialog.dart:71`),
  а проверок нет вовсе (см. пустой шаблон и занятую метку в этапе 1).
  Контроллеры текста остаются в `State`. **S**
  *Сделано.* Проверки к этому времени уже были (этап 1), но жили в
  `build` — теперь они под тестом: пустой шаблон, занятая метка,
  непереносимый путь как предупреждение, а не отказ, и метка по умолчанию
  константой. Поля уехали в `RuleFormFields`, `RuleDraft` — к блоку.
- [x] В `SavesBloc` — события вместо логики в `saves_section.dart`:
  ~~`SavePathSuggestionsRequested(game)` с ключом занятости и результатом в
  уже существующие `saveHints`;~~ (сделано, см. этап 1)
  ~~`SnapshotImportInspectRequested(path, game)`
  с полем `pendingImport` (сейчас `inspectPackage` и `try/catch` в виджете,
  `:392–417`).~~ (сделано: разобранный пакет ждёт ответа человека в
  состоянии, битый приходит `Notice`-ом и в журнал, а выбор файла остался
  у окна — системное окно не состояние). В `LibraryBloc` —
  ~~`GameExecutableDetectRequested(id)` и `GameFolderOpenRequested(id)`~~
  (оба сделаны: обход папки идёт в блоке, «ничего не нашлось» приходит
  `Notice`-ом, а найденное ждёт выбора человека в состоянии — у сборок с
  лаунчером и движком имена похожи до неразличимости). **M**
- [x] **`LogBloc`** для `LogCard` (`log_card.dart:29–50`: `_lines`, свой
  `_busy`, чтение и очистка). События `LogShowRequested`,
  `LogClearRequested`; побочная выгода — тесты без `runAsync`. **S**
  *Сделано, и побочная выгода получена:* показ, очистка и «ещё не
  показывали» проверяются обычным тестом с подменённым `AppLog`. Строки
  журнала уехали в `LogView`; у карточки осталось копирование — буфер
  обмена не состояние.
- [x] ~~**`ButtonCaptureBloc`** для диалога захвата кнопки
  (`gamepad_settings.dart:232–247`): поток нажатий геймпада — внешний
  источник. События `CaptureStarted(action)`, `RawButtonPressed(button)`,
  `CaptureCancelled`; на время захвата блок глушит действия в
  `GamepadService` — это и закрывает ошибку из этапа 1.~~ **S**
  *Рассмотрено и отклонено.* Ошибка этапа 1 закрыта заглушкой в самой
  службе, и проверена она там же, где живёт: «на время захвата кнопка
  приходит сырой», «крестовина не отдаётся», «после захвата действия
  возвращаются» (`gamepad_service_test.dart`). У окна остаётся одно
  состояние — «ждём нажатия», — а заглушка держится ровно столько,
  сколько открыто окно: `initState`/`dispose` для этого и заведены. Блок
  добавил бы событие, которое тут же превращается обратно в
  `Navigator.pop`.
- [x] ~~**`WindowBloc`** вместо состояния в `AppWindowFrame`
  (`window_frame.dart:202–262`): события от `WindowListener`
  (`WindowMaximized`, `WindowUnmaximized`, `FullScreenEntered`,
  `FullScreenLeft`) и `WindowSizeToggled` от клавиши. Последовательная
  обработка убирает трюк с `_revision`, а слушатель окна снова один.~~ **M**
  *Сделано иначе — службой `WindowModeWatch`.* Окно здесь такой же внешний
  источник, как движок загрузок: им владеет служба, а не блок, и отдаёт
  наружу `ValueListenable`. Блок на одно поле «развёрнуто» был бы
  передаточным звеном. Трюк с номером правки остался, но перестал быть
  незаметным: опрос висит, событие приходит раньше ответа, отставший
  ответ выбрасывается — и это проверено тестом, чего в виджете было не
  сделать вовсе.
- [x] **`FilesDropped(paths)`** в `LibraryBloc` вместо логики в
  `GameDropTarget`: разбор, занятость, `Notice`. Игры из `.torrent`
  библиотека публикует потоком, как `gameExits`, а `DownloadsBloc`
  подписывается и сам шлёт себе `DownloadRequested` — зависимость остаётся
  односторонней, а ожидание `library.stream.firstWhere` исчезает из обоих
  мест, где оно скопировано. **M**
- [x] **`ProxyFormBloc`** (`proxy_settings_card.dart:46–56`): сейчас неверный
  порт молча подменяется старым (`:50`), диапазон не проверяется, строка
  адреса показывает сохранённое, а не набранное (`:176`), `showInfo` идёт
  мимо `Notice` (`:55`). Состояние `{draft, portError, dirty}`; разбор —
  чистой функцией `ProxySettings.fromForm` под тестом. **S**
  *Сделано все четыре.* Разбор лежит геттерами состояния, а не отдельной
  функцией: `parsedPort`, `portInvalid`, `draft`, `dirty`, `canApply` —
  считаются от набранного и проверяются без окна. `showInfo` не заменён
  на `Notice`, а убран вовсе: клавиша активна, только когда есть что
  применять, и гаснет по нажатию — это и есть ответ человеку.
- [x] Производное — геттерами состояния, а не в `build`:
  сортировка всех снимков на каждую пересборку (`saves_page.dart:34–38,89–98`),
  подсчёты на странице загрузок (`downloads_page.dart:54–57`), отбор
  доступных игр (`available_games.dart:21–30`). Пересчёт места в очереди в
  индекс движка (`queue_column.dart:139–152`) — знание движка внутри
  виджета; событие `DownloadReordered(id, beforeId)` его забирает. **S**
  *Сделано:* `SavesState.entriesFor`, `LibraryState.downloadable`,
  `DownloadsState.orderIndexBefore`. Виджет очереди теперь называет
  соседа, а место среди всех задач считает состояние — и это наконец
  проверяется без движка.
- [x] Разделы — перечислением `AppSection`, а не числами: сейчас два
  параллельных списка и магическое `_downloadsSection = 1`
  (`navigation.dart:48,60–71`), `sectionCount = 4` в блоке и `section != 0`
  в оболочке. Блок остаётся блоком. **S**
  *Сделано:* `models/app_section.dart`, подпись — `sectionLabel` рядом с
  прочими переводимыми подписями моделей. Обойма собирается по
  `AppSection.values`, метка задач у загрузок — записью в карте, а не
  номером.
- [x] `DownloadHistoryBloc` — см. §3.
- [x] **`DownloadsBloc` не проверить без торрентов.** Он создаёт
  `DtorrentEngine` сам и держит по конкретному типу
  (`downloads_bloc.dart:43,100`). Одним параметром конструктора это не
  лечится: блок зовёт `verify`, `torrentPathFor`, `reorder`, `setProxy`,
  `pumpQueue`, `applyLimits`, которых в «узком» `DownloadEngine` нет, —
  контракт надо дорастить до того, чем блок пользуется. Цена вопроса видна
  в покрытии: из 13 событий тесты шлют два; запрос загрузки, пауза,
  возобновление, отмена и перестановка не исполняются ни разу (файл покрыт
  на 59 %). После правки — `FakeDownloadEngine` и тесты на эти пути. **M**
  *Контракт дорос до того, чем блок пользуется* (`downloadDir`,
  `maxConcurrent`, `pumpQueue`, `reorder`, `setProxy`, `torrentPathFor`,
  `verify`, `dispose`), движок приходит параметром конструктора, а
  `pumpQueue` и `reorder` переехали из расширения `EngineQueue` в класс:
  членами интерфейса расширения не бывают. `FakeDownloadEngine`
  записывает поручения и отвечает тем, что задал тест; отказ движка в нём
  одноразовый — доживший до конца теста, он встретил бы `close` и уронил
  бы прогон не там, где проверяли. 21 новый тест
  (`downloads_bloc_test.dart`), покрытие блока 59 % → 91 %, общий порог
  поднят с 78 % до 80 %.
- [x] Подписки на состояние целиком: `context.watch<…>().state` в 19 местах
  (`library_page.dart:120–123` перестраивает сетку на любой чих `busy` и
  `savePathsProgress`). Заменить на `select` по нужным полям. **S**
  *Сделано там, где это и было важно* — на странице библиотеки: игры и
  открытая игра берутся `select`, и занятость с ходом поиска путей сетку
  больше не трогают. Карточки настроек оставлены на `watch`: они
  показывают полтора десятка полей одного и того же состояния, которое
  меняется по нажатию человека, и `select` на каждое поле был бы длиннее
  самой карточки. Заодно раскладка страницы уехала в `LibraryBody`: у
  `LibraryPage` на руках остались выбор, поиск, фокус и системные окна.
- [x] **`GameRepository`** 💬 — единственный владелец списка игр и файла
  `library.json`; правка — функцией от текущего значения
  (`patch(id, (game) => …)`). `LibraryBloc`, `DownloadsBloc`, `SavesBloc`
  зависят от него, а не друг от друга — так советует и документация bloc
  («связывать блоки через доменный слой»). Уходят: слияние в
  `_onGameUpdated`, трюк `SavesBloc._updateGame`, ожидание игры в диалоге
  добавления. Делать после намеренческих событий из этапа 1. **L**
  *Решено не заводить, и вот на чём это решение стоит.* Три беды, ради
  которых слой предлагался, к этому дню закрыты по отдельности и
  дешевле: слияние в `_onGameUpdated` исчезло вместе с самим событием —
  правка игры стала намерением; `SavesBloc._updateGame` исчез там же.
  Осталось одно ожидание в `AddGameBloc._startIfRequested`, на одну
  строку и с записанной причиной, — ради него слой не заводят.
  Зависимость блоков при этом уже односторонняя и объяснена в
  `CLAUDE.md`: сохранения знают библиотеку, библиотека о них — нет, а всё
  обратное идёт потоками (`gameExits`, `gameRemovals`, `gameDrops`).
  Репозиторий эту зависимость не убрал бы, а переставил: на его месте
  оказались бы те же потоки, отложенная запись, `beforeLaunch` и
  порядок гашения — то есть ровно то, что сейчас проверено тестами и
  ни разу не гоняется. Цена **L**, выигрыш — чистота схемы; при таком
  счёте это работа, которую заводят под конкретную нужду, а не впрок.
  Если нужда появится (третий блок, которому понадобится править игры),
  решение стоит пересмотреть — но начинать с него, а не со слоя.
- [x] `LibraryBloc` пишет файлы сам (обложки, кадры —
  `library_metadata.dart:175–259`). Вынести в `CoverCache` и
  `GameMetadataFetcher`: `_onSteamLookup` (94 строки, сложность 21) станет
  «спросить сервис → положить в состояние». **M**
  *Оба сервиса заведены* в `lib/services/metadata/`: сборщик ходит в Steam
  и отдаёт всё за один заход (`appid`, описание, обложку байтами, кадры,
  оценку), кэш кладёт и убирает файлы — и только свои. `_onSteamLookup`
  ушёл из списка стража целиком (было 94 строки и сложность 29), правка
  карточки вынесена в `_applyMetadata`. Проверка «та ли игра» осталась
  двойной: после сети и после записи файлов — запись тоже ожидание.
  Уборка обложек при удалении игры (`_onGameRemoved`) тоже перешла на
  кэш. 15 новых тестов, оба сервиса подменяются параметром конструктора.
- [x] `FocusNode` внутри `NavigationBloc` — ресурс UI в блоке. Перенести в
  оболочку, а запрос фокуса подавать одноразовым сигналом по образцу
  `Notice.seq`. Блок остаётся блоком. **S**
  *Узел переехал в `_LibraryPageState`, к сетке и прокрутке: он живёт
  ровно столько, сколько поле поиска, а блок о существовании поля знать
  не обязан.* В состоянии остался счётчик `searchFocusSeq`, и счётчик, а
  не флаг, по той же причине, что у `Notice.seq`: вторая просьба подряд
  иначе не отличалась бы от первой, и фокус, уведённый человеком, туда
  уже не вернулся бы. Фокус ставится после кадра — просьба приходит
  вместе с переключением на библиотеку, и поля в этот миг ещё нет.
  Тесты спрашивают о фокусе сам виджет поля (`searchHasFocus`), а не
  блок.
- [x] События с неполным `props` (`GameUpdated` сравнивается по четырём
  полям из двадцати семи). Если события никто не сравнивает — убрать у них
  `Equatable`; иначе — полные `props`. **S**
  *Решено иначе, и правило записано в самих файлах событий.* `GameUpdated`
  больше нет — правка игры стала намерением. У оставшихся в `props` идут
  `game.id` и `snapshot.id` намеренно: полные `props` свелись бы к
  тождеству ссылок (ни `Game`, ни `SaveSnapshot` не `Equatable`), и «то же
  событие о той же игре» не совпало бы само с собой. Сравнивают события
  только тесты; трансформеры, которым равенство важно, не используются.

### Этап 5. Когнитивная сложность (P1)

Рецепты: ранний выход вместо вложенности; конвейер именованных шагов;
таблица вместо ветвлений; результат-значение вместо исключения для потока
управления; один уровень абстракции на функцию.

| Сложн. | Строк | Где | Что сделать |
|---|---|---|---|
| ~~22~~ | ~~106~~ | ~~`services/saves/bulk_transfer.dart:136` `importAll` (и `:63` `exportAll`)~~ | сделано как задумано: `_importOne`/`_exportOne` на ранних выходах, плюс `_restoreOne` и `_localIsNewer`; накопители ушли — счёт снимает сам отчёт, и разойтись с ним записям негде |
| ~~22~~ | ~~61~~ | ~~`services/saves/restore_transaction.dart:10` `_buildRestorePlan`~~ | сделано конвейером, как задумано: `_payloadEntries()` → `_destinationFor()` → `_PlanBuilder.add()`; счётчики и отказы держит строитель |
| 21 | 94 | `bloc/library/library_metadata.dart:31` `_onSteamLookup` | сеть и файлы — в сервис (этап 4) |
| ~~16~~ | ~~71~~ | ~~`services/launch/game_launcher.dart:70` `launch`~~ | сделано как задумано: `_resolveExecutable` (все отказы и «бандл → бинарник») → один `Process.start` → `_track`; развилка по системе осталась одна |
| 15 | 46 | `ui/widgets/game_drop_target.dart:88` `_handleDrop` | уходит в блок событием `FilesDropped` (этап 4) |
| ~~15~~ | ~~43~~ | ~~`services/launch/vdf.dart:70` `_tokens`, `:27` `parse`~~; `binary_vdf.dart:65` ещё нет | сделано: разбор ведёт `_VdfDocument` — где мы, что кладём, чем считать строку, — а разэкранирование стало `_unescape` выражением `switch`. Запись значения в `binary_vdf` по-прежнему цепочкой `if is` |
| 15 | 38 | `services/launch/steam_shortcuts.dart:355` `_putArtwork`, `:421`, `:127` | таблица `SteamArtwork.files(id)` и один цикл; `sync*`-итератор сегментов JPEG + `firstWhere`; разбор заголовков картинок (`:396–459`) — в свой файл |
| ~~14~~ | ~~72~~ | ~~`bloc/library/library_metadata.dart:497` `_onSavePathsLookup`~~ | сделано: `done()` ушёл в `finally`, запись найденного — в `_applyFoundPaths` |
| ~~13~~ | ~~72~~ | ~~`services/saves/save_manager.dart:415` `_restoreFrom`~~; `:139`, `:219` | сделано: `_checkedManifest` (общий с разбором пакета), `_resolveTargets` и `_backupBeforeRestore`. В `_materialize` флаг `complete` пока на месте |
| ~~12~~ | ~~43~~ | ~~`services/download/engine_queue.dart:66` `_launch`, `:20`~~ | сделано: `_modelFor` был и раньше, добавились предикат `_stillWanted(managed, generation)` и `_startTask` |
| ~~12~~ | ~~41~~ | ~~`services/launch/executable_finder.dart:71`, `:143`~~ | сделано: очки за расширение — таблицей по системам, обход папки — `_visitDirectory` |
| ~~12~~ | ~~31~~ | ~~`services/launch/windows_installs.dart:62` `installed`~~ | сделано: `_query` отдаёт пустую строку при сбое — отсутствующая ветвь реестра обычное дело, — и дальше обход идёт плоско |
| ~~12~~ | ~~27~~ | ~~`services/saves/ludusavi_catalog.dart:126` `find`~~ | сделано как задумано: ленивый `Map<int, LudusaviEntry>` (сбрасывается вместе с манифестом) и общий `ReleaseName.bestMatch` с каталогом Steam — порог остался разным, у базы путей он выше |
| ~~10~~ | ~~64~~ | ~~`services/system/update_download.dart:110` `_prepare`, `:424`~~ | сделано: шаги `_fetchPart → _promote → stage` с локальным `report(phase)`; `fetch` транспорта пущен через общий `_open` |
| ~~10~~ | ~~27~~ | ~~`core/save_path_template.dart:117` `collapse`~~ | сделано: `_rootsBySpecificity` считается один раз — окружение в Dart читается снимком при старте и на ходу не меняется |
| ~~—~~ | ~~72~~ | ~~`input/input_scope.dart:188` `build`, `:118` `_move`~~ | сделано: один `NavActionIntent(action)` вместо пяти намерений с пятью `CallbackAction`, таблица клавиш — константой рядом. Клавиатура теперь и правда сводится к `NavAction`: нажатие клавиши идёт тем же путём, что и нажатие геймпада |
| ~~12~~ | ~~32~~ | ~~`bloc/library/library_bloc.dart:359` `_onGameRemoved`~~ | сделано: обложки и кадры убирает `CoverCache`, папку установки — `_deleteInstallDir`, и «своё ли это» проверяется по одному разу в каждом |
| 9 | 44 | `bloc/library/library_bloc.dart:306` `_onGameUpdated` | исчезает целиком (этап 1) |
| ~~10~~ | ~~68~~ | ~~`ui/library/portal_sparks.dart:136` `edgeAt`~~ | сделано иначе, чем задумано: не таблица сегментов, а половина контура и поворот — вторая половина повторяет первую, и расходиться им негде. `advance` отдал рождение искр в `_emit` |
| 11 | 29 | `ui/library/library_atmosphere.dart:89` `_tick` | общий шаг часов — один на три копии (этап 1, риски) |
| 4 | 53 | `ui/library/scan_folder_dialog.dart:246` | три ветки с одним стилем: сначала вычислить текст, потом один `Text`; тернарники ради побочного эффекта (`:206–212`) → `toggle` в блоке |

**Классы-комбайны** — сложность не функции, а файла:

- [x] `SaveManager` (756 строк + 384 в `part`). Обход «правило → путь»
  повторён четырежды (`save_manager.dart:159,271,436,506`). Выделить
  `EvsavePackage` (открыть, манифест, правила, разбор имён записей),
  `SaveCollector` (сбор файлов и `lastLocalChange`), `RuleMatcher`
  (`_matchLocalRule` + `previewTargets`), а `RestoreTransaction` сделать
  настоящим классом вместо расширения. Сам `SaveManager` остаётся фасадом
  под `store.guard`. **L**
  *Сделано.* Выделены `SaveCollector` (обход диска), `RuleMatcher`
  (сопоставление правил и предпросмотр целей — теперь буквально одним
  кодом), `EvsavePackage` (архив, манифест, правила, имена записей) и
  `RestoreTransaction` — уже не расширение, а класс со своим файлом:
  общего у него с менеджером оказалось только переводы, предел размера и
  переименование, и передать их параметром дешевле, чем держать четыре
  куска одного класса. Исключения уехали в свой файл — их бросают все.
  Менеджер 846 → 656 строк, у сборщика и сопоставления свои тесты, без
  пакетов и снимков.
- [x] `DtorrentEngine` с четырьмя `part`-файлами: состояние задачи — в
  шести полях (`started`, `pausedByUser`, `error`, `task`, `model`,
  `generation`), правится в шести местах, а `_ManagedDownload` зовёт
  обратно `engine._persist()` (`managed_download.dart:95`). Перечень
  состояний слота с методами-переходами и чистая `DownloadQueue` — это же
  и лечение «вечной ошибки» из этапа 1. **L**
  *Очередь выделена.* `DownloadQueue` (`services/download/`) держит порядок
  и раздачу слотов и проверяется на одних идентификаторах — ни сети, ни
  диска: десять тестов там, где прежде правила жили внутри подъёма задач
  и проверялись только живой загрузкой. «Занимает слот» и «ждёт слота»
  стали свойствами самой задачи (`isActive`, `isWaiting`).
  *Состояние слота стало перечислимой* `SlotState` с переходами
  (`markRunning`, `markPaused`, `markWaiting`, `markFailed`) вместо трёх
  булевых полей, правившихся в шести местах: «занимает слот» и «ждёт
  слота» больше не складываются из флагов, а состояния «запущена и
  сорвалась разом» не бывает вовсе. Обратный вызов `engine._persist()`
  ушёл: задаче от движка нужны были папка для файла раздачи и «запиши» —
  их и передаём, а движок целиком она больше не знает.
  Осталась разбивка самого движка по частям — это уже про работу с
  раздачами, и делать её вслепую, без живого торрента, не стоит.
  *Закрыто тем, что можно было вынести, не трогая раздач.* Перевод наших
  настроек на язык библиотеки — окно расписания с пределом скорости и
  конфиг прокси — стал чистыми функциями в `torrent_task_options.dart`.
  Прокси и прежде проверялся, а окно не видел ни один тест: какие
  пределы доезжали до задачи, узнать можно было только живой раздачей.
  Теперь проверено, что килобайты доезжают байтами, что во время игры
  действует свой предел, что один предел раздачи оставляет приём
  свободным и что вне окна задача не встаёт на паузу. Часть
  `engine_limits.dart` ушла: от неё осталось два коротких метода в самом
  движке — поставить или снять окно, остановить раздачу по рейтингу.
  Попутно вернулся на место осиротевший комментарий `verify`, прилипший
  к соседнему геттеру.
  **Две оставшиеся части — `EngineQueue` (подъём задачи) и
  `_EngineStore` (файл сессии) — остаются расширениями, и это решение.**
  Они делят с движком всё его состояние: карту загрузок, очередь,
  запись файла. Ровно для этого случая расширение и уместно (так
  записано в `CLAUDE.md` про менеджер сохранений, где было наоборот), а
  классы с полным доступом к движку были бы теми же расширениями, только
  с лишним полем.
- [x] `UpdateDownload` (466 строк): транспорт (`:326–465`), проверка и
  распаковка (`:223–319`) — три класса. `SteamShortcuts` (460): профили,
  запись списка, витрина, заголовки картинок. **M**
  *Разложены оба.* Обновление: `UpdateTransport` (докачка с диапазоном и
  переадресацией), `UpdateUnpack` (изолят, zip-slip, права, ссылки) и сам
  `UpdateDownload` — подготовка и сверка, 466 → 219 строк. Исключение
  `UpdateException` уехало в свой файл: бросают его все трое.
  Ярлыки Steam: `SteamProfiles` (учётные записи и `loginusers.vdf`),
  `SteamGrid` (четыре картинки витрины) и `imageSizeOf`
  (`image_size.dart` — разбор заголовков JPEG и PNG), 490 → 289 строк.
  Прежние имена видны как раньше — `SteamArtwork` и `SteamProfile`
  переэкспортированы, `gridNameFor` остался тонкой статикой.

**Дублирование:**

- [x] Шесть самодельных HTTP-GET (`steam_catalog.dart:204,423`,
  `update_check.dart:224`, `ludusavi_catalog.dart:154`,
  `update_download.dart:326,424`), выбор «прокси или напрямую» — дважды,
  прореживание хода загрузки — трижды → `HttpFetcher` и `ProgressThrottle`. **M**
  *Сделано на четырёх из шести.* `HttpFetch`
  (`services/system/http_fetch.dart`) взял простые GET — каталог Steam,
  обложки, проверку обновлений и базу путей; слова об отказе остались
  зовущему (`describeStatus`), потому что отказ каталога — про каталог, а
  обновления — про обновление. **Два запроса обновления оставлены
  своими намеренно**: у них диапазон с докачкой и переадресация,
  уносящая диапазон за собой, — та самая часть, ради которой загрузка по
  плохому каналу вообще заканчивается, и общего с «прочитать тело» у неё
  нет. Выбор «прокси или напрямую» — `catalogHttpClient` плюс
  `ProxySettings.forCatalogs`, прореживание — `ProgressThrottle`
  (`lib/core/`) на все три места.
- [x] `_match` и `_normalize` названий: `save_path_finder.dart:147,167` ≡
  `save_activity_watch.dart:227,245`, третья копия нормализации — в
  `release_name.dart:142`. Проверка манифеста — `save_manager.dart:422` ≡
  `:570`; сборка `ScannedGame` — `library_scanner.dart:198` ≡ `:264`. **S**
  *Копии сведены*: `FolderMatch` (`services/saves/folder_match.dart`) на
  двоих, `_checkedManifest` и `LibraryScanner._describe` — на два места
  каждый. **Нормализацию из `ReleaseName` не трогали, и это решение, а
  не недосмотр**: там знаки заменяются пробелом, здесь убираются, —
  `Half-Life` превращается в `half life` у одного и в `halflife` у
  другого, и слияние поменяло бы обоим ответы.
- [x] В тестируемости мешает `AppLog.instance`: сервисы зовут его напрямую
  (`update_download.dart:102`, `update_installer.dart:96,124,143,165`,
  `proxy_http_overrides.dart:48`), и запись в журнал не проверить без
  правки глобала (а `update_install_test.dart:544,688` ставит его и не
  возвращает). Передавать функцией, как уже передаётся `L Function()`. **S**
  *Все три сервиса берут `AppLog Function()` параметром*, `collectLog` —
  готовый журнал (метод статический, конструктора у него нет), а оба теста
  перестали трогать глобал. Блоки и `bloc_common` по-прежнему пишут в
  `AppLog.instance`: у них журнал один на приложение и подменять его
  незачем.

Длинные `build` (глубина дерева 9–11) лечатся этапом 3:
`rule_tile.dart:24` (83 строки), `sync_folder_card.dart:24` (87),
`log_card.dart:59` (84), `rule_dialog.dart:48` (78),
`library_page.dart:118` (78), `downloads_page.dart:45` (72).
~~`main()` — 105 строк при сложности 2~~: сборка блоков вынесена в
`AppServices.bootstrap` (`lib/app_services.dart`), и обе функции стали
короче шестидесяти строк.

*Храповик длины опустошён.* Из девяти записей осталась одна, и лечится
она не длиной: `DownloadsBloc._finalize` разошёлся на две развязки
(«скачанное не сошлось» и «игра готова» — у каждой своя запись в
библиотеку, своё сообщение и своё уведомление), `SaveManager._createSnapshot`
отдал обход правил `_collectByRules`, `WindowChrome.resizeZones` стал
таблицей из четырёх уголков вместо двенадцати выписанных
прямоугольников, `liquidSelectionPath` — перемычкой отдельной функцией,
`_SpeedChartPainter.paint` — тремя шагами (сетка, столбцы сети, линия
диска), у `FeaturedArt` три затемнения ушли в постоянные, а
`SyncFolderCard` разделилась по своим двум обликам: `SyncFolderPrompt`
(папки ещё нет) и `SyncFolderContents` (что в ней лежит). Последней
ушла `Game.copyWith` — вместе с разбором модели на значения (этап 6).
Список пуст.

### Этап 6. Модели (P3)

Цена одного нового поля сегодня: у `Game` — шесть мест правки, у
`AppSettings` — семь, у флага эффекта — десять. «Забыли поле» уже случилось
дважды: `SteamGame.merge` без кадров и слияние в `_onGameUpdated` без оценки.

- [x] `Game` — 27 полей в одном классе; `copyWith` на 63 строки. Разложить
  на значения по образцу уже существующего `GameRating`: `GameMetadata`
  (appid, обложки, кадры, описание, оценка, маркер попытки),
  `SaveDiscovery` (шаблоны базы, найденные пути, маркер), `DownloadLink`
  (источник, задача, infohash, размер, ошибка), `PlayStats`. На диске формат
  прежний: каждая часть читает свои ключи из общей карты, `toJson`
  разворачивает `...part.toJson()` — ни пакетов, ни миграции. И это не
  только про чтение кода: событие, правящее `DownloadLink`, физически не
  сможет затереть `GameMetadata`. **M**
  *Отложено, и вот почему.* Главный довод — «событие не затрёт чужую
  часть» — уже закрыт этапом 1: событий вида «вот вся игра» нет, каждое
  называет, что меняет, и применяется к игре, какая она в состоянии
  сейчас. Осталось чтение кода, а цена у него высокая: две с половиной
  сотни обращений к полям, и половина имён живёт ещё и на других моделях
  — `sizeBytes` у снимка и блоба, `infoHash` у задачи движка,
  `description` и `rating` у данных Steam. Механическая замена тут
  молча испортит соседа, а ручная — это работа на отдельный заход, и
  делать её надо целиком, а не попутно.
  *Сделано отдельным заходом по решению владельца.* Частей четыре:
  `GameDetails` (назван не `GameMetadata` — это имя уже носит ответ
  сборщика в сервисах), `SaveDiscovery`, `DownloadLink`, `PlayStats`.
  Два отступления от плана, оба по смыслу: **`lastError` и `sizeBytes`
  остались у игры**, а не в загрузке — ошибкой кончается не одна
  загрузка, а размер — свойство установленной игры. `GameSource`
  переехал в свой файл, и `game.dart` его переэкспортирует: ни один
  импорт не поменялся.
  Опасение про соседей с теми же именами решилось не осторожностью, а
  анализатором: поля сняли с `Game`, и он назвал ровно те 178 чтений,
  что шли от игры, — по строке и столбцу, — а `sizeBytes` снимка и
  `infoHash` задачи остались нетронутыми, потому что у них тип другой.
  Чтения переписаны по его списку, 69 записей (конструкторы и
  `copyWith`) — руками. Полей у игры стало пятнадцать, `copyWith` — 22
  строки, и последняя запись храповика длины ушла. Запись на диске
  прежняя, плоская; `model_roundtrip_test` теперь держит и это — что
  вложенных объектов в записи нет, — и чтение старых ключей
  (`downloadGid`, `appid` без маркера поиска).
  Разбор окупился сразу же в одном месте: правило «что из найденного
  в Steam брать» стало чистой функцией над одной частью
  (`_foundDetails`), и обработчик поиска вернулся под шестьдесят строк.
- [x] Эффекты в `AppSettings` — `enum LibraryEffect(jsonKey, defaultOn)` и
  `Set<LibraryEffect>` вместо тринадцати булевых полей, выписанных семь раз
  в `app_settings.dart` и трижды в `effect_preset.dart:35–85`: `copyWith`,
  `toJson`, `fromJson`, `props` и наборы становятся циклами, набор —
  литералом множества. Старые ключи читаются как раньше. Остальное — в
  группы `Appearance`, `StartupSettings`, `SaveAutomation`. **M**
  *Сделано ровно так*, с одной разницей: значения по умолчанию не флагом у
  каждого, а одним списком `LibraryEffect.shipped` — он же и есть набор
  «обычно», и две записи одного и того же однажды разошлись бы. Читают
  украшения через `isOn`, пишут через `withEffect`; ключи в файле прежние.
  Ушло около двухсот строк: `app_settings.dart` 407 → 309,
  `effect_preset.dart` 108 → 80, `effect_details.dart` 170 → 83, а список
  переключателей в настройках стал перебором самой перечислимой.
  **Группы `Appearance`, `StartupSettings`, `SaveAutomation` не заведены** —
  это отдельная работа, и у неё своя цена: каждая группа означает ещё один
  слой в `copyWith` и в чтении настроек из виджетов.
- [x] Равенство. У `Game`, `SaveProfile`, `SavePathRule`, `SaveSnapshot`,
  `SnapshotBlob`, `GamepadStatus`, `GamepadBinding` нет `==` (`Equatable`
  уже в зависимостях). Следствия: `LibraryState.props` сравнивает игры по
  ссылке, и `copyWith` с теми же значениями даёт «новое» состояние и
  перестройку страницы; `AppSettings ==` по полю раскладки геймпада — тоже
  по ссылке; `_recheckDevices` (`gamepad_service.dart:104`) раз в секунду
  кладёт равный `GamepadStatus`, и слушатели (`app_footer.dart:47`,
  `gamepad_settings.dart:97,273`) перестраиваются раз в секунду. **S**
  *Все семь стали `Equatable`*, `Game` заодно получил `const`-конструктор.
  Держит это `model_equality_test.dart`: правка, ничего не меняющая, не
  выглядит правкой, а равное состояние геймпада слушателей не будит.

- [x] Стражи вместо генерации кода: на каждую модель — «все поля не по
  умолчанию → `fromJson(toJson(x))` равно `x`» и «`copyWith()` без
  аргументов равно исходному»; у `SaveSnapshot.copyWith`
  (`save_snapshot.dart:54`) нечем сбросить `note`. **S**
  *`model_roundtrip_test.dart`* держит и то и другое для `Game`,
  `SaveSnapshot`, `SaveProfile` и `AppSettings` — каждое поле в нём стоит
  **не** в значении по умолчанию, иначе забытая строка в `toJson`
  прошла бы незамеченной. Заметку снимка теперь можно и стереть:
  `copyWith` различает «не трогать» и «снять».

- [x] Слои: `models/app_settings.dart:2,4` тянет `material` (ради
  `ThemeMode`) и `input`; `models/save_snapshot.dart:1` импортирует сервис. **S**
  *Flutter из моделей ушёл*: свой `AppThemeMode`, а перевод на язык
  `ThemeMode` — расширением в слое темы. `SnapshotBlob` переехал в модели,
  и снимок больше не зависит от хранилища (оно его переэкспортирует).
  **Раскладка геймпада осталась в `lib/input` намеренно** и записана в
  списке стража с причиной: перенести `GamepadBinding` в модели значило бы
  притащить туда `package:gamepads` — плагин с платформенным кодом, то
  есть ровно то, от чего этот слой и берегут.
- [x] `GamepadService`: `dispose` (`:253`) закрывает контроллеры раньше, чем
  гаснут таймеры асинхронного `stop()`, — возможен `add` в закрытый поток;
  при быстром «выкл → вкл» раскладки `start()` видит ещё не обнулённую
  подписку (`:73` против `:131`), и геймпад остаётся выключенным. Блок
  здесь не нужен: сервис — преобразователь потока, `NavAction` уже событие. **S**
  *`stop` гасит своё синхронно и только потом ждёт отписки*, а `handleEvent`
  отбрасывает событие, пришедшее после закрытия. Тест на «выкл → вкл»
  падает без правки; на закрытие посреди удержания — сторожит тишину, но
  воспроизвести падение им не удалось: таймер повтора успевает отмениться
  микрозадачей раньше, чем выстрелит. Блок здесь так и не понадобился.

### Этап 7. Тесты, документы, CI (P3)

- [x] `README.md`, раздел «Состояние» (`:426–454`), отстал от кода: блоков
  назван четыре (без `SavesBloc`), счёт событий устарел, а обещание «в
  экранах нет ни `bool _busy`, ни `try/catch`» не сходится с
  `about_card.dart`, `add_game_dialog.dart` и `log_card.dart`. После этапа 4
  обещание станет правдой; счёт блоков поправить сразу. **S**
  *Обещание стало правдой этапом 4* — названные карточки и диалог теперь
  ходят через свои блоки, — и README переписан под пять основных блоков,
  девять блоков экранов и около сотни событий.
- [x] `test/` — 98 файлов (около 940 тестов, 21,6 тыс. строк) одной плоской
  папкой. Разложить по слоям, зеркалом `lib/`: `test/bloc/`,
  `test/services/saves/`, `test/ui/library/` … — найти тест к файлу станет
  делом секунды. Стражи — в `test/guards/`. **S**, механически
  *Сделано, и не механически.* Разложить по импортам не вышло: половина
  файлов — виджет-тесты, которые заодно трогают модели, блоки и сервисы,
  и механика уносит их в `test/models/`. Положенный не туда тест хуже
  плоского списка: его будут искать там, где его нет. Поэтому сто
  тридцать четыре файла разложены по тому, **о чём** каждый, руками:
  `core`, `models`, `input`, `bloc`, `services/<область>`,
  `ui/<раздел>`, `guards` (туда же уехал `localization_test` — он такой
  же страж) и `tool` (что проверяет сам репозиторий: `ci.yml`,
  `CHANGELOG.md`, отчёт о покрытии). Спорные решены по смыслу и записаны
  в `CLAUDE.md`: `drop_import_test` — у виджетов, `bulk_report_test` — в
  сервисах рядом с переносом.
- [x] Общее тестов — в `test/support/`: `waitFor` со сборкой блоков и
  `tearDown` скопированы примерно в десять файлов (и с разными таймаутами —
  5 и 10 с), опрос «`runAsync` + `pump`» — в четыре, обвязка `MaterialApp`
  написана 42 раза. Нужны `BlocFixture`, `pumpUntil`, `hostWidget`. Пять
  блок-тестов создают `SavesBloc` без `saveRoots`
  (`save_conflict_test.dart:32`) — по умолчанию туда попадают настоящие
  «Документы». **M**
  *«Документы» закрыты и больше не откроются*: девять таких мест получили
  пустой список, а держит это страж `test/guards/test_hygiene_test.dart` —
  новый `SavesBloc` без `saveRoots` роняет прогон.
  *Ожидания сведены*: `waitForState` (девять копий с разными сроками — и у
  всех одна и та же ловушка, нынешнее состояние проверяется первым) и
  `waitUntil`/`pumpUntil` вместо отмеренных пауз. Минус полторы сотни
  строк. **`hostWidget` не заведён**: обвязка `MaterialApp` в тестах
  разная не случайно — где-то нужна тема, где-то локализация, где-то
  подменённый `MediaQuery`, — и сводить её надо, глядя на каждый случай,
  а не одной функцией с десятком флагов.
  *`hostWidget` заведён — ровно так, глядя на каждый случай.* Из сорока
  с лишним обвязок `MaterialApp` двенадцать оказались одинаковыми до
  запятой: тема, язык и `Scaffold` под виджетом. Их и свела
  `test/support/host_widget.dart` — с двумя параметрами, оба нужны
  настоящим тестам: схема (контраст меряют на обеих) и язык. Отбирал
  скрипт, и отбирал строго: брал только обвязку, где нет ничего сверх
  этих пяти аргументов, а `Scaffold` несёт одно `body`. Всё, что
  отличается по делу, — свой `MediaQuery`, `builder` рамки окна, пустая
  тема ради чистого снимка отрисовки, — осталось своим: флаг на каждый
  такой случай прятал бы то, ради чего тест окружение и завёл. Минус
  восемьдесят строк.
- [x] Кандидаты во флаки — положительная проверка после фиксированной
  паузы: `drop_import_test.dart:178,202,224,245` (300 мс),
  `cover_backdrop_test.dart:82`. `fake_async` уже транзитивен — перенести в
  dev-зависимости 💬. **S**
  *Четыре паузы в сбросе заменены ожиданием состояния*: предел остался, но
  только чтобы прогон не висел вечно. Отрицательная проверка («оба
  приёмника не сработали») ждёт секунду — вторая игра появилась бы следом
  за первой, — а «неподходящий файл» дожидается сообщения, а не пустоты.
  **Пауза в `cover_backdrop_test` оставлена**: там ждут не состояния, а
  расшифровки картинки, и условия, по которому видно, что она готова,
  снаружи нет. `fake_async` в зависимости не переносили — это 💬.
- [x] 237 `find.text('русская строка')` против 11 через `L`: любая правка
  формулировки в ARB ломает тесты. Искать по ключу локализации или по
  `ValueKey`. 52 названия тестов написаны по-английски (девять файлов,
  например `library_effects_test.dart`) — против правила проекта. **M**
  *Названия переведены все* — сорок семь тестов и три группы. По ключу
  теперь ищут 82 места из 193: столько строк в ARB лежат без подстановок и
  встречаются там по одному разу. Остальные — либо с подстановкой, либо
  вовсе не из ARB (названия игр, пути, числа): для них правка формулировки
  ничего не ломает.
- [x] `color_palette_test.dart:30–93` — детектор изменений: дублирует
  константы и формулу `hashCode % 360`; проверять свойства (устойчивость,
  попадание в якоря), а не числа. **S**
  *Списки значений убраны*: кольцо частиц проверяется тем, что оно
  замкнуто и водит, волны — тем, что у схем они разные и без повторов,
  обложка — устойчивостью и тем, что низ темнее верха, затемнения — тем,
  что и правда затемняют. Числа остались только у якорей света: они и есть
  предмет проверки.
- [x] Не покрыто вовсе: `main.dart` и `system_notification_service.dart`
  (их нет в `lcov`), `suggestions_dialog.dart` (0 %), `drop_overlay.dart`
  (4 %), `sync_folder_card.dart` (18 %), `engine_limits.dart` (13 %),
  `saves_bulk.dart` (51 %). `check_coverage.dart:5–31` считает только
  загруженные файлы — непокрытый файл не входит в знаменатель; считать
  отсутствующие как ноль. Пороги в коде — 78 / 79 / 89, в `CLAUDE.md`
  записаны устаревшие 74 / 77 / 88; запас по «ядру и сервисам» — 80,2 при
  пороге 79, меньше заявленных двух-трёх пунктов. **S**
  *Безмолвные файлы теперь называются поимённо*: `check_coverage` сверяет
  отчёт со списком файлов `lib` и валит прогон на новом, у которого не
  выполнялось ни строки. Пятеро известных перечислены с причиной — `main`
  и обёртка системных уведомлений в прогоне не живут, а бочка экспортов,
  перечислимая и таблица констант исполняемых строк не содержат вовсе.
  Пороги подняты следом за достигнутым: 81 / 81 / 90 при 84,0 / 84,4 /
  93,3, и `CLAUDE.md` с ними сверен.

- [x] Страж имён артефактов ищет `contains` по всему тексту `ci.yml`, а
  `-macos.zip` есть и в комментарии (`ci.yml:169`): переименуй файл в самой
  команде (`:179`) — тест пройдёт. Искать в строке команды. **S**
  *Строки-комментарии из текста выброшены*, и проверено прямо: переименуй
  архив в команде упаковки — страж падает, как и заведено.

CI:

- [x] `permissions: contents: read` на уровне `ci.yml` (сейчас права заданы
  только у `release`, `:318`) и `timeout-minutes` каждому заданию:
  зависший ввод-вывод в `testWidgets` — это до шести часов на трёх
  раннерах. **S**
- [x] Сборка идёт только на теге (`ci.yml:138–140`), а тесты не
  компилируют ни раннеры, ни CMake, ни шейдер `drops.frag`, ни
  `installer.iss`: сломанная сборка обнаружится в день релиза. Собирать по
  расписанию или при изменении `windows/`, `linux/`, `macos/`, `assets/`,
  `pubspec.*`. **S**
  *Выбрано расписание, раз в неделю.* Отбор по путям точнее, но требует
  своего задания с `git diff` между базой и головой — а ошибка в нём
  красит прогон на каждом пуше, и проверить её, не запустив, нельзя.
  Расписание — четыре строки, которым нечем сломаться, и неделя верхний
  предел того, сколько поломка проживёт незамеченной.
- [x] `flutter pub get --enforce-lockfile` (`:58,101`); экшены — по SHA, а
  не по тегу (`:28,50,125,332`; `subosito/flutter-action` сторонний и
  участвует в релизной сборке); `--test-randomize-ordering-seed`. **S**
  *Всё три сделано.* Экшены закреплены по SHA с версией в комментарии
  рядом — иначе через полгода никто не скажет, что за коммит там стоит.
  Порядок тестов перемешивается случайным зерном: связь между тестами
  иначе живёт годами и всплывает на чужой машине.
- [x] `build-linux` на `ubuntu-latest` (`:191`): смена образа молча поднимет
  минимальную glibc у пользователей — закрепить версию. `choco install
  innosetup` без версии (`:282`). **S**
  *Закреплены оба*: `ubuntu-22.04` у сборки Linux и `innosetup 6.7.1` у
  мастера установки Windows. Прогон и анализ остались на `ubuntu-latest` —
  у них на выходе ничего, что уезжает к людям.
- [x] `release` ставит весь Flutter ради одного `dart run` (`:326`), хотя
  `analyze` тот же скрипт уже запускал (`:81`) — передать `notes.md`
  артефактом. **S**
  *Передаёт.* Анализ пишет описание в `notes.md` и кладёт артефактом,
  выпуск его забирает — ни Flutter, ни самого репозитория ему больше не
  нужно. Запасной путь остался на случай пустого описания.

Локализация и `pubspec`:

- [x] Ни одного ICU-плюрала: «{count} файлов», «{count} устройства»
  (`app_ru.arb:835,853`), в английском — «1 files». Стража подстановок
  (`localization_test.dart:55`) при этом придётся научить ICU. У 536 ключей
  одно `description` — переводчику не хватает контекста. **M**
  *Плюралы заведены там, где число читается вместе с существительным*:
  у русского четыре формы, у английского две, и страж подстановок теперь
  видит `{count, plural, …}` наравне с `{name}`. Склонение проверяется
  прямо — «1 файл», «3 файла», «7 файлов». Где число идёт меткой
  («Файлы на месте: 3»), оставлено как есть: там склонять нечего.
  Два ключа обзоров остались без плюрала намеренно — им приходит уже
  отформатированная строка с разрядами, а не число.
  **`description` у ключей по-прежнему нет** — это отдельная работа, и
  делать её надо целиком, а не попутно.
- [x] `cupertino_icons` и `collection` не используются (ноль обращений в
  `lib`). В `pubspec.yaml` остались шаблонные английские комментарии
  (`:3–19`, `:124–153`), нет `flutter:` в `environment`. **S**
  *Обе убраны, комментарии шаблона тоже*, а `flutter: '>=3.47.0'` в
  `environment` теперь та же версия, что закреплена в CI: без неё сборка на
  старом Flutter падала бы разбором кода, а не понятным «нужен новее».
  Регистраторы плагинов не изменились — платформенного кода у убранных
  зависимостей нет.

---

## 5. Порядок

1. **Сначала то, что грозит данным и главной функции** (этап 1, верх
   списка): пустой шаблон правила, симлинки при обновлении на macOS,
   одинаковые метки правил, уборка хранилища снимков, потерянное обновление
   в `GameUpdated`. Каждое — малым коммитом с тестом, который без правки
   падает. Мелкие — `merge` без кадров, `StateError` в наборах эффектов,
   `chmod` — попутно: они на час каждое.
2. Этап 0 (стражи) — параллельно с первым: он ничего не ломает и сразу
   останавливает рост нарушений.
3. Этап 2 до этапа 3: выносить виджеты лучше уже с `context.text` и темами
   компонентов, иначе каждый файл придётся открывать дважды.
4. Этап 3 — по папкам, каждая папка отдельным коммитом, список стража худеет.
5. Этап 4: сначала намеренческие события (они в этапе 1), затем
   внедряемый движок в `DownloadsBloc` с тестами, потом блоки экранов,
   `GameRepository` последним.
6. Этапы 5–7 — по мере того, как рядом идёт другая работа; исключение —
   `permissions` и `timeout-minutes` в CI: это пять минут, сделать сразу.

После каждого шага — ворота из `CLAUDE.md`: `dart format`, `flutter
analyze`, `flutter test` (полный прогон и без `| tail`: он съедает код
возврата). Вынос виджета поведение не меняет и нового теста не требует;
новый блок — требует. При разборе обработчиков блоков помнить про
микрозадачи: условие «делать ли шаг» остаётся на месте вызова, а не
прячется в `async`-метод, — тесты читают состояние сразу после `waitFor`.

## 6. Чего в плане нет намеренно

- перевода `NavigationBloc` на Cubit — решено оставить блоком;
- блока для `GamepadService` и `InputScope`: сервис — преобразователь
  потока с мимолётным состоянием, `NavAction` уже событие, а `InputScope`
  живёт фокусом и `BuildContext`;
- смены формата `.evsave` и замены зависимостей;
- `bloc_test` и `mocktail`: тесты здесь интеграционные — связанные блоки,
  настоящий диск, ожидание предиката; `expect: [состояния]` привязал бы их
  к числу `emit` и к `Notice.seq`. Общий `BlocFixture` даст больше;
- генерации кода для моделей (`freezed`, `json_serializable`): стражи на
  `copyWith` и `toJson` дают то же без новой сборочной ступени; вернуться к
  вопросу, если моделей станет больше;
- платного DCM: те же правила (сложность, вложенность, «не возвращать
  виджеты из методов») закрываются своими стражами в духе уже существующих.
