# TODO — второй разбор: что сделано, выводы и план

Разбор сделан 2026-09-21 по состоянию `main` на коммите `a26acfa` — сразу
после тега `v0.37.0`. Первый разбор (2026-09-19, коммит `1b33588`)
закрыт: все его 108 пунктов отмечены закрытыми, и вопрос этого разбора —
**так ли это на деле и хорошо ли вышло**. Его решения с причинами — в
[`docs/decisions/`](docs/decisions/README.md), оценка — в
[`docs/reviews/2026-09-19.md`](docs/reviews/2026-09-19.md), а весь текст,
на который ниже ссылаются «архив, §3» и «этап 4 архива», — в истории:
`git show f64b706:TODO.md`.

Как проверялось. В этот раз ворота запускались, а не только читались:
формат, анализатор, `bloc lint`, сверка регистраторов, полный прогон с
покрытием, `tool/check_complexity.dart` и, для сверки с ним, измеритель
когнитивной сложности на настоящем AST. История — `git log` и `gh run
list`. Код читали пять параллельных рецензий по непересекающимся областям
(сохранения и ядро; загрузки, система и запуск; блоки и сборка приложения;
интерфейс и тема; тесты, стражи, CI и документы); поведение библиотеки
загрузок сверено по исходникам закреплённого форка. Главные находки
перепроверены вторым чтением, две — временным тестом вне репозитория.
Ничего, кроме этого файла, не менялось.

Как читать — так же, как первый разбор: **P0** — ошибка или риск для
данных человека, **P1** — редкий случай с тяжёлым исходом или обязательное
правило, **P2** — устройство, **P3** — гигиена; **S** — до пары часов,
**M** — до дня, **L** — несколько дней; 💬 — сначала issue (зависимость,
формат, имена файлов релиза). Новое — отметка уверенности:

- **[▶]** — подтверждено прогоном: временный тест упал ровно так, как
  описано в пункте;
- **[✔]** — перепроверено по коду вторым чтением (про библиотеку загрузок
  — по исходникам форка `dtorrent_task_v2`, коммит `2668afb`);
- **[○]** — найдено рецензией чтением кода, вторым чтением не проверялось.

По правилу проекта правка начинается с теста, который без неё падает, — для
**[○]** он же находку подтвердит или снимет. Не подтвердилась — пункт
вычёркивают со словами «не подтвердилось», а не молча.

---

## 1. Итог

**План выполнен по-настоящему, и код стал заметно лучше — но самое опасное
лежало не в плане.** Первый разбор смотрел, *как написан код*; этот
спрашивал, *что станет с файлами человека, когда что-то пойдёт не так*, — и
нашёл девять ошибок уровня P0, часть — в местах, которые первый назвал
образцовыми (`JsonStore`, раскладка сохранений, хранилище снимков). Сами эти
места сделаны хорошо; беда на их стыках — там, где карантин встречается с
уборкой, а «наша папка» оказывается чужой.

### Ворота — зелёные, и это проверено

| Что | Итог |
|---|---|
| `dart format` | 588 файлов, правок ноль |
| `flutter analyze` | замечаний нет, включая подсказки |
| `bloc lint lib` | 0 на 434 файла |
| регистраторы плагинов | расхождений нет |
| `flutter test`, случайный порядок | 1248 прошли, 16 пропущены по платформе, 69 с на Windows |
| покрытие | 84,0 / 84,7 / 92,7 при порогах 82 / 82 / 90 |
| храповики сложности, длины, вложенности | пусты |
| CI на `a26acfa` | зелёный на трёх системах; релиз 0.37.0 собирался во время разбора |

История: 151 коммит за три дня (53 + 87 + 11). Только тринадцать из них
трогают `lib/` без `test/`, и все они — выносы без смены поведения: правило
«правка с тестом» соблюдалось. Каждый из трёх тегов стоит на зелёном `main`,
а прогон тега повторяет всё на трёх системах. Красный `main` был
(`ca1356d`) — его поймал страж `_reportedNowhere` и починил следующий
коммит: страж сработал ровно как задуман. Оборотная сторона темпа — в
выводах (§2).

### Оценка

| Область | Было | Стало | Коротко |
|---|---|---|---|
| Состояние (Bloc) | 3.5 | 4.5 | События-намерения, зависимость в одну сторону, блоки экранов, движок параметром и 21 тест на него. Остались пути закрытия: снимок, очередь метаданных, второе «Закрыть» (этап C). |
| Тема | 3.5 | 4.5 | Роли текста, токены, схемы расходятся данными. Осталось: шкалы отступов нет, `HardwareSurfaceTheme` стал мешком ручек, роли расползаются через `copyWith` (этап F). |
| Структура виджетов | 2.5 | 4 | Правило держит страж, листья получили границы перестроения. Цена: 47 % параметров — пробросы, 79 % виджетов используются в одном месте, один регресс от выноса (этап F). |
| Когнитивная сложность | 4 | 4.5 | Храповики пусты, AST-измеритель согласен в пределах ±1–3. Но самодельный не видит конструкторы и фабрики, а потолок в 60 строк стал целью (этап E). |
| Сервисы | 4 | 4 | Разбор `SaveManager`, обновления и движка сделан хорошо, правки этапа 1 подтверждены. Оценку держат новые P0: обновление на Linux, трафик мимо SOCKS5, hybrid-торрент, пределы скорости (этап A). |
| Тесты и ворота CI | 4.5 | 4.5 | +300 тестов, зеркало `lib`, закреплённый CI. Но у стражей нет самопроверки, общий процент прячет файлы с нулём, собранное приложение до выпуска не запускается ни разу (этапы E, G). |
| Документация | 5 | 4.5 | По-прежнему редкого качества, но отстала от кода минимум в восьми местах, а на неё опираются все следующие правки (этап G). |
| Устойчивость данных к сбоям | — | 3 | Не оценивалась. К чужому файлу (`shortcuts.vdf`) приложение бережнее, чем к своим `snapshots.json` и `library.json` (этапы A, B). |
| Процесс выпуска | — | 3 | Не оценивался. Три релиза поверх переразложенного опасного кода без единого запуска собранного приложения; запись `CHANGELOG`, которой нет в коде (этап G). |

### Что сделано хорошо и трогать не надо

Перепроверено и подтверждено — рецензии пытались это опровергнуть:

- **правка игры намерением**: ни один обработчик `LibraryBloc` и `SavesBloc`
  больше не пишет значение, захваченное до `await` (`_edit`, двойной
  `_stillSameGame`, `_resolveStoredPaths`); `busyWhile` гасит занятость в
  `finally`; подписки между блоками заводятся одним синхронным проходом;
- **раскладка сохранений**: план целиком до первой записи, замена готовится
  рядом с целью, одна раскладка на оба источника, откат проверяется подменой
  `rename`; `RuleMatcher` один на предпросмотр и раскладку; путь из чужого
  манифеста никогда не становится целью; `readableFormats` — множество;
- **хранилище снимков в пределах одного процесса**: `guard` + эпоха +
  `_deletions`, размер засчитывается после удаления, гонка в тесте
  поставлена подменой обхода, а не таймером;
- **`JsonStore`**: очередь не застревает после сбоя, права на пустом файле,
  `chmod` проверяется, временный файл убирается;
- **распаковка обновления**: ссылки только вниз и без `..`, запись сквозь
  ссылку наружу невозможна, через границу изолята идут три строки;
- **прокси для `HttpClient`**: недоступный отказывает, SOCKS получает имя
  цели без утечки DNS; **ярлыки Steam**: байт в байт, отказ на незнакомом,
  копия перед записью; резервация запуска игры снимается в `finally`;
- `DownloadQueue` и `SlotState` как чистые классы, `TorrentFile._Scanner`,
  `WindowGeometry.sanitized`, `AppLog`, `FakeDownloadEngine` с одноразовым
  отказом, `waitForState`, `expectRatchet` с двусторонней проверкой;
- в интерфейсе: `DecorativeMotion`, дисциплина ресурсов (скрипт не нашёл ни
  одной утечки контроллеров и подписок в 28 `StatefulWidget`) и `mounted`
  после `await`, оболочка целиком на `select`, семантика плитки,
  `EvaporateAlpha` / `Layout` / `Motion`, пара `primary` / `primaryFill`;
- в CI: экшены по SHA, `--enforce-lockfile`, права по умолчанию `read`,
  релиз черновиком, `release_artifacts_test`, `NOTICE.md`, `dependabot`,
  версия только из тега, пороги покрытия на 2–3 пункта ниже достигнутого.

### Где «закрыто» не значит «сделано»

Галочка в архиве стоит, а по коду — иначе:

1. **`DownloadHistoryBloc`** (архив, §3 и этап 4). План требовал блок уровня
   приложения с историями по id задачи; сделано переименование Cubit → Bloc.
   Все три беды из §3 на месте: у одной задачи две истории
   (`task_card.dart:29` и `game_detail.dart:37` живут в `IndexedStack`
   одновременно), история умирает с виджетом, при замершей загрузке выборок
   нет (`download_history_bloc.dart:49–53`), `DateTime.now()` зашит (`:26,55`).
   Обработчик не исполняется **ни одним** тестом (строки 46–76, покрытие
   24 %). При этом `CHANGELOG` выпущенной 0.36.0 утверждает: «График
   скорости у задачи теперь один на приложение» — этого в коде нет и не было
   (`git log -S'TasksSampled'` находит только сам план). Пункты D1 и G2.
2. **`dialogWidth`** (архив, этап 2) отмечен сделанным — токена нет: ноль
   упоминаний в `lib`, ширин диалогов пять (460, 520, 540, 560 ×3). Пункт D3.
3. **`SettingsChanged` → правка-функция** (архив, этап 1): переведены два
   отправителя с системным диалогом, шестнадцать по-прежнему шлют снимок
   настроек. Пункт C8.
4. **«Синхронная работа `archive` на главном изоляте»**: у обновления и
   восстановления — да; импорт пакета по-прежнему льёт записи синхронно
   (`save_manager.dart:617–632`), выгрузка — тоже. Пункт D4.
5. **«Исключения не гаснут без журнала»**: остановка задачи
   (`managed_download.dart:233–239`), пропуск сверки суммы
   (`update_download.dart:205,218`) и `_dropBackups` гасят молча. Пункты
   C19, G3, B2.
6. **Оговорки под галочкой**: `UpdateBloc` не на уровне приложения,
   `description` у ключей ARB нет, группы настроек не заведены, измеритель не
   на `package:analyzer`, клавиша обоймы не сведена, а «на живой macOS не
   проверено» и «без живого торрента не стоит» так и остались словами — при
   трёх релизах. Открытых пунктов файл показывает ноль. Этап D и пункт G1.
7. **«Вынос виджета поведение не меняет и нового теста не требует»** (архив,
   §5). Один раз из двухсот пятидесяти — изменил: ключ плитки уехал внутрь
   `build`, и `findChildIndexCallback` сетки умер. Ни один страж этого не
   заметил, потому что стражи смотрят на форму. Пункт F1.

---

## 2. Выводы

1. **Галочка мерила «сделано по плану», а не «работает у человека».**
   Девять P0 этого разбора найдены не в пунктах плана, а мимо них. Первый
   разбор читал код по слоям и искал, что в нём не так; разбор *по сценарию
   сбоя* — «файл не прочитался», «запущено два экземпляра», «распаковали не
   туда», «закрыли посреди работы» — находит другое. Следующий раз начинать
   с него.
2. **К чужому файлу приложение бережнее, чем к своим.** Для `shortcuts.vdf`
   действует «не понял — не трогай», копия перед записью и отказ при
   запущенном Steam. Для своих `snapshots.json` и `library.json` непонятое
   приравнено к испорченному: карантин, пустое состояние, работа дальше — и
   уборка хранилища стирает содержимое снимков, о которых больше не знает.
   Папка установки на Linux считается «нашей» без проверки. Правило одно на
   всё: **чего не понял — не удаляй и не перезаписывай**.
3. **Обещания о приватности шире кода.** HTTP-часть сделана так, как
   записано в `CLAUDE.md`: «отказывает, а не пропускает». А DHT,
   UDP-трекеры и поиск метаданных по magnet идут напрямую при включённом
   SOCKS5 — при подписи в настройках «для приватности выбирайте SOCKS5».
   Пределы скорости показаны в настройках, а библиотека их не читает вовсе.
   Форк починил DHT — и тем самым сделал утечку настоящей, а `CLAUDE.md` всё
   ещё говорит, что DHT не поднимается.
4. **Стражи держат форму, поведение держат только тесты.** Дробление прошло
   всех стражей и сломало ключ сетки; из 193 виджетов с единственным
   пользователем 162 не названы ни в одном тесте. Страж стоит столько,
   сколько его измеритель: самодельный счётчик сложности не видит 46 тел
   функций (конструкторы со списком инициализации, именованные фабрики,
   `operator ==`), и два невидимых уже сверх порога — при пустом списке.
5. **Потолок стал целью.** Восемь функций ровно в 59–61 строку, 23 `build` в
   полосе 50–60, а на месте методов-виджетов выросли замыкания до 52 строк
   внутри `build` — их правило не запрещает. Храповик лечит рост, но не
   подсказывает, *чем* мерить: следующая ступень — пробросы параметров и
   замыкания, а не строки.
6. **Темп обогнал проверку.** Около 40 % прогонов `main` отменены
   `cancel-in-progress` — пуши шли каждые 5–10 минут при девяти минутах
   тестов на Windows; та же группа отменит и еженедельную сборку. `CHANGELOG`
   писался по плану, а не по диффу, — отсюда запись о правке, которой нет.
   Собранное приложение до выкладки не запускается ни разу: регистрацию
   плагинов, шейдер, трей и `AppShutdown` первым исполняет пользователь.
7. **`CLAUDE.md` — память проекта, и она начала врать.** «Пять блоков» при
   четырнадцати папках, `DtorrentEngine.torrentFromBytes`, которого нет,
   «повторный запуск гасится `isStarted`», который никто не читает, «журнал
   причин», которого нет в выпускной сборке. На этот файл опирается каждая
   следующая правка, в том числе сделанная не человеком, — ошибка в нём
   размножается. Нужен страж (E9).

---

## 3. План

### Этап A. Чужие файлы, данные человека, приватность (P0)

Каждый пункт — малым коммитом с тестом, который без правки падает. A1
заслуживает отдельного выпуска.

- [x] **A1. Обновление по нажатию на Linux удаляет папку, в которой лежит
  приложение, — со всем чужим, что в ней было.** **[✔]** Архив пакуется без
  корневой папки (`ci.yml:286–287`: `tar -C bundle .`), `README.md:102`
  советует «распаковать куда угодно», папкой приложения считается
  `dirname(executable)` (`update_install.dart:38–41`), а «можно ли обновить»
  проверяет только запись (`update_installer.dart:82–89`). Помощник делает
  `mv root backup`, `mv staged root`, запуск, `rm -rf backup`
  (`update_install.dart:171–189`). Сценарий: `cd ~/Downloads && tar xzf
  evaporate-*.tar.gz && ./evaporate` → «Обновить» → содержимое `~/Downloads`
  удалено. Вторая дверь туда же — `./x.run --prefix ~/apps` при
  существующей папке (`tool/package_run.sh:97–108`: `mv`, затем `rm -rf
  "$target.old"`). Лечение:
  1. паковать архив с корневой папкой `evaporate/` и класть в сборку
     файл-маркер установки (`.evaporate-install`);
  2. `canInstall` отказывает, если маркера нет **или** в папке есть записи
     верхнего уровня сверх известных (`evaporate`, `lib`, `data`, маркер);
     отказ приходит тем же сообщением «обновите тем способом, каким ставили»;
  3. помощник сам сверяет маркер перед `mv` и перед каждым `rm -rf` — это
     последний рубеж, и он не должен верить тому, кто его собрал;
  4. `.run` отказывает, если цель существует и маркера в ней нет;
  5. **уже установленные версии чинятся только извне**: замену выполняет
     помощник *старой* сборки, и новая сборка его не исправит. Архиву для
     обновления дать новый хвост имени (`Release.updateSuffix`,
     `update_check.dart:112–117`) 💬 — клиент, не нашедший своего файла,
     клавишу «Установить» просто не показывает (`about_actions.dart:64`), и
     человек ставит новую версию руками, уже с защитой.
  Тесты: папка с чужим файлом → `canInstall == false`; POSIX-тест помощника
  «чужой файл рядом переживает обновление»; `release_artifacts_test` —
  в архиве ровно одна корневая папка. **M**
  *Сделано:* все пять шагов. Архив пакует `tool/package_tarball.sh`, хвост
  — `-linux-x64.tar.gz`; прежний в релиз не кладётся, и это сторожит
  `release_artifacts_test`. Проверка «своей папки» в помощнике общая с
  macOS: бандл узнаётся по `Contents/MacOS`. `.run` переставляет прежнюю
  установку и без маркера, если в папке нет ничего сверх сборки, — иначе
  не обновить тех, кто ставил им раньше. POSIX-тесты помощника идут только
  на macOS и Linux; на Windows сценарии прогнаны руками в Git Bash. C17
  (проба записи в `dirname(root)`, проверка запуска) не делался.
- [x] **A2. Испорченный `snapshots.json` стоит человеку содержимого всех
  снимков, и молча.** **[▶]** Файл не разобрался → карантин переименованием
  → состояние пустое → сразу `persist()` пустого списка
  (`saves_bloc.dart:226–241`); нечитаемые записи пропускаются поштучно без
  журнала (`:247–267`); ключ `version` пишется, но не читается. Следующая
  же уборка получает список живых из состояния (`:307–311`,
  `save_manager.dart:649–652`) и удаляет всё, чего в нём нет
  (`snapshot_store.dart:232–277`). Временный тест: снимок → испортить файл →
  перезапуск → новый снимок и его удаление → **блобов было 1, стало 0**,
  карантинная копия списка цела и бесполезна; `Notice` не пришёл. Лечение:
  карантин или хоть одна пропущенная запись **запрещают уборку до конца
  сеанса** и приходят сообщением, как у библиотеки
  (`_storageRecoveryNotice`); нечитаемая запись сохраняется как есть и
  пишется обратно нетронутой; мусор не удаляется сразу, а уходит в
  `blobs/.trash` со сроком — второй рубеж на случай, которого мы не
  предусмотрели. Тест — тот же сценарий: старые блобы на месте. **M**
  *Сделано:* `_reportDamage` в `SavesBloc` запрещает уборку до конца сеанса
  и приходит сообщением; нечитаемые записи лежат в `_unread` и пишутся
  обратно как были. Корзина — не `blobs/.trash`, а `blobs-trash` рядом с
  хранилищем (обход хранилища туда не заходит), срок две недели;
  вынесенное возвращается само — `contains`, `extractTo`, повторный `put`.
  Строка журнала о вынесенном — «вынесла в корзину», об удалённом насовсем
  — прежняя «освободила».
- [x] **A3. Частично испорченная библиотека пропадает на втором запуске.**
  **[▶]** Одна нечитаемая запись → `quarantine()` **переименовывает**
  `library.json` (`json_store.dart:112–118`), уцелевшие игры уходят в
  состояние (`library_bloc.dart:274–290`), но не на диск: `persist` никто не
  зовёт, а `close()` пишет, только если висит таймер (`:434–441`). У
  сложившейся библиотеки правок на старте нет (`_queueMetadata` молчит).
  Временный тест: после первого запуска `library.json` не существует, на
  втором — игр ноль и сообщения нет. Существующий тест
  (`library_bloc_test.dart:182–209`) останавливается на первой загрузке.
  Лечение: после карантина — `persist()` сразу; тест дописать вторым
  запуском. **S**
  *Сделано:* уцелевшие игры пишутся сразу после карантина, до того как
  библиотека объявлена загруженной; тест дописан вторым запуском.
- [x] **A4. Второй экземпляр приложения ничем не остановлен.** **[✔]** На
  Linux стоит явный `G_APPLICATION_NON_UNIQUE`
  (`linux/runner/my_application.cc:140`), в `windows/runner/main.cpp` нет
  мьютекса. Сценарий будничный: автозапуск «свёрнутым в трей», окна не
  видно, человек щёлкает ярлык. Два процесса — это два движка над одними
  файлами, «последний записавший прав» для `library.json` и настроек, а
  главное — уборка процесса A удаляет содержимое снимков процесса B: `guard`
  и эпохи живут в памяти одного процесса. Лечение: замок на файле в
  `dataDir` первым шагом `main` (`RandomAccessFile.lock` — система снимает
  его сама при гибели процесса); второй экземпляр просит первый показать
  окно и выходит; на Linux — снять `NON_UNIQUE` и показывать окно по
  `activate`. Тест службы замка: второй держатель получает отказ, после
  закрытия первого — замок. **M**
  *Сделано:* `SingleInstance` — замок на `evaporate.lock` первым шагом
  `main`, просьба показать окно через петлевой сокет (порт в
  `evaporate.port`: заблокированный файл на Windows другому процессу не
  прочесть). `NON_UNIQUE` на Linux оставлен: единственность держит замок,
  одинаково на трёх системах, а окно второго экземпляра не мелькает —
  раннеры показывают его после первого кадра. Тест — настоящим вторым
  процессом, иначе на POSIX он прошёл бы и без замка.
- [x] **A5. Правило, развёрнутое в корень, принимается — и восстановление с
  очисткой заменяет «Документы» целиком.** **[✔]** `resolve` требует только
  абсолютности (`save_profile.dart:102–107`): проходят `{DOCUMENTS}`,
  `{HOME}`, `C:\`. Выбрал в диалоге правила «Документы» → раскладка с
  очисткой отодвигает папку, ставит на её место файлы снимка и удаляет
  отодвинутое (`restore_transaction.dart:311–336,367–376`); без очистки —
  копирует все «Документы» в заготовку; снимок — в хранилище.
  `SavePathGlobs.expand` идёт мимо `resolve` вовсе **[○]**
  (`save_path_globs.dart:25–33`). Лечение: отказывать правилу, развёрнутому
  в корень плейсхолдера, домашнюю папку, корень диска или их предка; диалог
  говорит об этом словами. Тест на каждый корень. **S**
  *Сделано:* `SavePathTemplate.isTooBroad` — корни плейсхолдеров, папка
  игры целиком (`{GAME}`), корень диска и предки любого из них; отказ в
  `SavePathRule.resolve`, то есть и для правил, заведённых раньше. Диалог
  правила гасит клавишу и объясняет (`pathTooBroad`). `SavePathGlobs`
  результат и так разворачивается через `resolve`, но широкое теперь
  отсеивается уже там: `{HOME}/*` совпадал с «Документами».
- [x] **A6. При включённом SOCKS5 часть трафика раздач идёт напрямую, с
  настоящим адресом.** **[✔]** Настройки обещают: «для приватности
  выбирайте SOCKS5», «SOCKS5 применяется и к обмену с пирами»
  (`app_ru.arb:1050,1057`). По форку: `MetadataDownloader` слова `proxy` не
  содержит — пиров заводит сам и сам поднимает DHT
  (`metadata_downloader.dart:95,333–335,414`), а приложение зовёт его без
  вариантов (`managed_download.dart:93–126`); `TorrentTask.start()` всегда
  делает `_dht.bootstrap()` и `announce` прямым UDP (`task.dart:450,
  1367–1372`). `udp://`-трекеры идут через `RawDatagramSocket`, а
  `ws(s)://` — через статический клиент `WebSocket`, запомнивший маршрут
  первого вызова **[○]**. Форк починил DHT — утечка стала настоящей. В духе
  уже принятого решения («отказывает, а не пропускает»):
  1. в приложении, **S**: при `proxy.isUsable` выбрасывать из `announces`
     схемы `udp`, `ws`, `wss` и не искать метаданные magnet по сети —
     отказывать словами («через прокси magnet не найти, добавьте
     `.torrent`»); чистая функция «сетевой план при прокси» под тестом;
  2. в форке 💬, **L**: прокси для `MetadataDownloader`, выключатели DHT,
     LSD, UPnP и holepunch; до тех пор — честная подпись в настройках о том,
     что именно идёт через прокси, а что нет.
  *Сделана часть 1 и честная подпись; пункт открыт до правки форка:* при
  `proxy.isUsable` задача получает модель без `udp`/`ws`/`wss`
  (`announcesFor`; исходная модель с полным списком остаётся для
  выгрузки), magnet при SOCKS5 в сеть не идёт и падает с
  `magnetNeedsTorrentBehindProxy` (`canFetchMetadata`; при HTTP-прокси
  пиры и так идут напрямую, и это в подписи к нему). Подпись к SOCKS5
  говорит про DHT. **Открыто:** выключатели DHT/LSD/UPnP и прокси для
  `MetadataDownloader` в форке 💬 **L**.
  - *Форк исправлен:* при `useForPeers` задача не поднимает DHT и LSD,
    отказывается от uTP и holepunch и не объявляется UDP/WS-трекерам;
    `MetadataDownloader` получил `proxyConfig` и ходит к пирам через прокси
    без DHT. Попутно найдено худшее: прокси, данный `newTask`, до
    `PeersManager` не доезжал вовсе — пиры при SOCKS5 шли напрямую всегда.
    Приложение: `canFetchMetadata(proxy, trackers)` пускает magnet через
    SOCKS5, если в нём есть HTTP-трекер, подпись к SOCKS5 переписана.
    Тесты: `proxy_privacy_test` форка, `torrent_task_options_test`,
    `dtorrent_engine_test`.
- [x] **A7. Проверку путей раздачи обходит v2/hybrid-торрент.** **[✔]**
  `_rejectUnsafePaths` смотрит только `model.files`
  (`torrent_source.dart:84–94`), а раскладку для v2 и hybrid библиотека
  строит по дереву (`download_file_manager.dart:178–188`), склеивая ключи
  как есть (`file_tree.dart:118–140`, ни одной проверки на `..`). Раздача с
  безобидным `files` и деревом `{"..": {"..": {"x.bat": …}}}` пишет вне
  папки игр — а приходит она от незнакомых людей. Комментарий «мимо этой
  проверки в движок не попадает ничего» неверен. Лечение: отклонять не-v1 —
  собственный комментарий на `:50–52` и так говорит, что с ними не
  работали, — либо обходить и `FileTreeHelper.extractFiles` с тем же
  `unsafePath` (путь и `symlinkPath`). Тест: bencode-hybrid с `..` в дереве
  → `UnsafeTorrentException`. **S**
  *Сделано:* проверка идёт и по дереву (`FileTreeHelper.extractFiles`,
  путь и `symlinkPath`), v2 не отклоняется. Попутно выяснилось, что
  hybrid движок признаёт только при `piece layers` в корне файла: без них
  раздача считается v2, дерево само разворачивается в `files` и ловилось
  и раньше — тест собирает настоящий hybrid.
- [x] **A8. Пределы скорости не действуют вовсе, а их установка снимает
  паузу.** **[✔]** Библиотека кладёт предел в `_scheduledMax*Rate` и нигде
  его не читает (по всему форку — только геттеры, `task.dart:942–949`).
  Зато `addScheduleWindow` → `_evaluateNow()` → `resumeTask()`
  (`scheduler.dart:58–62,117`). `applyLimits` проходит по всем задачам с
  живым `task`, включая поставленные на паузу
  (`dtorrent_engine.dart:246–255`): запустил игру при заданном «пределе во
  время игры» — приостановленные загрузки качают на полную, а интерфейс
  показывает «Пауза». То же после остановки по рейтингу (`:276–287`).
  Лечение, **S**: не ставить окно задачам, у которых слот не `running`, и
  после `_limitTask` возвращать паузу; тесту нужен шов — фабрика
  `TorrentTask` в конструкторе движка: сейчас взаимодействие с живой задачей
  не проверяет ни один тест. Настоящий предел — только правкой форка 💬,
  **L**; до неё убрать настройку или подписать её честно.
  *Сделано иначе, чем предлагалось; пункт открыт до правки форка:* раз
  окно не ограничивает ничего и только снимает паузу, движок его не
  ставит вовсе — `applyLimits` пределы только запоминает (рейтинг
  раздачи считается нами и действует), `speedLimitWindow` удалён вместе
  с тестами (лежит в истории). Шов для `TorrentTask` поэтому не
  понадобился; от возврата вызова сторожит текстовый тест в
  `dtorrent_engine_test`. Настройки: предупреждение
  `speedLimitsInactive` над полями — выбрано «подписать», а не прятать.
  **Открыто:** настоящий предел в форке 💬 **L**.
  - *Форк исправлен:* `BandwidthLimiter` — общий ограничитель с бронированием
    (приём — пауза сокета, отдача — отложенный кусок), `setBandwidthLimiters`
    у задачи. Движок держит два на всё приложение, `applyLimits` ставит им
    скорость — и идущим задачам тоже; предупреждение `speedLimitsInactive`
    убрано. Окно расписания по-прежнему не зовётся. Тесты:
    `bandwidth_limiter_test` форка, «пределы становятся скоростью общих
    ограничителей».
- [x] **A9. «Документы» и Saved Games на Windows ищутся не там, где их
  держит система.** **[✔]** `placeholders` собирает их как
  `%USERPROFILE%\Documents` и `\Saved Games`
  (`save_path_template.dart:51–61`), не спрашивая систему. При
  «Документах», унесённых в OneDrive (обычное дело на Windows 11), правило
  базы путей `{DOCUMENTS}/My Games/X` разворачивается в папку, которой нет:
  автоснимок молча отвечает «сейвов ещё нет», а восстановление с другого
  устройства кладёт файлы туда, куда игра не смотрит, и рапортует успех. На
  Linux то же с `~/Документы`. Это главная функция приложения, и молчит она
  у заметной доли людей. Лечение: узнавать папки у системы один раз на
  старте (`AppPaths.init` уже ходит в `path_provider`; для Saved Games —
  `WindowsKnownFolder`) и подавать их в `SavePathTemplate` параметром;
  переменные окружения остаются запасным путём. Ядро при этом плагин не
  импортирует. Тест: корни подменяются, `{DOCUMENTS}` берётся из них. **M**
  *Сделано:* `SystemFolders` (ядро) — «Документы» через `path_provider`
  (known folder на Windows, `xdg-user-dirs` на Linux), Saved Games — из
  `User Shell Folders` через `reg`, с раскрытием `%VAR%` и проверкой, что
  папка существует (вывод `reg` в кодовой странице консоли). Подаются в
  `SavePathTemplate.useSystemFolders` из `AppPaths.init`; кэш корней
  сбрасывается там же. `WindowsKnownFolder` не понадобился — это была бы
  новая зависимость. Живой OneDrive-машины в прогоне нет: проверено
  подменой корней.

### Этап B. Перенос сохранений: редкий сбой, тяжёлый исход (P1)

- [x] **B1. Резервная копия перед восстановлением теряется именно тогда,
  когда нужна, а откат не переживает собственный сбой.** **[✔]** Копия
  снимается до `commit`, но в библиотеку попадает только из отчёта об успехе
  (`save_manager.dart:413–425`); сорвался `commit` — снимок `preRestore`
  нигде не заведён, и его содержимое унесёт следующая уборка, хотя
  комментарий на `:327–329` называет его «единственным путём назад».
  `_rollback` — без единого `try` (`restore_transaction.dart:340–349`): на
  Windows свежезаписанное держит антивирус (те же errno 32/145), исключение
  вылетает из `catch`, остальные цели не откатываются, а исходная причина
  теряется. Лечение: отдавать копию вызывающему колбэком **до** `commit`
  (как `onSnapshot` у переноса); каждый шаг отката в своём `try`; в
  сообщении называть путь, где лежат прежние сейвы. Существующий тест «сбой
  замены второй цели» стоит с `backupCurrent: false`
  (`save_manager_test.dart:546–553`) — с `true` копия обязана дойти. **M**
  *Сделано:* `restoreSnapshot(onBackup:)` отдаёт копию до `commit`, и
  блок кладёт её в состояние и на диск сразу (`_keepBackup`);
  `RestoreReport.backup` убран — второго пути быть не должно. `_rollback`
  — `try` на каждую цель с записью в журнал; не вернувшиеся на место копии
  называются в сообщении (`saveRollbackStranded`), а исходная причина
  сохраняется. Тесты — в `save_manager_test`: копия с `backupCurrent:
  true` при сорванной замене и откат, которому мешает занятый файл.
- [x] **B2. Возврат застрявших сейвов ленив, сносит живую заготовку,
  выбирает копию по неверному признаку и молчит.** **[✔]**
  `recoverInterrupted` зовётся только перед снимком и восстановлением
  (`save_manager.dart:163,336`), а при выключенном `autoSnapshotOnLaunch`
  (он выключен по умолчанию) — не зовётся перед запуском вовсе: игра не
  видит сейвов, заводит новые, цель «цела», а прогресс навсегда в
  `.evaporate-old-*`. `_recoverTarget` удаляет **любую** заготовку
  `evaporate-new` (`restore_transaction.dart:94–100`), включая ту, что
  прямо сейчас наполняет параллельная раскладка: `guard` — счётчик, а не
  замок, и автоснимок занятость не проверяет. «Последнюю отодвинутую» ищут
  по `modified` (`:108–111`), который переименование не меняет. О
  находке — строка в журнале на каждом снимке. Лечение: звать возврат в
  `beforeLaunch` без условий и на старте по всем играм; очередь операций по
  игре в `SaveManager` (или множество живых токенов, которые возврат
  пропускает); время — в имя (`evaporate-old-<время>-<uuid>`); остаток рядом
  с живой целью — через `Notice`; `_dropBackups` — с журналом. Тесты:
  остаток на диске + `snapshotBeforeLaunch` с выключенным снимком → цель на
  месте; `RestoreSource` на `Completer` + возврат посреди → оба файла в
  цели. **M**
  *Сделано:* живые пути раскладки (`_live` в `RestoreTransaction`) вместо
  очереди по игре — очередь ждала бы сама себя: снимок перед
  восстановлением идёт через тот же менеджер. Время — в имени
  (`backupPathFor`, микросекунды с нулями слева); у копий прежних сборок
  его нет, и они считаются старше. `recoverInterrupted` зовётся в
  `snapshotBeforeLaunch` без условий и один раз после загрузки библиотеки
  (`_checkInterrupted` — мимо обработчика, чтобы не держать закрытие);
  копии рядом с целой целью приходят `Notice` (`noticeRestoreLeftovers`).
  `_dropBackups` пишет в журнал. Тесты — `restore_transaction_test`
  (возврат посреди раскладки на `Completer`, выбор по имени, остаток) и
  `saves_storage_test` (запуск без автоснимка, сообщение на старте).
- [x] **B3. Автоснимок после выхода из игры теряется, если лончер закрыть
  следом.** **[✔]** Снимок уходит в состояние, затем идёт выгрузка в папку
  синхронизации (секунды; `autoExportToSync` включён по умолчанию), затем
  ротация, и только потом `persist()` (`saves_snapshots.dart:66–80`).
  `SavesBloc.close()` обещает дописать отложенное, но `_persistTimer` не
  присваивается нигде — `pending` всегда `false` (`saves_bloc.dart:149,
  406–419`). «Вышел из игры и закрыл лончер» — самый обычный порядок
  действий. Рядом: в `_onSnapshotDeleted` и после `_prune` уборка идёт
  раньше записи списка (`saves_snapshots.dart:140–163,259–278`) — обрыв
  между ними оставляет в списке снимок без содержимого. Лечение: `persist()` сразу после `emit` со
  снимком и безусловно в `close()`; везде порядок «список → удаление →
  уборка». Тест: `SaveManager` с зависшей выгрузкой, `close()`, повторная
  загрузка — снимок на месте. **S**
  *Сделано:* при включённой выгрузке список пишется до неё; без выгрузки
  — следом, как прежде (лишний `await` между `emit` сдвинул бы сообщение
  об удаче, см. `library_bloc_test`). `close()` дописывает список, если он
  менялся с последней записи (`_unsaved` через `onChange`; писать всегда
  нельзя — в виджет-тестах ввод-вывод при закрытии не завершается, и
  прогон вис); `_persistTimer` удалён. В `_prune` и
  `_onSnapshotDeleted` порядок «список → удаление → уборка». Тест —
  `snapshot_rotation_test`: выгрузка висит, блок закрыт, повторная
  загрузка находит снимок.
- [x] **B4. Импорт чужого пакета не сверяет ни CRC, ни размер — проверка
  осталась только на пути старых архивов.** **[✔]** «Записать запись пакета
  в файл» существует в двух копиях: `ArchiveEntrySource.writeTo` сверяет
  (`evsave_package.dart:159–177`), `_importEntry` — нет
  (`save_manager.dart:617–632`), а чужие пакеты теперь ходят вторым путём:
  импорт → хранилище → раскладка, где сверяется одна длина. Папка
  синхронизации — ровно то место, где файлы бывают недоехавшими. Тесты
  zip-slip, CRC и позднего выхода из папки (`save_manager_test.dart:382–435,
  437–500,582–632`) ходят через `archivePath`, то есть мимо. Лечение:
  переливать через `writeTo(tmp)` — уйдёт и дубль; заодно в изолят (D4).
  Тест: пакет из теста CRC через `importPackage` → `SaveException`. **S**
  *Сделано:* `_importEntry` переливает через `ArchiveEntrySource.writeTo`
  — дубль ушёл. Тест `save_manager_test` «импорт пакета с неверной CRC
  отказывает». Перенос в изолят — отдельно, в D4.
- [x] **B5. `createdAt` в манифесте — местное время без смещения.** **[✔]**
  💬 `toIso8601String()` от `DateTime.now()` (`save_snapshot.dart:85,129`,
  `save_manager.dart:204`), читается как местное время **другого**
  устройства: снимок из UTC+3 в 10:00 на устройстве в UTC+0 считается
  снятым в 10:00Z, и защита «здесь новее» (`bulk_transfer.dart:221–225`,
  допуск две минуты) ошибается на три часа — в одну сторону затирает
  прогресс, в другую даёт ложные конфликты. Нечитаемая дата подменяется на
  «сейчас» (`save_manager.dart:535–537`): такой пакет всегда самый свежий.
  Лечение: писать `toUtc()` — `DateTime.parse` читает `Z`, старые сборки
  тоже, формат совместим, но это всё же формат: сначала issue. Нечитаемую
  дату считать эпохой. Тест: `DateTime.parse(toManifest()['createdAt']).isUtc`. **S**
  - *Сделано:* `SaveSnapshot.writeMoment` пишет UTC со смещением,
    `readMoment` читает в местное, нечитаемое — эпоха; тем же путём и
    `snapshots.json`. Версия формата не менялась: `Z` читают и старые
    сборки. `snapshot_moment_test`.
- [x] **B6. Блоб пишется без `flush` перед переименованием, а `_isKept`
  верит одному имени.** **[○]** Индекс при этом сбрасывается на диск
  (`json_store.dart:145–149`), содержимое — нет (`snapshot_store.dart:
  149–156,171–188`). После обрыва питания под верным хешем лежит пустой или
  обрезанный файл, и каждый следующий снимок неизменившегося сейва отвечает
  «уже лежит». Выясняется в день восстановления. Временные имена там же
  строятся по часам (`:164–166`) — у двух одновременных `guard` на Windows
  они могут совпасть; брать `uuid`, как в `RestoreTransaction`. Лечение:
  `flush` перед `rename`; нулевую длину считать отсутствием; блоб, сорвавший
  раскладку, удалять, чтобы следующий `put` его переписал. Тест: обрезать
  лежащий блоб → `put` того же → восстановление проходит. **S**
  *Подтвердилось; сделано:* `_syncToDisk` (`RandomAccessFile.flush`)
  перед переименованием; временные имена — `uuid`; пустой файл под хешем
  — отсутствие (`_isWhole` в `_isKept`, `contains`, `extractTo`), и новая
  запись ложится поверх него; `StoredBlobSource.writeTo` на
  неразвернувшемся содержимом зовёт `discard` и отказывает словами, а не
  сырым `FormatException`. Тесты — `snapshot_store_test`, группа
  «испорченное содержимое».
- [x] **B7. Выгрузка пакета пишет прямо в целевой файл.** **[○]**
  `encoder.create` обрезает файл до проверки блобов, при сбое идёт
  `target.delete()` (`save_manager.dart:259–300`); автовыгрузка пишет под
  одно имя — пропал один блоб, и из Dropbox исчез вчерашний рабочий пакет, а
  клиент синхронизации успел унести половину файла. `.part` кладутся в чужую
  папку (`:280`). Лечение: временное имя рядом и переименование при успехе.
  Тест: лежит старый пакет, `addToArchive` бросает — старый уцелел. **S**
  *Подтвердилось; сделано:* `_materialize` пишет в скрытое
  `.<имя>.<uuid>.part` рядом и переименовывает при успехе; распакованные
  блобы — в `<dataDir>/export-staging`, а не рядом с пакетом. Тесты:
  «сорванная выгрузка не трогает прежний пакет» (и чужого в папке не
  оставляет), «удачная выгрузка заменяет прежний пакет» — заодно
  проверено, что замена существующего файла переименованием проходит на
  Windows.
- [x] **B8. Метки и вложенные правила: дыры остались на применении.** **[○]**
  `_onSaveRulesAdded` отсеивает только по шаблону
  (`library_edits.dart:198–217`), а диалог правила держит профиль на момент
  открытия: пока он открыт, поиск по базе заводит «Сохранения», человек
  сохраняет предзаполненные «Сохранения» — две одинаковые метки, перенос
  отказывает обеим. Два правила пакета с одной меткой молча сливаются в одну
  цель (`save_manager.dart:441–459`). Вложенность ловится только при
  восстановлении (`restore_transaction.dart:202–213`): есть `…/X/Saves`,
  база добавляет `…/X` — снимки с дублями, а любое восстановление падает.
  Лечение: метки через `_freeLabel` при применении; два входящих в одно
  местное — оба в `unresolved`; пересечение развёрнутых путей проверять при
  добавлении правила и при снимке, с отдельным сообщением. **S**
  *Подтвердилось; сделано:* `SaveProfile.withRules` — единственное место
  применения правил (и библиотека, и `SavesBloc._addRules`): повтор
  шаблона и пересечение по развёрнутым путям пропускаются, занятая метка
  разводится `_freeLabel` с учётом платформы. `RuleMatcher.assign` — два
  правила пакета на одно здешнее не получают пары оба, и предпросмотр идёт
  тем же путём. Пересечение: в диалоге правила (`overlaps`,
  `pathOverlapsRule`) и при снимке (`saveRulesOverlap`).
- [x] **B9. Файл данных от более новой сборки считается испорченным.**
  **[✔]** `version != 1` → `FormatException` → карантин → пустая библиотека
  (`library_bloc.dart:263–269`); у снимков `version` не читается вовсе.
  Пока версия всегда 1, беды нет, но первый же откат на прошлую сборку после
  смены схемы — а откат у обновления по нажатию предусмотрен — даст «всё
  пропало». Лечение: «новее, чем умею» — отдельный исход: ничего не
  перезаписывать, уборку запретить, сказать словами. **S**
  *Сделано:* версия новее своей — отдельный исход у обоих файлов:
  `_frozen` в `LibraryBloc` и `SavesBloc` запрещает запись (и уборку у
  снимков), вместо карантина — `noticeLibraryNewer` /
  `noticeSnapshotsNewer`. Нечитаемая версия (не число, меньше единицы)
  по-прежнему считается порчей. Тесты — «…от сборки новее…» в
  `library_bloc_test` и `saves_storage_test`: файл после правок цел
  байт в байт, содержимое снимков на месте.
- [x] **B10. Снимок не хранит время изменения файлов.** **[○]** 💬 После
  любого восстановления всё датировано «сейчас» (`snapshot_blob.dart:7–27`,
  `snapshot_store.dart:214–222`; `setLastModified` в `lib` не встречается):
  `lastLocalChange` после раскладки равен «сейчас», и все пакеты старше
  получают ложное «здесь новее» — человек привыкает жать «перезаписать»; у
  игр, берущих «Продолжить» по mtime, сбивается порядок слотов. Лечение:
  mtime — в блоб и в запись zip (поле стандартное, манифест не меняется),
  ставить при раскладке. **M**
  - *Сделано иначе:* время — не в записи zip (там местное, без пояса и с
    точностью 2 с — та же беда, что в B5), а в манифесте, ключом
    `modified` (имя записи → миллисекунды эпохи), и в `SnapshotBlob`.
    Раскладка ставит его файлу (`_placed`), слияние сохраняет у
    нетронутых, загрузка пакета берёт его из манифеста. Ключ
    необязательный — старые сборки его пропускают. `file_times_test`.
- [x] **B11. Провал автоснимка по вводу-выводу не доходит системным
  уведомлением.** **[✔]** В ветке `on Object` нет `_notifySnapshotFailed`
  (`saves_snapshots.dart:81–91`), а `createSnapshot` отдаёт сырой
  `FileSystemException` — файл ещё держит игра. Тихий автоснимок
  проваливается тихо, вопреки правилу уведомлений из `CLAUDE.md`. **S**
  *Сделано:* обе ветки провала идут через `_snapshotFailed` —
  уведомление автоснимку, сообщение ручному. Тест —
  `notifications_test`, снимок падает `FileSystemException`.
- [x] **B12. Отчёт массовой загрузки молчит о нечитаемых пакетах.** **[○]**
  Нечитаемое уходит только в журнал (`save_manager.dart:663–669`): сборка
  новее пишет формат `/2` — отчёт здесь говорит «применено: 0» без единой
  ошибки. Возвращать пропущенное из `scanSyncFolder` и класть в отчёт как
  `failed`. Рядом: `isCompatible` (`:549`) не смотрит на сопоставление по
  метке и красит значком «нет путей» пакет, который ляжет. **S**
  *Подтвердилось; сделано:* `scanSyncFolder(onSkipped:)`, и `importAll`
  кладёт пропущенное провалом с причиной. Совместимость — по игре с тем же
  названием через `RuleMatcher.assign` (`SaveManager.fits`, считается в
  обработчике сканирования); без такой игры остаётся прежнее суждение по
  платформе правил.

### Этап C. Завершение, блоки, загрузки (P1)

- [x] **C1. Второе «Закрыть» во время завершения убивает процесс сразу.**
  **[✔]** `run()` на повторном вызове возвращается немедленно
  (`app_shutdown.dart:49–51`), и второй `quit()` тут же делает `_exit(0)`
  или `destroy()` (`managed_window.dart:126–133`) — посреди первого прохода.
  Окно при этом до восьми секунд видно и нажимается; `isStarted` в `lib` не
  читает никто. Лечение: `run() => _running ??= _run()`, окно прятать в
  начале `quit()`. Существующий тест проверяет только последовательные
  вызовы (`app_shutdown_test.dart:74–83`); нужен шаг на `Completer` и два
  `quit()` подряд. **S**
  *Сделано:* `run() => _running ??= _run()`, `quit()` прячет окно первым
  делом. Тест — шаг на `Completer` и два `quit()` подряд.
- [x] **C2. То, что падает мимо блоков, в журнал не попадает.** **[✔]** Ни
  `FlutterError.onError`, ни `PlatformDispatcher.instance.onError`, ни
  `runZonedGuarded` в `lib` нет; `AppShutdown` создаётся без `onError`
  (`main.dart:68–69`); `AppLog.flush` стоит вторым шагом, а не последним —
  комментарий обещает обратный порядок; `Timer(…, persist)` выбрасывает
  `Future`, и отказ записи `library.json` пропадает без следа
  (`library_bloc.dart:244–247`). А журнал — «единственный способ узнать, что
  произошло у человека». Лечение: оба перехватчика в `_startLog`, `onError`
  в журнал, `flush` — последним, `persist` с записью сбоя. **S**
  *Сделано:* `AppLog.captureUnhandled` (оба перехватчика; функцией в
  службе, чтобы проверить тестом) из `_startLog`; у `AppShutdown` —
  `onError` в журнал (`_handleClose`); `AppLog.flush` — последним шагом;
  отложенная запись библиотеки — `_persistLogged`. Тесты —
  `app_log_test` и `library_bloc_test` (хранилище, отказывающее в
  записи: без правки ошибка уходила необработанной и роняла тест).
- [x] **C3. «Журнала причин» в выпускной сборке нет.** **[✔]** `verbose =
  kDebugMode` (`logging_observer.dart:19`): главный довод `CLAUDE.md` за
  Bloc у человека не работает. `onTransition` к тому же молчит про события,
  не меняющие состояние, — паузу, отмену, перестановку. Лечение: писать
  `onEvent` всегда, кроме короткого списка частых (`EngineStatsChanged`,
  `SpeedSampled`); имена событий, не содержимое — пароль прокси в журнал не
  попадает и сейчас. **S**
  *Сделано:* `onEvent` пишет имя события всегда; частые помечены
  интерфейсом `FrequentEvent` у себя, а не списком в наблюдателе
  (`EngineTasksChanged`, `EngineStatsChanged`, `SpeedSampled`,
  `SavePathsProgressChanged`, `GameSelected`, `LibraryQueryChanged`,
  `ScanSessionChanged`). Переходы — по-прежнему только в отладке.
- [x] **C4. Закрытие посреди очереди метаданных метит «уже пробовали» игры,
  за которыми в Steam не ходили.** **[✔]** Маркер пишется до сети, а выход
  по `_closing` его не откатывает (`library_metadata.dart:47–63`, зеркально
  `:421–449`); проверки `_closing` на входе нет, поэтому `close()` сливает
  буфер `asyncExpand` по событию, и каждое успевает поставить маркер и
  записать файл. Первый запуск, сорок игр, закрыл окно — часть осталась без
  обложек до ручной клавиши; шаг `library.close` всякий раз упирается в
  трёхсекундный предел. Лечение: `_closing` — первой проверкой; при выходе
  по нему после сети маркер возвращать. Тест: сборщик на `Completer`, три
  игры, `close()` — у второй и третьей маркер `false`. Там же осиротевший
  комментарий: описание `_applyMetadata` приклеилось к `_foundDetails`
  (`:79–89`). **S**
  *Сделано:* `_closing` — первой проверкой у обоих поисков (Steam и пути
  сохранений); выход по нему после сети возвращает маркер и пишет сразу
  (`_unmarkAttempt`). Комментарий `_applyMetadata` вернулся на место.
  Тест — сборщик на `Completer`, три игры, `close()`: у всех трёх маркер
  `false`.
- [x] **C5. Удаление игры не снимает её задачу в движке.** **[✔]** На
  `gameRemovals` подписан только `SavesBloc`; убрал из библиотеки качающуюся
  или раздающуюся игру — задача живёт дальше, переживает перезапуск, а
  клавиш у карточки нет: они рисуются только при игре
  (`task_header.dart:41–46`). Снять можно, лишь заведя ту же раздачу заново.
  Лечение: публиковать в `gameRemovals` связь с загрузкой, `DownloadsBloc`
  подписывается и зовёт `engine.remove`; удаление файлов — под `try` с
  `Notice`. Тест на `FakeDownloadEngine`: после `GameRemoved` в поручениях
  есть `remove`. **S–M**
  *Сделано:* `gameRemovals` несёт игру, а не id (загрузкам нужен её
  идентификатор задачи, а в состоянии её уже нет); `DownloadsBloc`
  подписан и зовёт `engine.remove`, отказ — в журнал. Удаление файлов в
  библиотеке — под `try` с `Notice`. Тест — `downloads_bloc_test`.
- [x] **C6. Игра, запущенная в момент закрытия лончера, теряет время сеанса
  и автоснимок.** **[○]** `dispose()` гасит колбэк выхода
  (`game_launcher.dart:156–164`), `elapsedFor` никто не записывает
  (`library_bloc.dart:433–448`). Записывать наигранное при закрытии. **S**
  *Сделано частично — время:* `close()` засчитывает `elapsedFor` идущих
  игр (правило «меньше минуты не в счёт» — как при выходе) и пишет файл
  сразу. Автоснимок при закрытии не снимается намеренно: игра ещё пишет
  сейвы, и снимок застал бы их на середине; выход её после закрытия
  лончера по-прежнему никто не видит.
- [x] **C7. `_onTasksChanged` держит снимок списка игр через `await
  _finalize`.** **[○]** Пока финализируется игра A (обход папки — секунды),
  игра B в соседнем заходе сорвалась: цикл идёт со старой `game` — второе
  уведомление о той же ошибке, повторный `_finalize`, `_relinkByInfoHash` по
  устаревшему списку (`downloads_bloc.dart:385–395,454,490`). У `_finalize`
  есть `finally`, но нет `catch`: при `FileSystemException` он повторяется
  каждую секунду. `library.persist()` сразу после `library.add(…)`
  (`:526–533,560–569`) пишет состояние **до** события. Лечение: финализация
  — отдельным событием по id, игра читается заново; ошибка →
  `GameDownloadRejected`. **M**
  *Подтвердилось; сделано:* финализация — событием
  `DownloadFinalizeRequested` по id, игра и задача читаются заново,
  `_finalizing` ставится при заявке; опрос движка стал синхронным и
  снимка через `await` больше не держит. Сбой проверки →
  `GameDownloadRejected` с причиной. `library.persist()` после `add`
  убран. Тест — «сорвавшаяся проверка приходит отказом, а не повтором».
- [x] **C8. `SettingsChanged` снимком остался у шестнадцати отправителей.**
  **[✔]** Включил автозапуск и тут же сменил режим окна — второй снимок
  несёт старый `launchAtStartup`, и обработчик честно выключает автозапуск в
  системе обратно (`window_startup_card.dart:28–37`). Окно узкое — время
  очереди записи плюс `reg.exe`, — но корень тот же, что у `GameUpdated`.
  Перевести всех на `SettingsPatched` и убрать событие-снимок. **S**
  *Сделано:* все отправители (и в приложении, и в тестах) шлют
  `SettingsPatched`; `SettingsChanged` удалён — второго способа записать
  настройки нет, объяснение — в `settings_event.dart`.
- [x] **C9. Два `SteamShortcutRequested` разом переписывают чужой
  `shortcuts.vdf` друг поверх друга.** **[○]** Обработчик параллельный и
  без проверки занятости (`library_metadata.dart:189–221`), временный файл
  один на всех. Очередь через `asyncExpand` и `isBusy` на входе. **S**
  *Подтвердилось; сделано:* `asyncExpand` у `SteamShortcutRequested` и
  `isBusy` на входе. Тест — запись ярлыков, считающая одновременные
  вызовы: без правки их было два.
- [x] **C10. Готовая раздающаяся задача держит слот очереди.** **[○]** По
  умолчанию слотов три, рейтинг — «раздавать вечно»: три скачанные игры, и
  четвёртая ждёт всегда, а строка состояния показывает «0 / 3» — интерфейс
  готовые задачи слотом не считает (`downloads_state.dart:19–29`), движок
  считает (`download_queue.dart:54–66`). Лечение: состояние `seeding`, не
  занимающее слот. Тест очереди: `isActive` ложно для готовой. **M**
  *Подтвердилось; сделано:* `SlotState.seeding`; переход решает чистая
  `DtorrentEngine.slotAfter` (running + complete → seeding), опрос движка
  после перехода сразу отдаёт слот следующей. Рейтинг раздачи смотрит на
  `isSeeding`. Тест — на `slotAfter`: живую задачу подставить нечем.
- [x] **C11. Magnet без пиров по-прежнему вечно «получает метаданные».**
  **[✔]** `MetaDataDownloadFailed` библиотека шлёт только после трёх
  несовпадений хеша; таймаута нет ни у неё, ни в `fetchMetadata()`
  (`managed_download.dart:93–126`) — тест сам описывает этот случай
  функцией `never` (`dtorrent_engine_test.dart:204–206`). В `CHANGELOG`
  0.35.0 это заявлено исправленным. Лечение: таймаут вокруг `fetch` →
  `_fail(metadataNotFound)`. Тест: `fakeAsync` + `never` → `error`, слот
  ушёл следующей. **S**
  *Сделано:* `metadataTimeout` у движка (по умолчанию 10 минут) вокруг
  получения метаданных; не дождались — `null`, и `_launch` срывает задачу
  с `metadataNotFound`. Тест — `never` и предел 50 мс, слот уходит
  следующей: без `fakeAsync`, предел — параметр.
- [x] **C12. Проверка целостности считает пропавшими файлы, которых на
  диске и не должно быть.** **[○]** Заполнители BEP 47 и
  `_____padding_file_N_____` входят в `model.files`, библиотека держит их
  виртуальными: раздача скачана целиком → `missing` → игра в ошибке
  навсегда (`dtorrent_engine.dart:458–463`). Пропускать `isPaddingFile` и
  записи с `symlinkPath`. **S**
  *Сделано:* `DtorrentEngine.expectedOnDisk` пропускает `isPaddingFile` и
  записи со `symlinkPath`; тест на заполнитель по имени, по атрибуту и
  ссылку.
- [x] **C13. «Отменить с удалением файлов» оставляет `<infohash>.bt.state`,
  и повторная загрузка верит старой карте кусков.** **[○]** Скачано 40 % →
  отмена с удалением → та же раздача снова: эти куски не запрашиваются,
  частично затронутые файлы создаются нулями, и `IntegrityCheck` их
  пропускает. Удалять `*.bt.state` и `*.bt.paths.json` в `remove()`. **S**
  *Сделано иначе:* не в `remove()` движка, а в
  `DownloadsBloc.deleteDownloaded` — там, где удаляют сами файлы: отмена
  без удаления файлов пусть по-прежнему позволяет докачать. Удаляются
  `<infohash>.bt.state` и `.bt.paths.json` в папке задачи; тест —
  `download_delete_files_test`.
- [x] **C14. Каждый старт задачи перечитывает всю папку игр по разу на
  каждый ещё не созданный файл.** **[○]** 💬 `detectMovedFiles()` форка
  зовёт `dir.list(recursive: true)` от общей папки на каждый отсутствующий
  файл: свежая раздача в N файлов — N обходов библиотеки до первого байта.
  На macOS и Linux хуже: файл привязывается к одноимённому файлу того же
  размера у **другой** игры (`steam_appid.txt`, redist). Правка форка:
  поиск перемещённых — опцией, выключенной для свежего состояния. **M**
  - *Форк исправлен:* при старте поиск перемещённых идёт только у раздачи
    с прогрессом, одним обходом на все файлы, по точному имени, и
    неоднозначное (два кандидата одного имени и размера) не привязывает.
    Три теста в `file_moving_test` форка падают без правки.
- [x] **C15. Перетаскивание вниз по очереди промахивается на позицию.**
  **[✔]** `[a,b,c]`, «a перед c» → `orderIndexBefore` отдаёт 2
  (`downloads_state.dart:52–56`) → `moveTo(a, 2)` → `[b,c,a]`
  (`download_queue.dart:33–42`). Все тесты двигают только вверх, а подделка
  движка записывает номер, не применяя его. Вычитать единицу, когда задача
  стоит выше соседа; подделка применяет перестановку. **S**
  *Сделано:* `orderIndexBefore(beforeId, moving:)` вычитает единицу, когда
  задача стоит выше соседа. Тест прогоняет посчитанный номер через
  настоящую `DownloadQueue.moveTo` — вниз и вверх; подделку движка
  трогать не понадобилось.
- [x] **C16. У `HttpFetch` и `UpdateTransport` нет предела на чтение
  ответа.** **[○]** Сеть пропала посреди ответа — `await for` не кончается
  (`http_fetch.dart:47–75`, `steam_catalog.dart:404–410`,
  `update_transport.dart:83–87`): очередь `asyncExpand` стоит до
  перезапуска, у обновления `installing` остаётся `true`. Общий таймаут у
  `HttpFetch`, простойный — у загрузки, `limitBytes` для JSON. Тест:
  петлевой сервер, замолчавший после заголовков. **S**
  *Сделано:* простойный предел `HttpFetch.idleTimeout` (30 с) на ожидание
  заголовков и каждого куска тела — им пользуются каталог Steam, база путей
  и проверка обновлений; тот же предел — у загрузки обновления
  (`UpdateTransport`, и на переадресациях). Общий предел не ставился
  нарочно: манифест путей по медленному каналу честно качается минутами.
  `limitBytes` у JSON уже был. Тест — петлевой сервер, замолчавший после
  заголовков.
- [x] **C17. Откат POSIX-помощника не покрывает незапустившуюся версию, а
  проба «можно ли обновить» смотрит не ту папку.** **[○]** `"$launch" &` — и
  сразу `rm -rf "$backup"`; бит запуска после распаковки ставится без
  проверки результата (`update_unpack.dart:111–113`). Проба пишет внутрь
  `root`, а `mv` требует прав на родителя: `/opt/evaporate` принадлежит
  пользователю → приложение закрылось, шесть секунд неудач, стартовала
  прежняя версия, в интерфейсе тишина. После запуска ждать и проверять
  `kill -0 $!`; пробу писать в `dirname(root)`. Делать вместе с A1. **S**
  *Сделано:* помощник ждёт `@SETTLE@` (5 с) и проверяет `kill -0` у
  запущенной; упавшую отодвигает в `.evaporate-broken`, возвращает
  прежнюю, запускает её и убирает сломанную (через `ours`). Проба записи —
  в `dirname(root)`, и папка обязана существовать. `chmod +x` при
  распаковке проверяется — не встал бит, обновление отказывает до замены.
  POSIX-тест «упавшая новая версия меняется обратно» идёт на macOS и
  Linux; на Windows сценарии прогнаны руками в Git Bash.
- [x] **C18. `ProxyHttpOverrides.apply`: гонка на время разрешения имени,
  отказ DNS на старте не лечится, обновление идёт мимо прокси.** **[○]**
  Смена A→B: запрос в окне уходит на `A.ip:B.port` с учётными данными B; два
  `apply` завершаются в порядке ответов DNS (`proxy_http_overrides.dart:
  87–95`). Автозапуск до появления сети → `blocked` до правки настроек.
  Проверка и загрузка обновления берут прямой клиент
  (`update_check.dart:231`, `update_transport.dart:27,128`) — вопреки
  комментарию «обновление спрашивает свои флаги». Лечение: оба поля разом
  под счётчиком поколения; повтор разрешения при `blocked`; про обновление
  — решить и записать. **M**
  *Сделано:* пара «настройки + адрес» ставится разом после DNS, поздний
  ответ отбрасывается поколением; при `blocked` — повтор разрешения через
  `retryDelay` (30 с), останавливается шагом завершения. Обновление решено
  пускать через прокси (`updateHttpClient`), решение записано в
  `CLAUDE.md` и у функции. Тесты — поздний ответ DNS и поднявшийся
  заблокированный прокси.
- [x] **C19. Остановка задач идёт по одной и гасит ошибки молча.** **[○]**
  Один `try` на три шага: сбой `metadata.stop()` отменяет `task.stop()`;
  `catch` без журнала (`managed_download.dart:233–239`). `task.stop()` ждёт
  объявление трекеру без таймаута — при пределе шага в три секунды до второй
  задачи на выходе дело может не дойти. `Future.wait` с `try` на шаг; на
  выходе и при смене прокси — `stop(true)`. **S**
  *Сделано, без теста:* три шага остановки — каждый в своём `try` с
  пределом 2 с и записью в журнал; `stop()` движка и перезапуск при
  смене прокси останавливают задачи через `Future.wait`. `stop(true)` не
  передать: интерфейс `TorrentTask.stop()` его не объявляет. Живую задачу
  тесту подставить нечем — тот же шов, что в A8 (фабрика `TorrentTask`).
- [x] **C20. Поиск игр.** **[○]** macOS: `.app` в корне обхода разбирается
  как обычная папка и предлагается игрой «Contents» с поставленной галочкой
  (`library_scanner.dart:101–123,147–176`) — папку `*.app` сразу считать
  игрой. Linux: `~/.steam/steam` — обычно ссылка, `resolveSymbolicLinks`
  нет, и каждая игра Steam находится дважды (`steam_install.dart:63–68`).
  После `await inspect` нет проверки поколения — отменённый заход дописывает
  игру в список следующего (`scan_session.dart:162–174`). **S**
  *Сделано:* `.app` — игра сразу (`_classify`), название без расширения;
  корни и библиотеки Steam — по настоящему пути (`resolveSymbolicLinks`,
  и для тех, что пришли из `libraryfolders.vdf`); после `await inspect` —
  проверка поколения. Тесты — бандл и корень-ссылка (последний на Windows
  пропускается); проверку поколения тестом не закрыл: она на пути записей
  реестра, который есть только на Windows.

### Этап D. Недоделанное под галочками

- [x] **D1. История скоростей — блоком уровня приложения**, как и было
  записано в §3 архива: события из потока движка, истории по id задачи,
  подменяемые часы, выборка и при замершей загрузке. Тесты на обработчик,
  которых сейчас нет ни одного. Запись в `CHANGELOG` 0.36.0 станет правдой
  задним числом — до тех пор см. G2. **M**
  *Сделано:* `DownloadHistoryBloc` на уровне приложения (`main.dart`,
  `TestHarness`), состояние — `DownloadHistories` по id задачи, событие
  `TasksSampled` из `downloads.stream`, часы подменяемые, выборка на
  каждом снимке; `DownloadHistoryScope` удалён, график и показания
  выбирают историю по `task.id`. Тесты обработчика —
  `download_history_test` (диск по приросту, замершая, снятая, пауза,
  предел в минуту). Запись `CHANGELOG` 0.36.0 стала правдой — G2 это
  упрощает.
- [x] **D2. `UpdateBloc` — в `AppServices.bootstrap`.** Найденное на старте
  уходит только в уведомление, карточка пуста до нажатия, а при выключенных
  уведомлениях стартовый запрос теряется целиком (`main.dart:105–112,
  322–342`). Цена мала: блок и так живёт всю жизнь приложения. Уведомление —
  по переходу в `found`. **S**
  *Сделано:* `UpdateBloc` заводится в `AppServices.bootstrap` и раздаётся
  через `BlocProvider.value`; стартовая проверка — событием того же блока,
  если включена в настройках; `AboutCard` берёт блок из контекста.
  Уведомление — `announceFoundReleases` (`lib/bloc/update/`) по переходу
  в `found` через `distinct`; тесты — `update_announcer_test` (один раз
  на версию, без найденного молчит, выключенное молчит).
- [x] **D3. `dialogWidth` — завести на деле**: пять ширин (460, 520, 540,
  560 ×3) → токен в `EvaporateLayout`, число — в стража. **S**
  *Сделано:* `EvaporateLayout.dialogWidth` (560) и `dialogWidthNarrow`
  (460, выбор игры из списка); добавление игры (520) и правило сохранений
  (540) выровнены по остальным. Страж раскладки ловит `width: 460|560`.
- [x] **D4. Архив — с главного изолята везде**: импорт пакета
  (`save_manager.dart:617–632`, вместе с B4) и выгрузка; хеш и gzip в `put`
  тоже считаются на главном. **M**
  *Сделано:* `Offload` (`lib/services/saves/offload.dart`, по умолчанию
  `Isolate.run`). `SnapshotStore.put` хеширует и сжимает в изоляте файлы от
  `offloadFromBytes` (1 МиБ), мелкие — на месте; `extractTo` так же.
  Выгрузка собирает пакет целиком в изоляте (`EvsaveJobs.write`), загрузка
  разбирает его там же (`EvsaveJobs.unpack`, со сверкой CRC через общий
  `writeVerified`); отказ — `UnreadableEntry`, слова подбирает менеджер.
  Подмена `addToArchive` убрана: сбой посреди выгрузки тесты ставят
  испорченным содержимым в хранилище. По ходу: выгрузка сверяет длину
  развёрнутого — обрезанный gzip разжимается молча. Тесты — `offload_test`.
  Раскладка снимка со своим архивом разжимает крупные записи там же
  (`EvsaveJobs.extractOne`: пакет открывается в изоляте заново по пути).
- [x] **D5. Решить и записать** то, что висит оговоркой: группы настроек
  (`Appearance`, `StartupSettings`, `SaveAutomation`) — снять с плана или
  делать; клавиша обоймы без моно 💬; `description` у ключей ARB — **M**,
  делать целиком или снять. Оговорка под галочкой — худший из статусов: её
  не видно.
  *Решено и сделано:*
  - **Группы настроек — сделаны.** `Appearance`, `StartupSettings`,
    `SaveAutomation` (`lib/models/`) — значения внутри `AppSettings`,
    правка через `withAppearance` / `withStartup` / `withSaves`; на диске
    ключи прежние, плоско (`model_roundtrip_test`: «запись настроек
    осталась плоской»). Набор украшений (`EffectPreset`) теперь над
    обликом, сетка библиотеки выбирает облик, а не всё состояние.
  - **Клавиша обоймы — на `label`**, исключение из стража кеглей снято.
  - **`description` у ключей ARB — сняты с плана.** Переводчиков, кроме
    нас, нет, ключи названы по смыслу, а механическое описание к пятистам
    сорока ключам пересказало бы имя ключа. Описание пишут там, где без
    него ключ двусмыслен, — как у `quitApp` рядом с `trayQuit`.

### Этап E. Ворота: чтобы стражи мерили то, что обещают (P1)

- [ ] **E1. Измеритель сложности не видит 46 из ~1760 тел функций.** **[✔]**
  Суть перепроверена сверкой с AST-измерителем, счёт — по рецензии. После
  `)` он ждёт `{` или `=>` — список инициализации не проходит
  (`tool/check_complexity.dart:163–165`); имя после точки отбрасывается
  (`:222`) — мимо идут `factory Foo.fromJson`; тип-запись или тип-функция в
  возврате — «не объявление» (`:186–217`); непарная скобка — молчаливый
  пропуск (`:176`). Итого невидимы 22 фабрики (все `fromJson`), 21
  конструктор (каждого блока), 3 `operator ==`. Два невидимых уже сверх
  порога при пустом списке: конструктор `LibraryBloc` — 90 строк,
  `DownloadsBloc` — 62. AST-измеритель (`cognitive_complexity`) даёт
  `SaveActivityWatch._scan` 16 при нашем 15, а `_materialize` и
  `HttpFetch.bytes` — ниже 12 при наших 15. Не считаются `??`, `?.`, `when`,
  ветки `switch`-выражений, второй `&&` в элементах коллекций. Лечение:
  (а) сразу, **S**: «перепись» — число тел на уровне членов класса обязано
  совпасть с числом найденных функций, иначе страж красный; эталонный набор
  из ~25 фрагментов с известными числами; (б) 💬, **M**: перейти на AST —
  `package:analyzer` dev-зависимостью или `cognitive_complexity` с
  `--fail-threshold`, закреплённый, как `bloc_tools`.
  *Сделано (а):* перепись `bodiesIn` в `tool/check_complexity.dart` — тела
  на уровне объявлений по `{`/`=>` не после `=`; страж «замер видит каждое
  тело функции» сверяет счёт по каждому файлу `lib` (1873 тела, ноль
  расхождений). Замер научен списку инициализации, `Имя.имя`, `operator`,
  типу-записи и типу-функции в возврате; эталонные фрагменты — по одному на
  каждую форму, плюс три ловушки (конструктор без тела, `if (a) g()`, поле
  с замыканием). Вскрылось четыре нарушения — все исправлены, а не внесены в
  список: конструкторы `LibraryBloc` (95) и `DownloadsBloc` (68) разложены
  на `_handle…`/`_listen`, `EvsaveJobs.write`/`unpack` — на `_addEntry`,
  `_withArchive`, `_unpackAll`. *Открыто:* (б) переход на AST 💬 и то, что
  замер не считает `??`, `?.`, `when` и ветки `switch`-выражений.
- [x] **E2. У шести стражей из семи нет теста «страж ловит нарушение».**
  **[○]** Сломанная регулярка — вечная зелень. `widget_structure` не видит
  `Widget get _header`, типы возврата вне трёх перечисленных (`Column
  _section()`), наследников проектных виджетов, `lib/main.dart` и
  `lib/input`; `theme_structure` — `Opacity(opacity: 0.35)` (уже три места:
  `draggable_game.dart:24,28`, `downloading_header.dart:37`),
  `Duration(seconds:`, `BorderRadius.all(Radius.circular(8))`,
  `Theme.of(context).brightness`; `layering` — транзитивное:
  `gamepad_binding.dart:2` сам тянет `package:gamepads`, то есть плагин в
  моделях уже есть, а «сервисы не знают блоков» заявлено, но не проверяется;
  `arb_usage` ищет `\.key\b` по всему тексту — убери `L.of(context).source`,
  и ключ останется «живым» за счёт `download.source` (таких около десятка).
  Лечение: табличные тесты детекторов на `SourceFile('lib/ui/x.dart',
  '<фрагмент>')`. **M**
  *Сделано:* у всех семи стражей группа «страж ловит нарушение».
  - `widget_structure` видит геттеры, конкретные типы Flutter (`Column
    _section()`), наследников своих виджетов (`widgetTypes`) и обходит
    `lib/main.dart` с `lib/input`; `of`/`maybeOf` — поиск, а не сборка.
  - `theme_structure`: `opacity:` дробью (три места переведены на новые
    ступени `EvaporateAlpha.lifted/ghost/underlay`), `Duration` в любых
    единицах (`FrameStep.minInterval` — в списке, это частота кадров),
    `Radius.circular(<число>)`, `brightness`.
  - `layering` транзитивный (`test/support/layering.dart`), «сервисы не
    знают блоков», белый список чистых пакетов для моделей и ядра; вскрыл
    `path_provider` в `app_paths`/`system_folders` — в список с причиной, а
    у записи про геймпад исправлено неверное «плагина в моделях нет».
  - `arb_usage` считает только чтения у переводов, одним проходом.
  - `localization` ищет кириллицу по разбору: двойные и тройные кавычки
    видны, хвостовой комментарий с апострофом больше не ловится.
  - `test_hygiene` берёт границу вызова по скобкам, а не по первому `);`.
- [ ] **E3. Общий процент прячет файлы с нулём.** **[✔]** По отчёту этого
  прогона: девятнадцать файлов с 0 % и сорок пять ниже 50 %. С нулём — весь
  интерфейс папки синхронизации (`sync_package_row`, `pick_game_dialog`,
  `bulk_report_view`, `bulk_outcome_group`, `sync_folder_contents`), окно
  захвата кнопки, окно выбора исполняемого файла; `update_transport.dart` —
  43 %: докачка с диапазоном, «ради которой загрузка по плохому каналу
  вообще заканчивается», не покрыта — тест подменяет её подделкой;
  `saves_bulk.dart` — 54 %, `managed_download.dart` — 62 %.
  `_reportedNowhere` ловит «ни разу не загружен», но не «загружен и не
  выполнен». `app_services.dart` — порядок подъёма и гашения блоков — внесён
  в исключения в ответ на красный прогон, а не покрыт. Лечение: храповик «на
  файл» — список файлов ниже 50 % с их числом, новым туда нельзя, **S**;
  слить lcov трёх систем, **M**: сейчас тест, существующий только для
  Windows или macOS, можно удалить, и порог не заметит.
  *Сделано (храповик на файл, S):* `thinFileProblems` и `thinFiles` в
  `tool/check_coverage.dart` — сорок два файла ниже 50 % с долей по
  прогону ubuntu (отчёт скачан из артефакта `coverage-<sha>`); новый тонкий
  файл, опустившийся названный и доросший, но не вычеркнутый, валят прогон;
  записи сверяются с существующими файлами. Тесты — в
  `coverage_report_test`. Числа — на три пункта ниже достигнутого; вне Linux
  список только показывается (под Windows `menu_entry_row` — ноль).
  *Открыто:* слить lcov трёх систем (**M**) и
  покрыть `app_services.dart` вместо записи в `_reportedNowhere`.
- [x] **E4. Запуск, код возврата и остановка игры на Windows не проверяются
  ни одним тестом.** **[✔]** Семь тестов из двенадцати пропущены со словами
  «скрипт sh не запустится на Windows» (`game_launcher_test.dart`) — на
  главной игровой системе `_track`, `terminate` и колбэк выхода не
  исполняются ни на одном раннере. «Игрой» взять `cmd.exe` с `['/c', 'exit
  3']` и `ping -n` для «идёт» и «остановить». **S**
  *Сделано:* «игры» для каждой системы в `game_launcher_test` —
  `exitingWith`, `stillPlaying`, `noisy`: на POSIX скрипт `sh`, на Windows
  `cmd.exe /c exit N`, цикл `for /L` с выводом в оба потока и `PING.EXE`
  без `cmd` посередине (убитый `cmd` оставил бы `ping` сиротой). Пять
  тестов больше не пропускаются на Windows; остались пропуски по делу —
  `python3` для игры, глухой к сигналу, и формат `.app`.
- [x] **E5. Следующая ступень храповика — то, чем дробление обходят.** **[○]**
  Замыканий-строителей в `build` — 68, из них 21 длиннее 25 строк
  (`library_grid.dart:51` — 52, `navigation_rack.dart:37` — 51,
  `library_body.dart:78` — 49); диалоги, собранные в функциях
  (`cancel_dialog.dart:71`, `confirm.dart:19`); три `const`-виджета верхнего
  уровня (`featured_art.dart:75,90,101`). Страж на длину замыкания в `build`
  и на число параметров виджета (больше семи), оба храповиком. **S**
  *Сделано:* `buildClosures` в `tool/check_complexity.dart` (литерал функции
  после `(`, `,` или `:` внутри `build`, в том числе записанного стрелкой) и
  `wideWidgets` в `test/support/widget_structure.dart` (параметры без
  `key`). Оба — храповики в `widget_structure_test` с числом: тридцать одно
  `build` с замыканием длиннее 25 строк (по самому длинному) и девять
  виджетов шире семи параметров, `LibraryBody` — семнадцать. Диалоги,
  собранные функциями (`showConfirm`), оставлены: это идиома `showDialog`,
  а не обход. Три `const`-затемнения в `featured_art.dart` — тоже: это
  постоянные экземпляры, а не сборка, и перестраивать их нечему.
- [ ] **E6. Тесты: паузы под проверками и чувствительность к лишнему
  `await`.** **[○]** Пятьдесят `Future.delayed`. «Ничего не произошло за
  50 мс» проходит вхолостую на медленной машине
  (`downloads_bloc_test.dart:257,381,485`, `update_bloc_test.dart:103,124,
  134`, `save_hints_test.dart:172`, `download_pipeline_test.dart:289`);
  положительные после паузы будут плавать (`downloads_bloc_test.dart:444`,
  `library_bloc_test.dart:504,787`). Под тесты подогнан код приложения
  (`saves_snapshots.dart:94–100`, синхронный `existsSync` в
  `saves_hints.dart:47–58`) — это слабость обвязки, а не свойство блоков.
  Лечение: барьером `await bloc.close()` или тестовый `BlocObserver` на
  `onEvent`/`onDone` с ожиданием «обработчиков в работе нет». Неперенесённые
  копии `waitFor` — `catalog_progress_test.dart:122`,
  `bulk_report_test.dart:88,95`. **M**
  *Сделано:* барьер `HandlerTracker` (`test/support/bloc_idle.dart`,
  `onEvent`/`onDone` всех блоков; `settle()` ждёт два спокойных оборота —
  обработчик мог передать событие соседу через поток), со своим тестом.
  Паузы под проверками заменены в `downloads_bloc_test` (пять и опрос
  `waitForCall`), `update_bloc_test` (три), `save_hints_test` (две),
  `library_bloc_test` (две), `download_pipeline_test` (три опроса); копии
  `waitFor` в `catalog_progress_test` и `bulk_report_test` — на
  `waitForState`. Оставлены по делу: ожидание отложенной записи по таймеру,
  пауза «пусть второе нажатие придёт, пока первое ждёт снимка», оборот
  микрозадач и имитация работы в подделке. *Открыто:* код приложения,
  подогнанный под тесты (`saves_snapshots.dart:94–100`, синхронный
  `existsSync` в `saves_hints.dart`), — откатывать отдельно, с барьером.
- [ ] **E7. `avoid_public_bloc_methods` в `bloc_lint` 0.4.2 не видит
  публичные члены после первого `@override`.** **[○]** Без пометок проходят
  `persist` у библиотеки и сохранений, `snapshotBeforeLaunch`, `applyLimits`
  и `torrents`, который снаружи не нужен вовсе. Проверить на минимальном
  примере, пометить эти места руками, сообщить автору пакета. В
  `analysis_options.yaml` написано «восемь мест», их девять. **S**
  *Проверено и сделано:* на минимальном примере правило ловит метод до
  `@override` и молчит о методе после. `torrents` и `applyLimits` стали
  приватными (тест пределов — через `DownloadLimitsRefreshed` и барьер),
  `persist` у двух блоков и `snapshotBeforeLaunch` помечены с объяснением;
  в `analysis_options.yaml` — «двенадцать мест» и почему пометки стоят и там,
  где линтер слеп. Свой страж — `bloc_members_test` (весь класс, перенесённый
  заголовок, геттеры). *Открыто:* сообщить автору `bloc_lint` — наружу, по
  согласию.
- [x] **E8. Страж «тема не теряет поле» — по исходнику.** **[○]** Тест
  `values` не ловит поле, забытое и в `values`, и в `lerp`, — то есть ровно
  тот случай, ради которого написан. `copyWith` у `HardwareSurfaceTheme`
  принимает 2 поля из 22, у `EffectsPalette` — 1 из 5, и `values` там нет.
  Сверять число `final`-полей с `values.length` и числом аргументов `lerp`.
  **S**
  *Сделано:* `test/ui/theme/theme_fields_test.dart` — счёт от объявлений:
  у каждого `ThemeExtension` в `lib/ui/theme` столько же именованных
  аргументов у конструктора в `lerp`, сколько `final`-полей, и столько же
  элементов в `values`, где он есть; нашлись все четыре расширения, страж
  ловит поле, забытое в `lerp`. Сейчас всё сходится. `copyWith` с
  урезанным набором (2 из 22 у `HardwareSurfaceTheme`, 1 из 5 у
  `EffectsPalette`) оставлен: в `lib` его не зовёт никто, а объявить его
  требует `ThemeExtension`. Полный набор был бы кодом без читателя; когда
  понадобится правка поля — расширят вместе с правкой.
- [x] **E9. Страж «имена из документов существуют».** Каждый путь и каждое
  CamelCase-имя в обратных кавычках из `CLAUDE.md`, `README.md`,
  `CONTRIBUTING.md` находится в `git ls-files` или в исходниках; список
  исключений — «бывший `SaveFreshnessCubit`»; «N блоков» сверяется с
  `lib/bloc/*`. Рецензия сделала это скриптом: из 323 имён разошлись
  единицы, так что ворота выйдут дешёвыми. **S**
  *Сделано:* `test/tool/docs_names_test.dart` — пути и CamelCase-имена в
  обратных кавычках из `CLAUDE.md`, `README.md`, `CONTRIBUTING.md`
  существуют (файл или папка; имя — в коде `lib`, `test`, `tool`,
  `.github`, раннеров); исключение одно — «бывший `SaveFreshnessCubit`».
  Список блоков уровня приложения в CLAUDE.md сверяется с тем, что раздаёт
  `main.dart`. Нашлось и исправлено: `lib/ui/app_colors.dart` в README
  (цвета давно в `lib/ui/theme/`) и «пять блоков» в обоих документах —
  после D1 и D2 их семь.

### Этап F. Интерфейс после дробления

Сначала ошибки (P1), потом устройство (P2).

- [x] **F1. Дробление потеряло ключ плитки.** **[✔]** `itemBuilder`
  возвращает `LibraryGridTile` без `key` (`library_grid.dart:76–98`), а
  `ValueKey(game.id)` уехал на `MouseRegion` внутри `build`
  (`library_grid_tile.dart:62`); до выноса `_tile` возвращал `MouseRegion` с
  ключом напрямую (`git show 102cac8^`). `findChildIndexCallback` ключа
  больше не получает — комментарий над ним неправда: набрал букву в поиске
  или сменил полку — плитки собираются заново, `RiseIn` всходит повторно,
  приподнятость теряется. Лечение: `key: ValueKey(game.id)` на
  `LibraryGridTile`. Тест: состояние `RiseIn` той же игры после отбора — тот
  же объект. **S**
  *Сделано:* `key: ValueKey(game.id)` — на `LibraryGridTile` в
  `itemBuilder`, с `MouseRegion` снят. Тест «после отбора плитка остаётся
  той же» (состояние `RiseIn` «Беты» после ухода «Альфы» с полки — тот же
  объект) без правки падает.
- [x] **F2. Замер для догоняющей прокрутки считает другими числами, чем сама
  сетка.** **[✔]** `_measure` берёт 64, 36 и +40 (`library_grid.dart:25–26,
  108–115`), делегат — поля 28+28, просвет 28, шаг ряда +32 (`:66–74`). По
  подсчёту рецензии число столбцов расходится для 45 ширин из 170: при 1280
  — шесть настоящих против пяти посчитанных. Это главный путь возврата из
  игры: `GamePage` заменяет `LibraryBody`, `PageStorageKey` в проекте нет,
  сетка возвращается в ноль и надеется на `scrollTo` — 120 игр, открыл
  сотую, «назад» — прыжок мимо, фокус не вернулся. Лечение: одна функция над
  теми же постоянными; лучше — `PageStorageKey` сетке. Тест: `_measure` и
  `getLayout` делегата сходятся. **S–M**
  *Сделано:* делегат сетки один — `LibraryGrid.delegateFor`, поля —
  `LibraryGrid.padding`, а `layoutFor` спрашивает у того же делегата
  `getLayout` (`crossAxisCount`, `mainAxisStride`) вместо своей арифметики.
  Тест на 1100, 1280 и 1600: столбцы и шаг ряда из `layoutFor` совпадают с
  разложенными плитками; прежняя формула при 1280 давала пять вместо
  шести. `PageStorageKey` не ставил: у сетки свой `ScrollController`, и
  сохранённое смещение спорило бы с догоняющей прокруткой к фокусу.
- [x] **F3. «/» съедается во всех текстовых полях под оболочкой.** **[✔]**
  `SingleActivator(slash) → search` (`input_scope.dart:221`), защиты нет —
  проверка на `EditableText` стоит только у «назад» (`:153–155`). В пароле
  прокси «/» не вводится и уводит в поиск; «Fate/stay» в поиске не набрать.
  Единственный тест проверяет, что «/» уводит фокус. Лечение: не
  перехватывать при фокусе в `EditableText` (или
  `DoNothingAndStopPropagationTextIntent` над полями). Тест: «/» в поле
  остаётся в поле. **S**
  *Сделано:* «/» — своё намерение `TypedSearchIntent` с действием,
  выключенным, пока фокус в поле; выключенное действие `Shortcuts` не
  обрабатывают, и символ доходит до поля. Проверка поля — `focusInTextField`
  по предкам: контекст узла фокуса — `Focus` внутри `EditableText`, и
  прежняя проверка «сам виджет — поле» (она же у Escape в `_back`) не
  срабатывала никогда. У Escape заметной разницы нет: на странице игры
  полей вне диалогов нет, а в остальных местах исход тот же — снятие
  фокуса. Тесты: «/» в поле оболочкой не обрабатывается и фокус остаётся;
  `Ctrl+F` уводит в поиск из поля.
- [x] **F4. Текст на заливке `danger` не проходит WCAG.** **[○]**
  `dangerFilled` в «Картридже» — 2,67:1 (`button_styles.dart:24–25`),
  SnackBar ошибки — около 3,0:1 в обеих схемах (`snack.dart:14–15`);
  комментарий там говорит о белом тексте, а текст — `textPrimary`. У
  `danger` нет пары заливка/надпись, как у `primary`, и тест контраста
  меряет его только текстом на подложках. Завести `dangerFill` и `onDanger`
  с тестом пары. **S**
  *Сделано:* `dangerFill` и `onDanger` в палитре (Арклайт — `E96A5C` с
  `0A0D11`, Картридж — `A8231B` с белым), `dangerFilled` и SnackBar
  ошибки — на паре, SnackBar — плотной заливкой, без `withValues`. Тест
  пары «надпись на опасной заливке читается» (≥ 4,5) в обеих схемах;
  `theme_fields_test` держит, что пара есть в `values` и `lerp`.
- [x] **F5. В интерфейсе показана русская «журнальная» подпись.** **[○]**
  `info_section.dart:53–54` выводит `source.label`, задокументированный как
  «для журналов» (`game_source.dart:15–20`): в английской локали —
  «Source: Локальная папка: …». Страж локализации мимо — литерал лежит в
  `lib/models`. Лечение: `gameSourceLabel(l, kind)` в `ui/labels.dart` и
  страж «`lib/ui` не читает `.label` моделей». **S**
  *Сделано:* журнальные подписи названы `logLabel` (`GameSource`,
  `SnapshotOrigin`, `NavAction`, состояние движка), показ — через
  `gameSourceLabel(l, kind)` в `ui/labels.dart`. Страж в
  `localization_test`: `lib/ui` не читает `.logLabel`. Тест
  `info_section_test`: в английской локали источник — «Folder: …».
  Три из четырёх `logLabel` не читает никто — записано в F13.
- [x] **F6. Обложки декодируются в полный размер.** **[○]** У `CoverArt`,
  `DetailCover`, `CoverBackdrop`, `FeaturedArt` нет `cacheWidth`: 600×900 —
  около 2 МиБ, 48 плиток — около 99 МиБ при пределе кэша картинок в 100, а
  выбранная человеком 2000×3000 — 23 МиБ одна. В сетке — `cacheWidth ≈ 215 ·
  scale · dpr`. `cacheWidth: 1024` у кадров (`shot_frame.dart:52`) ничего не
  даёт: вверх `ResizeImage` не масштабирует. **S**
  *Сделано:* `decodeWidth(context, logical)` (`lib/ui/library/cover/`,
  ширина × плотность экрана, ступенями по 64): плитка — по своей ширине
  через `LayoutBuilder`, `DetailCover` — по 132, `FeaturedArt` — по ширине
  окна, размытый `CoverBackdrop` — 480. `cacheWidth: 1024` у кадров снят:
  вверх расшифровка не растягивает, и он не делал ничего. Тест
  `cover_decode_test`: плитка 200 точек при плотности 2 просит 448.
- [x] **F7. Геймпад и клавиатура: четыре места, куда не дойти.** **[○]** У
  `LauncherActionButton` нет состояния фокуса — только 12 % заливки поверх
  золота (`launcher_action_button.dart:30–68`); ползунок мёртвой зоны с
  геймпада не меняется (влево-вправо всегда уходят в `focusInDirection`);
  окно захвата кнопки с геймпада не отменить — любая кнопка становится
  назначением; очередь загрузок переставляется только мышью. Рядом: окно
  поиска игр не закрывается Esc, но закрывается B; `NavigationKey` не
  передаёт `selected` в `Semantics`. **M**
  *Сделано:*
  - `LauncherActionButton` — состояние фокуса: кант `selection` и ореол, как
    при наведении.
  - Ползунок мёртвой зоны — `AdjustValueIntent` (`lib/input/input_scope.dart`):
    влево-вправо обход сперва предлагает фокусу, и тот, кто объявил действие,
    меняет значение шагом деления.
  - Окно захвата кнопки — Start закрывает без назначения
    (`CaptureButtonDialogState.cancelButton`), подсказка в окне это говорит.
  - Очередь — клавиши «выше»/«ниже» на `QueuedCard`, тем же
    `DownloadReordered`, что и перетаскивание.
  - Окно поиска игр — Esc через свой `DismissIntent` и узел фокуса с
    `autofocus`: в окне не было сфокусировано ничего, и Esc уходил вниз.
  - `NavigationKey` — `selected` в `Semantics`.
  Тесты: `reachability_test` (фокус клавиши, шаг ползунка, выбранный раздел
  для диктора — без правок все три падают), `capture_button_dialog_test`,
  `queue_keys_test`, «Esc закрывает окно» в `scan_folder_dialog_test`. По
  ходу найдена ошибка замера: стрелка-аргумент тянулась до конца вызова, и
  одиннадцать «длинных замыканий» были её артефактом — список храповика
  переснят (двадцать мест).
- [ ] **F8. Состояние экрана — в дерево, а не пробросами.** **[○]** В
  виджетах с четырьмя и более параметрами 162 из 348 только передаются
  дальше: `LibraryBody` — 12 из 17, `AddGameFields` — 11 из 14,
  `ProxyAddressFields` — 10 из 10, `LibraryToolbar` — 8 из 8; `onShelf` и
  `onQuery` идут четыре шага. Причина одна: `LibraryViewBloc` создаётся без
  провайдера (`library_page.dart:42,71`), а родители `AddGameBloc` и
  `ProxyFormBloc` всё равно превращают события в колбэки. Лечение:
  провайдер, листья читают блок сами; однопользовательские с пробросом от
  70 % — слить обратно (`SourceFields`, `LibraryShelfBar`,
  `LibraryHeadingBar`, `ProxyAddressFields`). Критерий выноса: повторное
  использование, свой `State` или ресурсы, свой `select` при трёх и менее
  параметрах, граница перерисовки, отдельный тест. **Дробить дальше — не
  лечение.** Туда же: наведение делает `setState` всей страницы и следом
  вторую перестройку от `GameSelected`; в `build` есть побочные действия —
  `forgetGone` освобождает `FocusNode` (`library_page.dart:131`), `_measure`
  пишет в контроллер; контроллер сетки раздать `InheritedNotifier`. **M**
  *Сделано (библиотека):*
  - `LibraryViewBloc` раздаётся `BlocProvider.value` под страницей; вкладки
    полок и поле поиска читают и меняют его сами, а `onShelf`, `onQuery` и
    `shelf` из цепочки ушли.
  - `LibraryShelfBar` слита с `LibraryToolbar`: она только считала числа.
  - Наведение больше не перестраивает страницу: `LibraryGridTile` сама слушает
    контроллер и перестраивается, когда меняются её «под курсором» и «горит»;
    `hovered`, `active`, `tileKey` и `focusNode` из параметров ушли. Тест —
    экземпляр `LibraryBody` после наведения тот же.
  - `forgetGone` — после кадра и только при смене набора игр.
  Итог по параметрам: `LibraryBody` 17 → 14, `LibraryGridTile` 11 → 8.
  *Открыто:* формы добавления игры (`AddGameFields`, 11 из 14 проброшены) и
  прокси (`ProxyAddressFields`, 10 из 10) — у них своя логика событий,
  отдельная правка. `_measure` пишет в контроллер из `LayoutBuilder` —
  оставлено: это поля без оповещения, не перестройка.
- [ ] **F9. `HardwareSurfaceTheme` стал мешком ручек одиннадцати
  виджетов.** **[○]** Из 22 полей шесть — у `GlassSurface`, двенадцать — у
  десяти других, по одному; четыре мёртвых (`backdrop*Opacity`,
  `grilleHole`); цветов — два. Архив (§2) советовал тему на компонент рядом
  с виджетом — к этому и вернуться: `double`-ручки к тому, кто их читает. В
  `evaporate_theme.dart:153` остался `isDark ? 0.82 : 0.9`. **M**
  *Сделано:* мёртвые `backdropCool/Deep/WarmOpacity` удалены;
  `isDark ? 0.82 : 0.9` в теме карточек — поле `materialCardOpacity`; шесть
  полей стекла — своё расширение `GlassSurfaceTheme`
  (`lib/ui/theme/glass_surface_theme.dart`, `arclight`/`cartridge`, в
  `_build` обеих схем), `GlassSurface` читает его; тесты — концы смешивания
  и различие схем, страж полей по исходнику его подхватил. *Открыто:*
  одиночные ручки десяти других виджетов (`shellOpacity`, `toolbarOpacity`,
  `cardOpacity`, `readoutOpacity`, `scrimOpacity`, `ambientStrength`,
  `vignetteOpacity`, `keySheen`, тени обоймы и кадра) всё ещё в
  `HardwareSurfaceTheme`; `grilleHole` ушёл с `HardwareGrille` в F13.
- [ ] **F10. Роли текста расползаются, а шкалы отступов нет вовсе.** **[○]**
  47 употреблений ролей из 133 правят роль через `copyWith` (`caption` — 8
  из 10, `figure` — 4 из 5); `label` (9 / w700 / +1,4) и `eyebrow` (9,5 /
  w800 / +1,6) — то самое расхождение, которым §2 архива обосновал роли, —
  стали двумя ролями; `small`, `chip`, `pathSmall` — все 11,5. Мимо стража:
  `fontWeight:` — 15 мест, `letterSpacing:` — 7 мест с семью значениями,
  `TextStyle(` — 11. Отступы: 163 `SizedBox` с 17 значениями, 76
  `EdgeInsets` с 22 числами; размеры значков — 93 места, 13 значений;
  `Curves.*` по месту — 10 при четырёх токенах; 900 мс в
  `animated_progress.dart:64` идут мимо `context.motion`, то есть мимо
  системной просьбы не двигаться. Лечение: шкала `EvaporateSpacing` и
  размеры значков токенами, стражи храповиком; роли свести, правку ролей
  через `copyWith` — только цветом. **M–L**, по папкам, как этап 2 архива.
  *Сделано (движение):* 900 мс в `animated_progress.dart` — ступень
  `EvaporateMotion.track`, в наборе `still` она ноль, как остальные; запись
  из списка длительностей стража снята. Тест: при `disableAnimations` полоса
  сразу на новом значении (без правки падает). *Открыто:* шкала
  `EvaporateSpacing` и размеры значков токенами, свод ролей текста (правка
  ролей через `copyWith` — только цветом), `Curves.*` по месту (десять мест)
  — **M–L**, по папкам.
- [ ] **F11. Вечно живые страницы подписаны на состояние целиком.** **[○]**
  `downloads_page.dart:31–32` и `saves_page.dart:31–34` перестраиваются
  каждую секунду, пока скрыты в `IndexedStack`; `SavesPage` сортирует все
  снимки в `build`, а их список — неленивая `Column`; `engine_readout.dart:
  23` и `app_footer.dart:23` — `watch` там, где хватило бы `select`. **S**
  *Сделано:* `DownloadsPage` и `SavesPage` подписаны через `select` с
  оглядкой на `TickerMode`: скрытому разделу селектор отдаёт постоянное
  `null`, и перестраиваться не от чего, а показавшийся перестраивает смена
  `TickerMode`. `EngineReadout` выбирает только движок и показания,
  `AppFooter` — только раскладку геймпада. Тест `hidden_page_test`:
  скрытые загрузки не перестраиваются от снимка задач (без правки падает),
  показанные видят то, что лежит в блоке. *Не сделано:* сортировка снимков
  в `build` у `SavesPage` и неленивая `Column` — частоту перестроек убрал
  `select`, сам список — отдельная правка.
- [x] **F12. Числа, даты и размеры идут мимо локали.** **[○]** `formatBytes`
  всегда «1.5 GB» (9 вызовов из интерфейса), `formatDateTime` всегда
  dd.MM.yyyy (14), при обещании «1,2 МБ» в `ui/labels.dart:124`. Журнальные
  функции оставить русскими, показ — через локаль. **M**
  *Сделано:* `bytesLabel`, `dateTimeLabel`, `countLabel` в
  `lib/l10n/labels.dart` (`NumberFormat` и `DateFormat` по `l.localeName`,
  единицы — ключами ARB `unitB…unitTB`), реэкспорт из `ui/labels.dart`;
  все 23 показа в `lib/ui` и сообщения человеку в блоке сохранений и
  сервисах переведены. `formatBytes`/`formatDateTime` оставлены журналу и
  заметке резервной копии: она хранится в снимке, а `DateFormat` языка без
  загруженных интерфейсом данных падает — на этом и сорвались тесты
  восстановления при первой попытке. Тест `labels_test` на обоих языках;
  `snapshot_tile_test` ждал «140.0 MB» в русском интерфейсе — исправлен.
- [x] **F13. Мёртвый код после дробления.** **[○]** `HardwareGrille`
  (единственное упоминание — `findsNothing` в тесте, который упасть не
  может), `gameRootLabel` — единственный читатель пяти ключей ARB, которые
  из-за него проходят стража, `AppColors.ambientWarm/Cool/Deep`,
  `frostDark/Light`, `EvaporateTheme.radiusShell` (из «четырёх радиусов
  корпуса» живут три), `EvaporateMotion.settle`, `GamepadBinding.unassign`,
  обязательный и нечитаемый `RuleTile.gameDir`, `putBytes` в хранилище;
  `assets/branding/frost_world_background.png` — 2,4 МБ без ссылок, едет в
  каждую сборку и каждое обновление. Два цикла импортов: `shots_backdrop` ↔
  `shots_slideshow` ↔ `shot_frame` и `shell` ↔ `shell_layout`. **S**
  *Дописано при F5:* `logLabel` у `SnapshotOrigin`, `NavAction` и
  состояния движка «для журналов» — но в журнал их не пишет никто.
  *Сделано:* удалены `HardwareGrille` (с тестом, который упасть не мог, и
  полем `grilleHole`), `gameRootLabel` вместе с пятью ключами ARB, цвета
  `ambientWarm/Cool/Deep` и `frostDark/Light`, `radiusShell`,
  `EvaporateMotion.settle`, `GamepadBinding.unassign`, нечитаемый
  `RuleTile.gameDir`, `logLabel` у `SnapshotOrigin`, `NavAction` и состояния
  движка, `frost_world_background.png` (2,4 МБ). `putBytes` ушёл из
  хранилища — тестам хватает `putText` (`test/support/`), который кладёт
  обычным `put`. Циклы импортов разорваны: тайминги подложки кадров — в
  `ShotsTiming`, поля окна — в `ShellLayout`. Документы — «три радиуса»,
  кривые без `settle`.

### Этап G. Выпуск и документы

- [x] **G1. Собранное приложение до выкладки не запускается ни разу.** **[○]**
  `integration_test` нет, `main.dart` и `app_services.dart` исключены из
  покрытия поимённо. Лечение: ключ `--smoke` (первый кадр, запись
  `JsonStore` во временный дом, `exit 0`) и его запуск в каждой сборке — на
  Linux под `xvfb-run`; на macOS — «репетиция обновления»: свой же
  `UpdateUnpack` над `dist/*-macos.zip` и `codesign --verify --deep`. **M**
  И чек-лист перед тегом, десять минут, в `CONTRIBUTING.md`: обновление с
  прошлого релиза на Windows; малый легальный magnet через SOCKS5 до
  «Играть»; выход из игры → автоснимок → восстановление поверх изменённых
  сейвов; `.evsave` на другую систему; недоступный прокси даёт `blocked`;
  `.run` — поставить, обновить, удалить. Это и закрывает «на живой macOS не
  проверено» из архива.
  - *Сделано:* `--smoke` (`SmokeRun`): полный подъём во временном доме
    (`AppPaths.init(home:)`), первый кадр, запись `JsonStore` с правами,
    штатное завершение, непустой журнал — итог кодом выхода; шесть тестов
    на честность ответа. В CI после упаковки: Windows запускает папку
    сборки, Linux и macOS — архив релиза, распакованный нашим
    `UpdateUnpack` (`tool/rehearse_update.dart`), Linux под `xvfb-run` и
    `dbus-run-session`, macOS сперва через `codesign --verify --deep
    --strict`. Чек-лист перед тегом — в `CONTRIBUTING.md`, на двух языках.
    *Открыто:* проверено только на Windows — собранный `evaporate.exe
    --smoke` вернул 0 за 3 с тем же сценарием PowerShell, что в CI; шаги
    Linux и macOS впервые пройдут на ручном запуске сборок
    (`workflow_dispatch`), и до него пункт не закрыт.
  - *Проверено:* ручной прогон сборок 35685886154 — Windows, macOS и Linux
    зелёные, дымовой запуск и репетиция обновления прошли на всех трёх.
- [x] **G2. `CHANGELOG` сверять с диффом, а не с планом.** Запись 0.36.0 про
  единый график скорости не соответствует коду: до D1 — поправить разделом
  «Исправлено» в следующей версии. В порядок выпуска: каждая строка раздела
  сверена с `git diff v<прошлая>..HEAD`. Пункт «Для разработки» в 0.37.0 —
  внутренний; исправление ухода запросов мимо прокси заслуживает раздела
  «Безопасность». **S**
  *Сделано:* неверная запись 0.36.0 о графике скорости исправлена пунктом
  «Исправлено» неизданной версии (сделано в D1). Раздел «Безопасность» —
  первым в неизданной версии (утечки мимо прокси: UDP/WebSocket-трекеры,
  обновления, запросы по адресу прежнего прокси) и в 0.37.0 (прокси, до
  которого не дотянуться); внутренний пункт «Для разработки» из 0.37.0
  убран. В `CONTRIBUTING.md` порядок выпуска дополнен шагом «сверьте каждую
  строку с `git diff v<прошлая>..HEAD`» и правилом раздела «Безопасность».
- [ ] **G3. Подлинность обновления держится на одной учётной записи
  GitHub.** **[○]** 💬 Windows-клиент молча запускает `setup.exe
  /VERYSILENT` из последнего релиза; `SHA256SUMS` считает то же задание и
  кладёт в тот же релиз, а его недоступность обновление не отменяет — это
  закреплено тестом (`update_install_test.dart:436`), и пропуск сверки в
  журнал не пишется (`update_download.dart:205,218`). Лечение: подпись
  `SHA256SUMS` (ed25519 / minisign) с открытым ключом в приложении и отказ
  без неё; правило защиты тегов `v*`; в `README.md:110` поправить совет для
  macOS — с macOS 15 обход через контекстное меню убран. **M**
- [ ] **G4. CI.** **[✔]** `cancel-in-progress` отменяет прогоны `main`, а
  группа общая для `push`, `schedule` и `workflow_dispatch` — любой пуш в
  понедельник утром отменит еженедельную сборку (`ci.yml:20–22`);
  репозиторий публичный, минуты бесплатны — экономии нет. Отменять только
  для `pull_request`, имя события — в группу. **[○]** `pages.yml` держит
  экшены по тегам и без `timeout-minutes` — вопреки «закрепить всё, что CI
  берёт снаружи»; транзитивные зависимости `bloc_tools` разрешаются заново
  на каждом прогоне — перенести в `dev_dependencies` под
  `--enforce-lockfile` 💬; зерно перемешивания писать в сводку при падении,
  репортёр `failures-only`; сделать тег в форке `dtorrent_task_v2`. **S**
  - *Сделано:* группа `concurrency` включает имя события, отменяются только
    прогоны `pull_request`; экшены `pages.yml` закреплены хешами, у задач
    `timeout-minutes: 10`; зерно перемешивания выбирается в шаге
    (`$RANDOM$RANDOM`) и при падении уходит в сводку готовой командой,
    репортёр `failures-only`. *Открыто:* `bloc_tools` в `dev_dependencies`
    (💬 — тянет свой набор зависимостей в `pubspec.lock` приложения) и тег
    в форке `dtorrent_task_v2` (правка чужого репозитория).
- [x] **G5. «Ровно то, что гоняет CI» — не ровно.** Локальный перечень в
  `CLAUDE.md` — три команды; в CI сверх них `bloc lint`, сверка
  регистраторов, порог покрытия и случайный порядок — красный `ca1356d`
  упал именно на пороге. Один `tool/gate` со всеми шагами и хук pre-push (он
  же пачкует пуши и снимает половину отмен из G4); версия Flutter локально
  3.47.5 против 3.47.4 в CI — сверять. **S**
  - *Сделано:* `tool/gate.dart` — шесть шагов задач `analyze` и `test` по
    порядку, без обрыва на первом провале, итог кодом возврата; перед ними
    сверка версии Flutter с `FLUTTER_VERSION` (предупреждение: 3.47.5 против
    3.47.4). `gate_test.dart` читает `ci.yml`: шаг CI без строчки в воротах
    или в `ciOnlySteps` роняет прогон, команды сверяются дословно. Хук
    `.githooks/pre-push` — по желанию (`core.hooksPath`): полный прогон идёт
    минуты, и навязывать его каждому пушу в свою ветку незачем. Перечни в
    `CLAUDE.md` и `CONTRIBUTING.md` заменены одной командой.
- [x] **G6. Документы против кода.** **[○]** `CLAUDE.md`: «Пять блоков» при
  четырнадцати папках в `lib/bloc` — семь блоков экранов не названы;
  `DtorrentEngine.torrentFromBytes` → `TorrentSource.fromBytes`; «`Vdf` — на
  сорок строк» при ста пятидесяти; «DHT в библиотеке не поднимается» —
  форк его починил (и см. A6); «повторный запуск гасится `isStarted`» —
  см. C1; «в журнал уходит „что случилось → что изменилось“» — только в
  отладке (C3); «диалог добавления сам генерирует id» — теперь `AddGameBloc`;
  правка игры переехала в `library_edits.dart`, `library_drops.dart` не
  назван; `SettingsPatched` не описан; «единственный `BlocListener`» — их
  три; «`DownloadHistoryScope` оборачивает обе половины» — областей две
  (D1). `README.md:364–393` — «пять задач», `needs: analyze`, без
  расписания; `:493` — ссылка на несуществующий `lib/ui/app_colors.dart`.
  `CONTRIBUTING.md:35` — «сборки только на теге», перечень CI без `bloc
  lint`, регистраторов и покрытия. После правок — страж E9. **S**
  - *Сделано:* `CLAUDE.md` — `TorrentSource.fromBytes`, `Vdf` без «сорока
    строк», `AddGameBloc` вместо диалога, `library_edits.dart` и
    `library_drops.dart` названы, `SettingsPatched` описан, слушатели
    сообщений и два слушателя диалогов разведены, журнал причин — «имя в
    выпуске, переход в отладке»; блоки, DHT и `isStarted` поправлены
    раньше. `README.md` и `README.en.md` — шесть задач CI, расписание,
    `needs: [analyze, test]`, `tool/gate.dart`. `CONTRIBUTING.md` —
    сборки по расписанию. Страж E9 читает теперь и `README.en.md`, и цели
    ссылок `](путь)` — и сразу нашёл в английском README
    `test/theme_test.dart` и `lib/ui/app_colors.dart` (там же поправлены
    ссылка на `test/release_artifacts_test.dart` и «четыре радиуса»).
- [x] **G7. Этот файл.** Архив ниже — 1500 строк закрытого плана. Ценное в
  нём — решения с причинами: тема в три яруса, «по умолчанию Bloc»,
  `GameRepository` и блоки захвата кнопки и окна — рассмотрены и отклонены,
  нет `bloc_test`, `freezed`, DCM. Им место в `docs/decisions/NNNN-*.md`,
  таблице оценок — в `docs/reviews/2026-09-19.md`; остальное хранят `git` и
  `CHANGELOG`. В `TODO.md` остаётся живое. Четыре стража ссылаются в
  комментариях на «этап 3 / 5 / 6 в `TODO.md`» — это этапы архива; при
  переезде поправить и их. Этапы этого разбора названы буквами как раз
  затем, чтобы с ними не путаться. **M**
  - *Сделано:* архив (1550 строк) убран из файла; весь его текст — в
    `git show f64b706:TODO.md`, на что указывает шапка. Решения —
    `docs/decisions/0001…0005` (по умолчанию Bloc, тема в три яруса, нет
    репозитория игр, нет блоков захвата кнопки и окна, нет `bloc_test`,
    `freezed` и DCM) с оглавлением; оценка и способ разбора —
    `docs/reviews/2026-09-19.md`. Четыре стража ссылаются теперь на этапы
    первого разбора в `docs/reviews/`, а `CLAUDE.md` велит заглядывать в
    решения, прежде чем предлагать слой или зависимость. Страж E9 читает и
    записи решений, а ссылки разбирает от папки документа.
- [ ] **G8. Гигиена.** **[○]** В рабочей копии на Windows 93 файла с CRLF,
  включая `tool/*.sh` (в индексе LF, CI в порядке — лечится перевыкладкой);
  нет `.editorconfig`; `site/assets/NunitoSans.ttf` без текста OFL; в
  `pubspec.yaml` шаблонный хвост после `fonts:`; у трёх записей стража
  длительностей нет причины; копия `shortcuts.vdf` перед записью хранится в
  одном поколении — вторая правка затирает оригинал человека; `equatable`
  3.0 и `tray_manager` 0.7 — мажорные обновления ждут решения. **S**
  - *Сделано:* CRLF в рабочей копии снят у 88 файлов — остались три, у
    которых он по правилу (`.ps1`, `.iss`, `.rc`); статус git при этом не
    сдвинулся. `.editorconfig` заведён, `line_endings_test` сверяет его с
    `.gitattributes`. `NunitoSans.ttf` нужен наброскам оформления (их
    выкладывает `pages.yml`), поэтому не удалён: `OFL-NunitoSans.txt` собран
    из строки авторства в самом шрифте, и рядом легли лицензии трёх
    остальных копий в `site/assets/` — их страница раздаёт тоже;
    `font_licenses_test` сторожит обе папки, `NOTICE.md` дополнен. Хвост
    шаблона в `pubspec.yaml` убран. У двух записей стража длительностей
    причина записана (у `frame_step` она была). Копий `shortcuts.vdf`
    теперь две — `.bak` до последней записи и `.orig` до первой, которую
    не переписывают; тест падает без правки. *Открыто:* `equatable` 3.0 и
    `tray_manager` 0.7 — 💬, мажорные обновления ждут решения.

---

## 4. Порядок

1. **A1 — сегодня и отдельным выпуском**: единственное место, где
   приложение удаляет то, чего не создавало. Вместе с ним C17 — тот же
   помощник. Помнить про пятый шаг A1: уже установленных чинит только смена
   имени архива.
2. **A2, A3, A5, A7** — по часу-двум каждое, тесты-сценарии уже описаны; A2
   и A3 временным тестом воспроизведены. Затем **A4** (замок экземпляра) —
   он закрывает второй вход в беду A2.
3. **A6 и A8, половины «в приложении»**: отказ честнее обещания. Правки
   форка — отдельной работой и своим issue; до них подписи в настройках
   говорят правду.
4. **A9** — главная функция на главной системе; затем этап B сверху вниз:
   B1–B4 — про то, что человек теряет при сбое, B5 — сначала issue.
5. **C1–C5** — пять малых правок про закрытие и журнал; остальной этап C —
   по мере работы рядом: загрузки — пачкой, и C14 вместе с правками форка.
6. **Этап E идёт параллельно со вторым шагом** и ничего не ломает: E1(а),
   E3 и E4 — сразу. Перед любым новым дроблением или разбором — E2 и E5:
   иначе следующая перекройка снова пройдёт стражей и снова что-нибудь
   сдвинет.
7. **F1–F3** — сразу, это ошибки; F8 — раньше F9–F10: после него файлов
   станет меньше, и открывать их придётся один раз.
8. **G2, G4–G6** — вечер; **G1** — до следующего релиза, в котором тронут
   обновление, раскладку сохранений или движок.

Темп. Три дня по пятьдесят коммитов — это и есть причина вывода 6. Для
этапов A и B — наоборот: правка, тест, **ручная проверка сценария на живых
данных**, и только потом следующая. После каждого шага — ворота из
`CLAUDE.md`, полный прогон и без `| tail`.

## 5. Чего в плане нет намеренно

- нового дробления виджетов и понижения порогов длины: следующая ступень —
  пробросы и замыкания (F8, E5), а не строки;
- возврата к `GameRepository`, блоку захвата кнопки и `WindowBloc`: решения
  в архиве обоснованы, и рецензии их подтвердили;
- смены формата `.evsave`: B5 с ним совместим, B10 манифест не трогает — и
  оба всё равно идут через issue;
- ухода с `dtorrent_task_v2`: A6, A8 и C14 лечатся правками форка; вопрос о
  замене встанет, только если автор их не примет, а держать форк станет
  дороже;
- хранения пароля прокси в связке ключей: новая зависимость с платформенным
  кодом ради того, что на POSIX закрыто правами, а на Windows — профилем
  пользователя; вернуться, если появится синхронизация настроек;
- `integration_test` на весь интерфейс: дымового запуска собранного
  приложения (G1) хватает, чтобы поймать то, чего тесты не видят вовсе.
